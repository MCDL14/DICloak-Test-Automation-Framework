# Dicloak 自动化框架开发进度同步

更新时间：2026-05-29

说明：

- `[x]` 表示已完成。
- `[ ]` 表示待完成。
- 本文档只同步开发实施进度，不记录计划书完善、需求沟通、文档整理类任务。

## 一、项目骨架

- [x] 创建项目目录结构。
- [x] 创建 `requirements.txt`。
- [x] 创建 `.gitignore`。
- [x] 创建 `README.md`。
- [x] 创建 `run.py` 命令行入口。
- [x] 创建 `launcher_ui.py` 启动界面预留入口。
- [x] 创建 `config/` 目录。
- [x] 创建 `core/` 目录。
- [x] 创建 `pages/` 目录。
- [x] 创建 `locators/` 目录。
- [x] 创建 `tests/` 目录。
- [x] 创建 `tests/p0/` 目录。
- [x] 创建 `tests/p0/environment_management/` 环境管理用例目录。
- [x] 创建 `tests/p0/proxy_management/` 代理管理用例目录。
- [x] 创建 `tests/p0/extension_management/` 扩展管理用例目录。
- [x] 创建 `tests/p0/environment_group_management/` 环境分组管理用例目录。
- [x] 创建 `tests/p0/member_management/` 成员管理用例目录。
- [x] 创建 `tests/p0/global_settings/` 全局设置用例目录。
- [x] 创建 `tests/p1/` 目录。
- [x] 创建 `test_data/` 目录。
- [x] 创建 `test_data/import/` 目录。
- [x] 创建 `test_data/export/` 目录。
- [x] 创建 `test_data/bookmarks/` 目录。
- [x] 创建 `test_data/members/` 目录。
- [x] 创建 `test_data/extensions/` 目录。
- [x] 创建 `logs/` 目录。
- [x] 创建 `reports/` 目录。
- [x] 创建 `screenshots/` 目录。

## 二、配置文件

- [x] 创建 `config/config.example.yaml`。
- [x] 创建本地 `config/config.yaml`。
- [x] 创建 `config/test_data.example.yaml`。
- [x] 创建本地 `config/test_data.yaml`。
- [x] 将测试数据配置从主配置拆分到独立测试数据文件。
- [x] 实现 `test_data_file` 配置项，支持主配置按路径加载测试数据文件。
- [x] 实现 APP 启动配置。
- [x] 实现 CDP 连接配置。
- [x] 实现账号配置。
- [x] 实现飞书配置。
- [x] 实现报告配置预留。
- [x] 实现运行参数配置。
- [x] 实现统一超时时间配置 `timeouts`，并填入当前默认等待值。
- [x] 实现日志配置。
- [x] 实现环境名称和内核环境配置。
- [x] 实现书签相关配置。
- [x] 实现成员导出相关配置。
- [x] 实现批量导入相关配置。
- [x] 实现批量导出相关配置。
- [x] 实现抓包工具相关配置。
- [x] 实现本地扩展包相关配置。
- [x] 配置真实 APP exe 路径。
- [x] 配置真实登录账号和密码。
- [x] 配置真实飞书 webhook。
- [x] 配置真实批量导入文件路径和文件名。
- [x] 配置真实批量导出文件路径和文件名。
- [x] 配置真实成员导出路径和文件名。
- [x] 配置真实成员导出预期文件完整路径。
- [x] 配置真实书签文件路径和文件名。
- [x] 配置真实抓包工具进程名和启动路径。
- [x] 配置真实本地扩展包名称和路径。
- [x] 将书签、批量导入、成员导出预期文件、本地扩展包、抓包工具启动文件、导出目录改为项目内 `test_data/` 相对路径。

## 三、Core 基础能力

- [x] 实现 `core/config.py` 配置读取。
- [x] 实现配置字段默认值处理。
- [x] 实现配置字段缺失校验。
- [x] 实现 `core/logger.py` 日志系统。
- [x] 实现日志文件按运行时间生成。
- [x] 实现日志保留天数清理。
- [x] 实现 `core/result.py` 执行结果统计。
- [x] 实现通过、失败、异常、跳过数量统计。
- [x] 实现通过率计算。
- [x] 实现失败用例摘要收集。
- [x] 实现失败截图统一采集：优先 CDP，失败后回退 mss 桌面截图，Windows 下再回退 UIAutomation 桌面截图。
- [x] 实现失败摘要和日志记录截图路径。
- [x] 实现 unittest result 层用例前后恢复钩子。
- [x] 实现全局 APP 稳定态恢复 `pages/app_page.py`，用于关闭阻塞弹窗、抽屉、下拉浮层并等待遮罩消失。
- [x] 实现模块级恢复分层设计，全局恢复不进入具体业务模块。
- [x] 实现环境管理模块 `recover_to_module_home()`，进入环境列表并清理环境管理模块筛选和选中状态。
- [x] 实现环境分组管理模块 `recover_to_module_home()`，进入环境分组列表并关闭阻塞浮层。
- [x] 实现 `core/assertions.py` 断言辅助。
- [x] 实现 `core/retry.py` 失败重试。
- [x] 实现重试日志记录。
- [x] 实现 flaky 标记。
- [x] 实现 `core/circuit_breaker.py` 模块级熔断。
- [x] 实现登录失败熔断。
- [x] 实现 APP 启动失败熔断。
- [x] 实现 CDP 连接失败熔断。
- [x] 实现环境管理模块熔断。
- [x] 实现内核模块熔断。
- [x] 实现批量导入模块熔断。
- [x] 实现 `core/runner.py` 用例运行编排。
- [x] 实现命令行参数解析。
- [x] 实现按级别运行用例。
- [x] 实现按模块运行用例。
- [x] 实现单模块文件或路径精确运行。
- [x] 实现按业务模块分类运行用例，支持环境管理、代理管理、扩展管理、环境分组管理、成员管理、全局设置。
- [x] 实现按单条用例运行。
- [x] 实现用例执行优先级排序：批量运行时 `test_01_kernel_integrity.py` 固定优先执行。
- [x] 实现退出码规则。

## 四、环境预检

- [x] 实现 `core/precheck.py`。
- [x] 检查 Python 版本。
- [x] 检查 APP `exe_path` 是否存在。
- [x] 检查 APP `work_dir` 是否存在。
- [x] 检查 APP 启动参数。
- [x] 检查批量导入文件完整路径是否存在。
- [x] 检查书签文件存放目录是否存在。
- [x] 检查成员导出预期正确文件是否存在。
- [x] 检查批量导出目录是否存在。
- [x] 检查抓包工具启动路径是否存在。
- [x] 检查本地扩展包完整路径是否存在。
- [x] 检查 CDP 端口占用情况。
- [x] 检查飞书 webhook 是否为空。
- [x] 实现预检结果日志输出。
- [x] 实现预检失败时取消自动化任务。
- [x] 实现预检失败退出码。

## 五、APP 生命周期

- [x] 实现 `core/app.py`。
- [x] 实现 Dicloak 进程检测。
- [x] 实现 Dicloak 进程检测的 `Get-Process` 兜底，避免 `tasklist` 被拒绝时误判。
- [x] 实现启动前检测已有 Dicloak APP。
- [x] 实现已有 Dicloak APP 强制结束，避免退出确认弹窗卡住流程。
- [x] 实现已有 APP 关闭超时处理。
- [x] 实现按配置启动 Dicloak APP。
- [x] 实现启动时附加 CDP 参数。
- [x] 实现调试模式 `--attach-existing-app`，支持只连接已手动启动的 APP。
- [x] 实现 APP 进程 30 秒检测。
- [x] 实现 APP 进程检测失败日志。
- [x] 实现 APP 进程检测失败飞书通知。
- [x] 实现进程检测失败后继续尝试 CDP 连接。
- [x] 实现 CDP 连接失败后取消自动化任务。
- [x] 实现测试结束后关闭 APP。

## 六、CDP 与页面控制

- [x] 实现 `core/cdp_driver.py`。
- [x] 实现通过 `http://127.0.0.1:9222/json` 获取页面列表。
- [x] 实现 Playwright `connect_over_cdp` 连接。
- [x] 实现运行器 CDP 启动检查后释放连接，避免和用例内 CDP 连接冲突。
- [x] 实现 WebSocket CDP 兜底连接。
- [x] 验证 CDP 目标页面识别方式。
- [x] 实现 CDP 默认页面选择跳过 DevTools，优先连接 Dicloak 主页面。
- [x] 实现按 URL 关键字选择页面。
- [x] 实现按页面标题选择页面。
- [x] 实现按页面关键元素选择页面。
- [x] 实现页面健康检查。
- [x] 实现元素查询。
- [x] 实现元素点击前等待元素可见且可用。
- [x] 实现按坐标点击元素。
- [x] 实现按脚本定位元素并由 Playwright 直接点击，避免表格按钮坐标点击偏移。
- [x] 实现脚本定位元素点击前等待元素出现、可见且可用。
- [x] 实现文本输入前等待元素可见且可用。
- [x] 实现文本读取前等待元素可见。
- [x] 实现元素、脚本元素、请求监听等待时间优先读取 `config.yaml`。
- [x] 实现等待元素出现。
- [x] 实现等待元素可点击。
- [x] 实现等待文本变化。
- [x] 实现页面截图。
- [x] 实现 CDP 连接失败错误输出。
- [x] 加固左侧业务菜单点击：环境管理入口改为点击菜单项容器，避免文字坐标贴近窗口顶部时点击不触发。

## 七、UIAutomation 能力

- [x] 实现 `core/ui_driver.py`。
- [x] 实现 APP 主窗口查找。
- [x] 实现原生控件查找。
- [x] 实现原生按钮点击。
- [x] 实现原生输入框输入。
- [x] 实现 Windows 文件选择器识别。
- [x] 实现 Windows 文件选择器输入文件路径。
- [x] 实现 Windows 文件选择器确认操作。
- [x] 实现原生弹窗识别。
- [x] 实现原生弹窗关闭或确认。
- [x] 实现桌面截图。
- [x] 实现控件查找失败日志。

## 八、飞书通知

- [x] 实现 `core/feishu.py`。
- [x] 实现 webhook 为空时跳过通知。
- [x] 实现文本消息 payload 组装。
- [x] 实现 @ 人内容组装。
- [x] 实现 Webhook 请求发送。
- [x] 实现 HTTP 状态码校验。
- [x] 实现飞书响应 `code == 0` 校验。
- [x] 实现请求异常处理。
- [x] 实现飞书测试通知。
- [x] 实现执行总结通知。
- [x] 实现失败告警通知。
- [x] 实现模块级熔断通知。

## 九、文件与测试数据处理

- [x] 实现导入文件完整路径拼接。
- [x] 实现批量导出文件完整路径拼接。
- [x] 实现成员导出文件完整路径拼接。
- [x] 实现完整路径存在性校验。
- [x] 实现导出文件生成等待。
- [x] 实现文件大小稳定性判断。
- [x] 实现 xlsx 文件读取。
- [x] 实现 xlsx 表头读取。
- [x] 实现 xlsx 行数据读取。
- [x] 实现书签覆盖数据生成。
- [x] 实现书签追加数据生成。
- [x] 实现用例创建数据记录。
- [x] 实现用例后置数据清理。
- [x] 实现清理失败日志记录。

## 十、页面对象

- [x] 创建 `pages/login_page.py`。
- [x] 实现登录。
- [x] 实现判断是否已登录。
- [x] 实现读取当前登录账号。
- [x] 实现账号不一致时退出重登。
- [x] 实现登录页可见输入框兜底登录。
- [x] 实现读取当前团队。
- [x] 实现自动化账号切换到目标团队。
- [x] 实现团队列表加载等待。
- [x] 实现目标团队切换后校验。
- [x] 实现登录失败识别。
- [x] 创建 `pages/environment_page.py`。
- [x] 实现进入环境列表。
- [x] 实现创建环境。
- [x] 实现查询环境。
- [x] 实现打开环境。
- [x] 实现关闭环境。
- [x] 实现删除环境。
- [x] 实现读取环境列表数据。
- [x] 创建 `pages/kernel_page.py`。
- [x] 实现进入内核管理。
- [x] 实现读取内核列表。
- [x] 实现删除指定内核。
- [x] 实现下载指定内核。
- [x] 实现读取环境内核版本号。
- [x] 创建 `pages/import_page.py`。
- [x] 实现进入批量导入。
- [x] 实现选择导入文件。
- [x] 实现提交导入。
- [x] 实现读取导入结果。
- [x] 实现识别导入失败环境。

## 十一、元素定位

- [x] 创建 `locators/login_locators.yaml`。
- [x] 创建 `locators/environment_locators.yaml`。
- [x] 创建 `locators/kernel_locators.yaml`。
- [x] 创建 `locators/import_locators.yaml`。
- [x] 验证登录页元素定位。
- [x] 验证环境列表页元素定位。
- [x] 验证环境创建元素定位。
- [x] 验证环境打开元素定位。
- [x] 验证环境关闭元素定位。
- [x] 验证环境删除元素定位。
- [x] 验证环境分组筛选下拉框元素定位。
- [x] 验证环境列表备注列读取和筛选结果定位。
- [x] 验证环境行内置顶和取消置顶菜单定位。
- [x] 验证环境名称列快捷编辑入口和弹窗定位。
- [x] 验证环境序号表头升序/降序排序按钮定位。
- [x] 验证列表字段设置入口和字段拖拽排序定位。
- [x] 验证内核管理元素定位。
- [x] 验证内核版本读取元素定位。
- [x] 验证批量导入入口元素定位。
- [x] 验证导入结果元素定位。
- [x] 验证文件上传可通过 DOM input 完成。
- [x] 实现 Windows 文件选择器兜底定位。

## 十二、P0 用例

- [x] 创建 `tests/p0/environment_management/test_01_kernel_integrity.py`。
- [x] P0-内核完整性校验：进入个人设置。
- [x] P0-内核完整性校验：进入基础设置。
- [x] P0-内核完整性校验：读取环境缓存目录，并增加页面加载等待与可见输入框兜底匹配。
- [x] P0-内核完整性校验：清空环境缓存目录下的 `browsers` 子目录。
- [x] P0-内核完整性校验：返回环境管理。
- [x] P0-内核完整性校验：读取首行环境名称并按需筛选 `142`。
- [x] P0-内核完整性校验：点击目标环境打开按钮。
- [x] P0-内核完整性校验：监听 `open_env` 请求并解析内核主进程 PID。
- [x] P0-内核完整性校验：通过内核主进程 PID 读取命令行请求参数并解析内核 CDP 调试端口。
- [x] P0-内核完整性校验：失败时尝试关闭已打开环境，避免遗留运行状态。
- [x] P0-内核完整性校验：等待指定内核 PID 启动。
- [x] P0-内核完整性校验：解析并校验指定内核 PID 的 CDP 端口。
- [x] P0-内核完整性校验：校验打开按钮变为关闭。
- [x] P0-内核完整性校验：优先通过内核 CDP 读取真实版本，兜底从命令行解析并校验 142 前缀。
- [x] P0-内核完整性校验：点击目标环境关闭按钮。
- [x] P0-内核完整性校验：等待指定内核 PID 关闭。
- [x] P0-内核完整性校验：校验关闭按钮变回打开。
- [x] P0-内核完整性校验：真实执行验证。
- [x] P0-内核完整性校验：刷新 APP 后校验 `browsers` 目录存在 142 版本内核文件夹。
- [x] P0-内核完整性校验：再次打开 142 环境，并通过内核 PID 校验启动路径位于 142 版本缓存目录。
- [x] P0-内核完整性校验：清理下载记录中除第一行外的内核记录，并校验删除结果。
- [x] P0-内核完整性校验：下载最新 134 内核，并校验 `browsers` 目录存在 134 版本内核文件夹。
- [x] P0-内核完整性校验：筛选并打开 134 内核环境，校验内核版本号。
- [x] P0-内核完整性校验：点击环境列表搜索按钮右侧第二个按钮清除筛选状态。
- [x] P0-内核完整性校验：补充步骤真实执行验证。
- [x] 实现环境列表用例结束时统一清除筛选状态，避免影响后续流程。
- [x] 创建 `tests/p0/environment_management/test_02_create_default_environment.py`。
- [x] P0-创建默认配置环境：点击创建环境并填写环境名称。
- [x] P0-创建默认配置环境：提交默认配置环境。
- [x] P0-创建默认配置环境：校验环境列表存在新建环境。
- [x] P0-创建默认配置环境：打开环境并校验按钮变为关闭。
- [x] P0-创建默认配置环境：关闭环境并校验按钮变回打开。
- [x] P0-创建默认配置环境：点击打开按钮右侧更多 icon。
- [x] P0-创建默认配置环境：通过更多菜单删除环境并确认二次弹窗。
- [x] P0-创建默认配置环境：校验环境已删除。
- [x] P0-创建默认配置环境：真实执行验证。
- [x] P0-创建默认配置环境：调整为开头搜索清理残留、删除后二次搜索确认、用例结束兜底清除筛选。
- [x] 创建 `tests/p0/environment_management/test_03_batch_create_environments.py`。
- [x] P0-批量创建 5 个环境：进入环境管理页。
- [x] P0-批量创建 5 个环境：点击批量创建并填写创建数量。
- [x] P0-批量创建 5 个环境：填写环境名称前缀并提交。
- [x] P0-批量创建 5 个环境：按前缀筛选并校验 5 个环境创建成功。
- [x] P0-批量创建 5 个环境：逐行点击最左侧选择框选中 5 个环境。
- [x] P0-批量创建 5 个环境：点击批量工具栏“打开环境”。
- [x] P0-批量创建 5 个环境：校验 5 个环境均打开成功并逐个校验按钮状态。
- [x] P0-批量创建 5 个环境：点击批量工具栏“关闭环境”并确认二次弹窗。
- [x] P0-批量创建 5 个环境：校验 5 个环境均关闭成功并逐个校验按钮状态。
- [x] P0-批量创建 5 个环境：通过顶部批量工具栏“更多操作”里的“删除环境”删除 5 个环境，并确认删除成功。
- [x] P0-批量创建 5 个环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_04_create_134_kernel_environment.py`。
- [x] P0-创建 134 内核环境：点击创建环境并填写环境名称。
- [x] P0-创建 134 内核环境：展开“指纹设置”。
- [x] P0-创建 134 内核环境：点击“更多指纹”。
- [x] P0-创建 134 内核环境：点击“浏览器内核”下拉框并选择 `ChromeBrowser 134`。
- [x] P0-创建 134 内核环境：提交创建并校验环境创建成功。
- [x] P0-创建 134 内核环境：打开环境并校验按钮变为关闭。
- [x] P0-创建 134 内核环境：通过内核 CDP 校验版本号为 134 前缀。
- [x] P0-创建 134 内核环境：关闭环境并校验按钮变回打开。
- [x] P0-创建 134 内核环境：删除环境并确认删除成功。
- [x] P0-创建 134 内核环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_05_batch_create_134_kernel_environments.py`。
- [x] P0-批量创建 134 内核环境：新增批量创建 134 内核环境配置，创建数量为 3。
- [x] P0-批量创建 134 内核环境：点击批量创建，填写环境名称前缀和创建数量。
- [x] P0-批量创建 134 内核环境：展开“指纹设置”，点击“更多指纹”，选择 `ChromeBrowser 134`。
- [x] P0-批量创建 134 内核环境：校验 3 个环境创建成功。
- [x] P0-批量创建 134 内核环境：批量打开环境并收集 3 个 `/open_env` 请求里的内核 PID。
- [x] P0-批量创建 134 内核环境：加固批量打开串行 `/open_env` 捕获，支持页面 `fetch`/`XMLHttpRequest` 调用点监听。
- [x] P0-批量创建 134 内核环境：逐个校验环境按钮状态变为关闭。
- [x] P0-批量创建 134 内核环境：逐个通过内核 PID 解析 CDP 端口并校验版本号为 134 前缀。
- [x] P0-批量创建 134 内核环境：批量关闭环境并逐个校验按钮状态变回打开。
- [x] P0-批量创建 134 内核环境：通过顶部批量工具栏“更多操作”里的“删除环境”删除 3 个环境，并确认删除成功。
- [x] P0-批量创建 134 内核环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_06_batch_import_environments.py`。
- [x] P0-批量导入环境：点击批量创建右侧下拉按钮并选择“批量导入”。
- [x] P0-批量导入环境：通过隐藏 `input[type=file]` 上传配置文件里的 xlsx。
- [x] P0-批量导入环境：提交导入并等待“导入结果”弹窗。
- [x] P0-批量导入环境：校验导入结果为 3 条成功、0 条失败。
- [x] P0-批量导入环境：关闭导入结果弹窗后回到环境管理。
- [x] P0-批量导入环境：在环境列表校验 xlsx 中 3 个环境创建成功。
- [x] P0-批量导入环境：删除导入产生的 3 个环境并确认删除成功。
- [x] P0-批量导入环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_07_edit_environment_name.py`。
- [x] P0-编辑环境名称：读取环境列表第一个环境的环境序号和原始名称。
- [x] P0-编辑环境名称：通过该环境行最右侧更多按钮进入“编辑”。
- [x] P0-编辑环境名称：修改名称为 `自动化-编辑环境名称` 并提交。
- [x] P0-编辑环境名称：通过环境序号精准校验名称修改成功。
- [x] P0-编辑环境名称：再次通过环境序号进入编辑并还原原始名称。
- [x] P0-编辑环境名称：通过环境序号精准校验名称还原成功。
- [x] P0-编辑环境名称：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_08_edit_fixed_open_url.py`。
- [x] P0-编辑固定打开网址：筛选预置环境 `自动化编辑打开网址`。
- [x] P0-编辑固定打开网址：进入编辑环境并在“高级设置 - 固定打开网页”写入 `https://bilibili.com`。
- [x] P0-编辑固定打开网址：打开环境并通过内核 CDP 校验 `bilibili.com` 标签页自动打开。
- [x] P0-编辑固定打开网址：通过内核 CDP 关闭 `bilibili.com` 标签页后关闭环境。
- [x] P0-编辑固定打开网址：再次编辑环境并清空“固定打开网页”。
- [x] P0-编辑固定打开网址：再次打开环境并校验不会自动打开 `bilibili.com` 标签页。
- [x] P0-编辑固定打开网址：关闭环境并清除环境列表筛选。
- [x] P0-编辑固定打开网址：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_09_filter_environment_group.py`。
- [x] P0-环境列表筛选-自动化分组：点击环境列表“环境分组”筛选框。
- [x] P0-环境列表筛选-自动化分组：在下拉选项中选择 `自动化分组`。
- [x] P0-环境列表筛选-自动化分组：点击搜索按钮刷新列表。
- [x] P0-环境列表筛选-自动化分组：校验当前列表每一行“环境分组”列均为 `自动化分组`。
- [x] P0-环境列表筛选-自动化分组：用例结束时清除筛选。
- [x] P0-环境列表筛选-自动化分组：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_10_filter_environment_remark.py`。
- [x] P0-环境列表备注筛选：在“序号/名称/备注”筛选框输入 `备注UI自动化`。
- [x] P0-环境列表备注筛选：点击搜索按钮刷新列表。
- [x] P0-环境列表备注筛选：校验当前列表每一行“备注”列均包含 `备注UI自动化`。
- [x] P0-环境列表备注筛选：用例结束时清除筛选。
- [x] P0-环境列表备注筛选：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_11_top_environment.py`。
- [x] P0-置顶环境：获取环境列表第三个环境的环境序号。
- [x] P0-置顶环境：点击该环境行内最右侧更多选项入口。
- [x] P0-置顶环境：在下拉菜单点击“置顶”。
- [x] P0-置顶环境：等待 `env/batch/top` 接口响应完成。
- [x] P0-置顶环境：校验环境列表第一个环境序号等于刚刚记录的环境序号。
- [x] P0-置顶环境：再次点击该环境行内更多选项入口。
- [x] P0-置顶环境：在下拉菜单点击“取消置顶”。
- [x] P0-置顶环境：等待 `env/batch/top` 接口响应完成。
- [x] P0-置顶环境：校验环境列表第一个环境序号不再等于刚刚记录的环境序号。
- [x] P0-置顶环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_12_quick_edit_environment_name.py`。
- [x] P0-列表快捷修改环境名称：获取第一行环境名称和环境序号。
- [x] P0-列表快捷修改环境名称：点击该环境名称。
- [x] P0-列表快捷修改环境名称：点击该环境名称右侧快捷编辑入口。
- [x] P0-列表快捷修改环境名称：在编辑弹窗内修改名称为 `自动化-列表快捷修改环境名称` 并点击确定。
- [x] P0-列表快捷修改环境名称：按环境序号校验名称修改成功。
- [x] P0-列表快捷修改环境名称：再次通过名称右侧快捷编辑入口还原原始名称。
- [x] P0-列表快捷修改环境名称：按环境序号校验名称还原成功。
- [x] P0-列表快捷修改环境名称：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_13_sort_environment_serial.py`。
- [x] P0-环境序号升降序：读取当前激活排序方向，并点击当前方向取消排序状态。
- [x] P0-环境序号升降序：点击“环境序号”升序按钮。
- [x] P0-环境序号升降序：校验当前列表环境序号符合升序规则。
- [x] P0-环境序号升降序：点击“环境序号”降序按钮。
- [x] P0-环境序号升降序：校验当前列表环境序号符合降序规则。
- [x] P0-环境序号升降序：用例结束后保持“环境序号”降序状态。
- [x] P0-环境序号升降序：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_14_move_remark_column.py`。
- [x] P0-字段排序-备注调整到首位：点击列表表头最右侧字段设置按钮。
- [x] P0-字段排序-备注调整到首位：在“列表字段设置”窗口中将“备注”拖动到首位。
- [x] P0-字段排序-备注调整到首位：点击确定并校验列表第一列变为“备注”。
- [x] P0-字段排序-备注调整到首位：再次打开字段设置窗口。
- [x] P0-字段排序-备注调整到首位：将“备注”拖动回“环境名称”字段下面。
- [x] P0-字段排序-备注调整到首位：点击确定并校验“备注”列恢复到“环境名称”后面。
- [x] P0-字段排序-备注调整到首位：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_15_export_environment.py`。
- [x] P0-导出环境：在“序号/名称/备注”筛选框输入 `导出环境` 并点击搜索按钮。
- [x] P0-导出环境：点击列表表头最左侧全选框，选中当前筛选结果。
- [x] P0-导出环境：通过顶部批量工具栏“更多操作”进入“导出环境 -> 导出所选”。
- [x] P0-导出环境：在“导出环境”弹窗点击确定。
- [x] P0-导出环境：优先捕获 Chromium 下载事件，兜底处理 Windows 原生保存弹窗，按配置路径保存导出文件。
- [x] P0-导出环境：等待导出文件生成并校验文件存在且非空。
- [x] P0-导出环境：筛选后收集当前列表环境序号、名称、备注、分组，并与导出的 xlsx 数据行进行一致性校验。
- [x] 创建 `tests/p0/environment_management/test_16_create_multi_group_environment.py`。
- [x] P0-创建多分组环境：点击创建环境并填写环境名称。
- [x] P0-创建多分组环境：读取创建抽屉里已选环境分组，保留并纳入预期校验。
- [x] P0-创建多分组环境：补选 `分组三` 和 `分组二`。
- [x] P0-创建多分组环境：提交创建并校验环境列表里的环境分组符合预期。
- [x] P0-创建多分组环境：删除环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_17_batch_create_multi_group_environments.py`。
- [x] P0-批量创建多分组环境：点击批量创建并填写创建数量 `3`。
- [x] P0-批量创建多分组环境：填写环境名称前缀 `自动化-批量创建多分组环境`。
- [x] P0-批量创建多分组环境：读取批量创建抽屉里已选环境分组，保留并纳入预期校验。
- [x] P0-批量创建多分组环境：补选 `分组三` 和 `分组二`。
- [x] P0-批量创建多分组环境：提交批量创建并校验 3 个环境创建成功。
- [x] P0-批量创建多分组环境：逐个点击环境分组列 `查看` 并校验真实气泡窗里的完整分组内容。
- [x] P0-批量创建多分组环境：删除 3 个环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_18_edit_single_environment_multi_group.py`。
- [x] P0-单环境编辑多分组：获取列表第一个环境的环境序号。
- [x] P0-单环境编辑多分组：通过环境序号点击更多操作并进入编辑。
- [x] P0-单环境编辑多分组：读取并保留编辑抽屉中已有环境分组。
- [x] P0-单环境编辑多分组：追加选择 `分组二`，点击确定并处理二次确认。
- [x] P0-单环境编辑多分组：按环境序号匹配列表行并校验分组修改成功。
- [x] P0-单环境编辑多分组：再次进入编辑并取消选择 `分组二`。
- [x] P0-单环境编辑多分组：按环境序号匹配列表行并校验分组还原成功。
- [x] 创建 `tests/p0/environment_management/test_19_batch_edit_environment_multi_group.py`。
- [x] P0-批量编辑环境多分组：获取列表前三个环境的环境序号和当前分组。
- [x] P0-批量编辑环境多分组：按环境序号依次勾选前三个环境。
- [x] P0-批量编辑环境多分组：通过顶部批量工具栏“更多操作”进入“设置环境分组”。
- [x] P0-批量编辑环境多分组：修改方式选择 `追加`，选择 `分组二` 并提交。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组追加成功。
- [x] P0-批量编辑环境多分组：再次勾选前三个环境，修改方式选择 `覆盖`。
- [x] P0-批量编辑环境多分组：选择 `分组三` 和 `分组二` 并提交。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组覆盖成功。
- [x] P0-批量编辑环境多分组：最后再次勾选前三个环境，通过 `覆盖` 选择 `未分组`。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组重置为 `未分组`。
- [x] 创建 `tests/p0/environment_management/test_20_create_tag.py`。
- [x] P0-创建标签：点击清除筛选按钮右侧按钮，打开“标签管理”弹窗。
- [x] P0-创建标签：点击“创建标签”，填写标签名称 `自动化-创建标签` 并点击创建。
- [x] P0-创建标签：在标签列表中校验标签创建成功。
- [x] P0-创建标签：点击目标标签行“操作”列第二个按钮删除标签。
- [x] P0-创建标签：在标签列表中校验标签删除成功。
- [x] P0-创建标签：关闭“标签管理”弹窗。
- [x] 创建 `tests/p0/environment_management/test_21_create_environment_with_tags.py`。
- [x] P0-创建带有标签的环境：点击创建环境并填写环境名称 `自动化-创建带有标签的环境`。
- [x] P0-创建带有标签的环境：点击 `设置标签`。
- [x] P0-创建带有标签的环境：在 `请选择/输入创建标签` 输入框中输入并选择 `标签1`。
- [x] P0-创建带有标签的环境：修改输入框内容并选择 `标签2`。
- [x] P0-创建带有标签的环境：提交创建并在列表中校验环境存在。
- [x] P0-创建带有标签的环境：校验环境列表行中包含 `标签1` 和 `标签2`。
- [x] P0-创建带有标签的环境：删除环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_22_batch_create_environments_with_tags.py`。
- [x] P0-批量创建带有标签的环境：点击批量创建，创建数量输入 `3`，名称输入 `自动化-批量创建带有标签的环境`。
- [x] P0-批量创建带有标签的环境：点击 `设置标签`。
- [x] P0-批量创建带有标签的环境：在 `请选择/输入创建标签` 输入框中输入并选择 `标签1`。
- [x] P0-批量创建带有标签的环境：修改输入框内容并选择 `标签2`。
- [x] P0-批量创建带有标签的环境：提交创建并校验环境名称、数量和标签内容。
- [x] P0-批量创建带有标签的环境：删除 3 个环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_23_batch_edit_environment_tags.py`。
- [x] P0-批量编辑环境标签：记录当前列表前三个环境的环境序号和原有标签内容。
- [x] P0-批量编辑环境标签：选择前三个环境，点击 `更多操作` -> `编辑标签`。
- [x] P0-批量编辑环境标签：选择 `追加标签`，输入并选择 `标签7`，提交后校验标签追加成功且原有标签仍保留。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签`。
- [x] P0-批量编辑环境标签：选择 `覆盖标签`，输入并选择 `标签5`、`标签1`，提交后按环境序号校验标签已覆盖。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签` 并选择 `清空标签`。
- [x] P0-批量编辑环境标签：处理清空标签二次确认，并按环境序号校验标签已清空。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签` 并选择 `覆盖标签`。
- [x] P0-批量编辑环境标签：输入并选择 `标签1`，提交后按环境序号校验标签均为 `标签1`。
- [x] 创建 `tests/p0/environment_management/test_24_edit_environment_tags.py`。
- [x] P0-编辑环境标签：记录列表第一个环境的环境序号和原始标签。
- [x] P0-编辑环境标签：进入编辑抽屉，设置标签为原始标签加 `标签9` 并提交。
- [x] P0-编辑环境标签：按环境序号校验标签编辑成功。
- [x] P0-编辑环境标签：再次进入编辑抽屉，按原始标签集合还原并提交。
- [x] P0-编辑环境标签：按环境序号校验标签还原成功。
- [x] 创建 `tests/p0/environment_management/test_25_filter_environment_tag.py`。
- [x] P0-筛选标签：点击环境列表搜索按钮右侧、清除筛选按钮左侧的更多筛选入口。
- [x] P0-筛选标签：在更多筛选抽屉的 `标签` 输入框中输入并选择 `标签2`。
- [x] P0-筛选标签：点击 `立即筛选`。
- [x] P0-筛选标签：按列表环境序号读取 `标签` 列，校验筛选结果均包含 `标签2`。
- [x] P0-筛选标签：点击清除筛选按钮恢复列表。
- [x] 创建 `pages/global_settings_page.py`。
- [x] 创建 `core/kernel_cdp.py`，用于通过内核 CDP 新建页面、访问内核地址并读取页面错误文本。
- [x] 创建 `tests/p0/global_settings/test_01_disable_view_password.py`。
- [x] P0-校验禁止查看网站密码：进入 `全局设置` 页面。
- [x] P0-校验禁止查看网站密码：定位 `禁止查看网站密码` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-校验禁止查看网站密码：进入 `环境管理`，在 `序号/名称/备注` 输入框搜索 `142`。
- [x] P0-校验禁止查看网站密码：打开筛选后列表第一个环境并解析内核 CDP 端口。
- [x] P0-校验禁止查看网站密码：在内核中访问 `chrome://password-manager/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-校验禁止查看网站密码：控制内核访问 `https://www.bilibili.com`。
- [x] P0-校验禁止查看网站密码：点击 B 站登录入口，打开登录弹窗。
- [x] P0-校验禁止查看网站密码：在登录弹窗密码输入框输入 `12345678`。
- [x] P0-校验禁止查看网站密码：点击密码框的小眼睛按钮。
- [x] P0-校验禁止查看网站密码：断言点击小眼睛后密码仍未以明文显示。
- [x] P0-校验禁止查看网站密码：关闭 B 站测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，并取消筛选。
- [x] 创建 `tests/p0/global_settings/test_02_disable_browser_devtools.py`。
- [x] P0-禁止打开浏览器开发者工具：进入 `全局设置` 页面。
- [x] P0-禁止打开浏览器开发者工具：定位 `禁止打开浏览器开发者工具界面` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止打开浏览器开发者工具：进入 `环境管理`，在 `序号/名称/备注` 输入框搜索 `142`。
- [x] P0-禁止打开浏览器开发者工具：打开筛选后列表第一个环境并解析内核 CDP 端口。
- [x] P0-禁止打开浏览器开发者工具：控制内核访问 `https://www.bilibili.com`。
- [x] P0-禁止打开浏览器开发者工具：尝试通过 `F12`、`Ctrl+Shift+I`、右键检查快捷路径打开开发者工具。
- [x] P0-禁止打开浏览器开发者工具：校验内核 CDP target 列表中没有打开 `devtools://` 或开发者工具 target。
- [x] P0-禁止打开浏览器开发者工具：关闭 B 站测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，并取消筛选。
- [x] P0-禁止管理/移除扩展：进入 `全局设置` 页面，定位 `禁止管理/移除扩展，以及从本地安装扩展至浏览器` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止管理/移除扩展：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止管理/移除扩展：控制内核访问 `chrome://extensions/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止管理/移除扩展：控制内核访问 Chrome Web Store 指定扩展页，点击 `添加至 Chrome` 并校验安装没有成功进入已安装状态。
- [x] P0-禁止管理/移除扩展：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置取消该选项并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_04_disable_member_access_google_extension_pages.py`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：进入 `全局设置` 页面，定位 `禁止成员访问谷歌扩展商店和扩展设置页面` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：控制内核访问 `chrome://extensions/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：紧接着访问 Chrome Web Store 指定扩展页，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置取消该选项并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_05_block_specific_websites_google_and_baidu.py`。
- [x] P0-禁止访问指定网址：进入 `全局设置` 页面，打开 `访问网站限制` 功能开关。
- [x] P0-禁止访问指定网址：将 `限制方式` 设置为 `禁止访问指定网址`，并勾选快捷选择中的 `谷歌应用商店`。
- [x] P0-禁止访问指定网址：清空 `网址列表` 后输入 `https://baidu.com` 并点击 `确定` 保存。
- [x] P0-禁止访问指定网址：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止访问指定网址：控制内核依次访问 `https://baidu.com`、`https://chromewebstore.google.com`、`https://bilibili.com`，校验百度和谷歌扩展商店包含 `ERR_BLOCKED_BY_CLIENT`，b 站正常加载。
- [x] P0-禁止访问指定网址：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置关闭 `访问网站限制` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_06_allow_specific_website_bilibili.py`。
- [x] P0-允许访问指定网址：进入 `全局设置` 页面，打开 `访问网站限制` 功能开关。
- [x] P0-允许访问指定网址：将 `限制方式` 设置为 `允许访问指定网址`，清空 `网址列表` 后输入 `https://bilibili.com` 并点击 `确定` 保存。
- [x] P0-允许访问指定网址：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-允许访问指定网址：控制内核依次访问 `https://bilibili.com`、`https://chromewebstore.google.com`、`https://baidu.com`，校验 b 站正常加载，谷歌扩展商店和百度均被 `ERR_BLOCKED_BY_CLIENT` 拦截。
- [x] P0-允许访问指定网址：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置关闭 `访问网站限制` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_07_disable_packet_capture_software.py`。
- [x] P0-禁用抓包软件：进入 `全局设置` 页面，打开 `禁用抓包软件` 功能开关。
- [x] P0-禁用抓包软件：读取 `test_data.packet_capture.process_name`，清空 `软件名称` 输入框并写入抓包软件进程名，点击 `确定` 保存。
- [x] P0-禁用抓包软件：读取 `test_data.packet_capture.startup_path` 启动抓包软件，并确认配置的抓包进程存在。
- [x] P0-禁用抓包软件：进入 `环境管理`，点击列表第一个环境的 `打开`，校验出现 `禁止打开环境` 弹窗，并校验内核进程未启动、环境按钮仍为 `打开`。
- [x] P0-禁用抓包软件：关闭 `禁止打开环境` 弹窗，关闭抓包软件后再次打开同一环境，校验不再弹窗、内核进程存在、环境按钮变为 `关闭`。
- [x] P0-禁用抓包软件：关闭环境并校验按钮恢复为 `打开`，回到全局设置关闭 `禁用抓包软件` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_08_bookmark_setting_overwrite.py`。
- [x] P0-书签设置-覆盖：打开 142 环境并在内核中创建临时书签，确认原始书签存在。
- [x] P0-书签设置-覆盖：进入 `全局设置`，配置书签设置为覆盖模式并上传覆盖书签文件。
- [x] P0-书签设置-覆盖：重新打开环境，通过内核 CDP 校验上传书签存在，临时书签已被覆盖删除。
- [x] P0-书签设置-覆盖：关闭环境、清理筛选并关闭书签设置开关。
- [x] 创建 `tests/p0/global_settings/test_09_bookmark_setting_append.py`。
- [x] P0-书签设置-追加：打开 142 环境，读取或创建原始书签作为追加前基线。
- [x] P0-书签设置-追加：进入 `全局设置`，配置书签设置为追加模式并上传追加书签文件。
- [x] P0-书签设置-追加：重新打开环境，通过内核 CDP 校验原始书签和追加书签同时存在。
- [x] P0-书签设置-追加：配置覆盖清空规则，重新打开环境并校验书签已清空。
- [x] P0-书签设置-追加：关闭环境、清理筛选并关闭书签设置开关。
- [x] 创建 `tests/p0/global_settings/test_10_environment_field_display_limit.py`。
- [x] P0-环境字段显示限制：记录环境列表原始表头并确认字段设置入口可见。
- [x] P0-环境字段显示限制：进入 `全局设置`，配置环境字段显示限制为 `环境序号`、`环境名称`。
- [x] P0-环境字段显示限制：返回环境列表，校验字段设置入口隐藏且列表表头只保留指定字段。
- [x] P0-环境字段显示限制：关闭限制后校验字段设置入口恢复可用，表头恢复到原始状态。
- [x] 创建 `tests/p0/global_settings/test_11_environment_list_pagination_setting.py`。
- [x] P0-环境列表分页设置：进入 `全局设置`，配置环境列表分页固定为 `20 条/页`。
- [x] P0-环境列表分页设置：刷新并进入环境列表，校验分页选择器隐藏且当前页行数符合固定分页。
- [x] P0-环境列表分页设置：关闭分页限制后校验分页选择器恢复可见，并恢复为 `10条/页`。
- [x] 创建 `tests/p0/global_settings/test_12_environment_list_sort_limit.py`。
- [x] P0-环境列表排序设置：进入 `全局设置`，配置环境列表按 `环境序号` 升序。
- [x] P0-环境列表排序设置：刷新并进入环境列表，校验表头排序按钮隐藏且环境序号升序。
- [x] P0-环境列表排序设置：切换为 `环境序号` 降序并校验列表降序。
- [x] P0-环境列表排序设置：关闭排序限制后校验排序按钮恢复可见，并恢复环境序号降序。
- [x] 创建 `locators/environment_group_locators.yaml`，集中维护环境分组模块的菜单、按钮、弹窗、表格行和输入框定位。
- [x] 整理 `locators/environment_group_locators.yaml`，补充筛选模式切换、搜索/清除、下拉菜单、表格单元格、行内编辑、行内操作、授权成员悬浮窗等统一元素，并补齐每个元素的人工查看注释。
- [x] 整理 `pages/environment_group_page.py`，将环境分组通用 CSS 选择器改为读取 locator 文件，页面对象保留按业务文本、分组 ID、列内容判断的动态逻辑。
- [x] 创建 `pages/environment_group_page.py`，封装进入环境分组、创建分组、删除分组、二次确认、校验存在/不存在等操作。
- [x] 接入环境分组模块级恢复，失败恢复时进入环境分组列表并关闭阻塞浮层。
- [x] 创建 `tests/p0/environment_group_management/test_01_create_environment_group.py`。
- [x] P0-创建环境分组：进入环境分组页面。
- [x] P0-创建环境分组：如果存在历史分组 `自动化-创建环境分组`，先删除并处理二次确认。
- [x] P0-创建环境分组：点击 `创建环境分组`，填写分组名称并提交。
- [x] P0-创建环境分组：在列表中校验分组创建成功。
- [x] P0-创建环境分组：删除该分组并校验删除成功。
- [x] 创建 `tests/p0/environment_group_management/test_02_group_containing_environment.py`。
- [x] P0-包含环境的分组：进入环境分组页面，按需清理历史分组 `自动化-包含环境的分组`。
- [x] P0-包含环境的分组：创建分组并校验创建成功。
- [x] P0-包含环境的分组：前往环境列表，点击创建环境，记录默认环境名称。
- [x] P0-包含环境的分组：清空创建环境抽屉中默认环境分组，只选择 `自动化-包含环境的分组` 后提交。
- [x] P0-包含环境的分组：校验环境创建成功，并校验该环境的环境分组正确。
- [x] P0-包含环境的分组：回到环境分组页面，在 `包含环境` 筛选框输入记录的环境名称，并从下拉框中选择该环境。
- [x] P0-包含环境的分组：校验环境分组列表筛选结果均包含目标环境，且返回目标分组。
- [x] P0-包含环境的分组：点击筛选行最右侧的 `清除筛选` 按钮，校验筛选状态清除后目标分组仍可见。
- [x] P0-包含环境的分组：回到环境分组页面，删除该分组时勾选 `删除该分组下的环境`。
- [x] P0-包含环境的分组：校验环境分组删除成功。
- [x] P0-包含环境的分组：前往环境列表校验刚创建的环境已被删除，随后立即清除筛选并回到环境分组页面。
- [x] 创建 `locators/member_locators.yaml`，集中维护成员列表、编辑弹窗、环境分组多选和行内编辑按钮定位。
- [x] 创建 `pages/member_page.py`，封装进入成员列表、打开编辑内部成员弹窗、读取/追加成员授权环境分组、校验成员列表授权分组回显等操作。
- [x] 创建 `tests/p0/environment_group_management/test_03_group_authorized_member.py`。
- [x] P0-授权成员的分组：进入环境分组页面，按需清理历史分组 `自动化-授权成员的分组`。
- [x] P0-授权成员的分组：创建环境分组并在列表中校验创建成功。
- [x] P0-授权成员的分组：前往成员列表，打开 `自动化成员1` 的编辑内部成员弹窗。
- [x] P0-授权成员的分组：记录成员原本勾选的环境分组，并追加勾选本次创建的环境分组后提交。
- [x] P0-授权成员的分组：回到环境分组页面，点击新建分组授权成员列的 `查看`，校验悬浮窗包含 `自动化成员1`。
- [x] P0-授权成员的分组：通过 `授权成员` 筛选框选择 `自动化成员1`，校验筛选结果包含原始分组和新建分组。
- [x] P0-授权成员的分组：清除筛选内容，删除新建环境分组并确认二次弹窗。
- [x] P0-授权成员的分组：校验分组删除成功后，前往成员列表确认 `自动化成员1` 的授权环境分组恢复为原本分组，再回到环境分组页面。
- [x] 创建 `tests/p0/environment_group_management/test_04_filter_group_name.py`。
- [x] P0-环境分组名称筛选：进入环境分组页面。
- [x] P0-环境分组名称筛选：找到 `分组名称` 筛选框右侧切换模式按钮，切换到 `备注`。
- [x] P0-环境分组名称筛选：输入 `勿动！！！` 并点击搜索按钮。
- [x] P0-环境分组名称筛选：校验筛选后列表存在数据，且列表中所有备注都匹配 `勿动！！！`。
- [x] P0-环境分组名称筛选：再次切换回 `分组名称`，点击 `清除筛选` 按钮，校验筛选内容清除。
- [x] 创建 `tests/p0/environment_group_management/test_05_edit_group_name.py`。
- [x] P0-修改环境分组名称：进入环境分组页面，找到列表中第一个有编辑入口的分组。
- [x] P0-修改环境分组名称：记录该分组名称和名称下方的 ID。
- [x] P0-修改环境分组名称：点击该分组行的编辑按钮，在 `编辑环境分组` 弹窗中将名称修改为 `自动化-修改环境分组名称` 并点击确定。
- [x] P0-修改环境分组名称：根据记录的 ID 找到同一分组，校验名称修改成功。
- [x] P0-修改环境分组名称：再次编辑该分组，将名称还原为第一步记录的名称并点击确定。
- [x] P0-修改环境分组名称：根据记录的 ID 找到同一分组，校验名称还原成功。
- [x] 创建 `tests/p0/environment_group_management/test_06_edit_group_remark.py`。
- [x] P0-修改环境分组备注：进入环境分组页面，找到列表中第一个有编辑入口的分组。
- [x] P0-修改环境分组备注：记录该分组备注和名称下方的 ID。
- [x] P0-修改环境分组备注：点击该分组行的编辑按钮，在 `编辑环境分组` 弹窗中将备注修改为 `自动化-修改环境分组备注` 并点击确定。
- [x] P0-修改环境分组备注：根据记录的 ID 找到同一分组，校验备注修改成功。
- [x] P0-修改环境分组备注：再次编辑该分组，将备注还原为第一步记录的备注并点击确定。
- [x] P0-修改环境分组备注：根据记录的 ID 找到同一分组，校验备注还原成功。
- [x] 扩展 `locators/member_locators.yaml`，补充创建成员类型浮层、成员列表表头/单元格、行内删除、radio、switch、已选文本等成员管理统一元素。
- [x] 扩展 `pages/member_page.py`，封装创建外部成员、删除成员、读取成员列表字段、打开编辑弹窗补充校验等操作。
- [x] 创建 `tests/p0/member_management/test_01_create_external_member.py`。
- [x] P0-创建外部成员：进入成员列表页面。
- [x] P0-创建外部成员：点击 `创建成员`，在类型浮层中选择 `外部成员` 卡片的 `创建`。
- [x] P0-创建外部成员：在 `创建外部成员` 弹窗中填写名称 `自动化-创建外部成员`、成员分组 `运营组`、邮箱 `wklocxt1k@tempmail.cn`、环境分组 `未分组`、成员身份 `员工`。
- [x] P0-创建外部成员：检查 `到期停用` 开关，开启时关闭。
- [x] P0-创建外部成员：点击确定后在成员列表中校验成员名称、授权环境分组、成员身份、所属成员分组、成员状态。
- [x] P0-创建外部成员：打开编辑弹窗补充校验成员邮箱、成员分组和环境分组回显。
- [x] P0-创建外部成员：删除该成员并校验删除成功。
- [x] 创建 `tests/p0/member_management/test_02_edit_external_member_name.py`。
- [x] P0-编辑外部成员名称：进入成员列表页面。
- [x] P0-编辑外部成员名称：找到 `外部成员1` 并点击编辑。
- [x] P0-编辑外部成员名称：将成员名称修改为 `自动化-编辑外部成员名称` 并点击确定。
- [x] P0-编辑外部成员名称：在列表中校验名称修改成功。
- [x] P0-编辑外部成员名称：再次编辑该成员，将名称还原为 `外部成员1` 并点击确定。
- [x] P0-编辑外部成员名称：在列表中校验名称还原成功。
- [x] 创建 `tests/p0/member_management/test_03_create_internal_member.py`。
- [x] P0-创建内部成员：进入成员列表页面。
- [x] P0-创建内部成员：点击 `创建成员`，在类型浮层中选择 `内部成员` 卡片的 `创建`。
- [x] P0-创建内部成员：在 `创建内部成员` 弹窗中填写名称 `自动化-创建内部成员`、登录账号 `mcdl666`、登录密码 `M12345678`、成员分组 `运营组`、环境分组 `未分组`、成员身份 `员工`。
- [x] P0-创建内部成员：根据当前必填项选择上级经理 `外部成员1`，检查 `到期停用` 开关，开启时关闭。
- [x] P0-创建内部成员：点击确定后关闭 `创建成功` 凭证弹窗，在成员列表中校验成员名称、授权环境分组、成员身份、所属成员分组、成员状态。
- [x] P0-创建内部成员：打开编辑弹窗补充校验登录账号、成员分组和环境分组回显。
- [x] P0-创建内部成员：删除该成员并校验删除成功。
- [x] 创建 `tests/p0/member_management/test_04_edit_internal_member_name.py`。
- [x] P0-编辑内部成员名称：进入成员列表页面。
- [x] P0-编辑内部成员名称：找到 `内部成员003` 并点击编辑。
- [x] P0-编辑内部成员名称：将成员名称修改为 `自动化-编辑内部成员名称`，选择当前必填的上级经理 `外部成员1` 并点击确定。
- [x] P0-编辑内部成员名称：在列表中校验名称修改成功。
- [x] P0-编辑内部成员名称：再次编辑该成员，将名称还原为 `内部成员003` 并点击确定。
- [x] P0-编辑内部成员名称：在列表中校验名称还原成功。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表筛选区、成员分组筛选框、清空筛选按钮图标和筛选已选文本等统一元素。
- [x] 扩展 `pages/member_page.py`，封装成员分组筛选、清空筛选、读取当前列表“所属成员分组”列和等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_05_filter_member_group.py`。
- [x] P0-成员分组筛选：进入成员列表页面，先创建临时外部成员 `自动化-成员分组筛选` 到 `运营组`，保证 `运营组` 筛选结果非空。
- [x] P0-成员分组筛选：点击 `成员分组` 筛选框，选择 `运营组`，校验列表“所属成员分组”列均为 `运营组`。
- [x] P0-成员分组筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-成员分组筛选：再次点击 `成员分组` 筛选框，选择 `管理组`，校验列表“所属成员分组”列均为 `管理组`。
- [x] P0-成员分组筛选：再次点击 `清空筛选` 按钮，并删除临时 `运营组` 成员。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表筛选区输入框和搜索按钮图标统一元素。
- [x] 扩展 `pages/member_page.py`，封装成员名称/ID 筛选、搜索、读取当前列表成员名称和 ID、等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_06_filter_member_name.py`。
- [x] P0-成员名称筛选：进入成员列表页面，找到 `成员名称/ID` 筛选输入框，输入 `自动化成员`。
- [x] P0-成员名称筛选：点击输入框右侧第一个搜索按钮，校验列表中成员名称均包含 `自动化成员`。
- [x] P0-成员名称筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-成员名称筛选：再次输入 `1972494001272483841` 并点击搜索按钮，校验列表中成员 ID 均匹配该 ID。
- [x] P0-成员名称筛选：再次点击 `清空筛选` 按钮。
- [x] 通过临时脚本验证成员列表 `更多筛选` 抽屉结构，确认 `备注` 输入框和 `立即筛选` 按钮定位。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表更多筛选抽屉统一元素。
- [x] 扩展 `pages/member_page.py`，封装更多筛选入口、备注输入框、立即筛选按钮、读取当前列表备注列和等待备注筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_07_filter_member_remark.py`。
- [x] P0-成员备注筛选：进入成员列表页面，点击搜索按钮右侧 `更多筛选` 按钮。
- [x] P0-成员备注筛选：在展开抽屉中找到 `备注` 输入框，输入 `必要数据`。
- [x] P0-成员备注筛选：点击 `立即筛选`，校验列表中成员备注均包含 `必要数据`。
- [x] P0-成员备注筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] 扩展 `pages/member_page.py`，封装登录账号/邮箱更多筛选、读取当前列表成员名称、逐行打开编辑弹窗读取账号或邮箱并等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_08_filter_member_login_account_email.py`。
- [x] P0-登录账号、邮箱筛选：进入成员列表页面，点击搜索按钮右侧 `更多筛选` 按钮。
- [x] P0-登录账号、邮箱筛选：在展开抽屉中找到 `登录账号/邮箱` 输入框，输入 `mcdl003` 并点击 `立即筛选`。
- [x] P0-登录账号、邮箱筛选：筛选后逐行打开编辑弹窗读取 `登录账号`，校验包含 `mcdl003`。
- [x] P0-登录账号、邮箱筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-登录账号、邮箱筛选：再次通过 `登录账号/邮箱` 输入 `oytrhsjwe@tempmail.cn` 并点击 `立即筛选`。
- [x] P0-登录账号、邮箱筛选：筛选后逐行打开编辑弹窗读取 `成员邮箱`，校验包含 `oytrhsjwe@tempmail.cn`。
- [x] P0-登录账号、邮箱筛选：用例结束时清空筛选。
- [x] 修复 `MemberPage.open_list()`：进入成员列表前先按需展开 `团队管理`，避免当前停留在环境管理且团队菜单折叠时找不到 `成员列表`。
- [x] 加固 `MemberPage` 成员行定位：编辑、删除、存在判断、详情读取、授权环境分组读取和勾选操作改为解析成员名称/ID 后精确匹配，避免 `外部成员1` 误命中 `外部成员10` 这类同名前缀数据。
- [x] 加固 `MemberPage` 批量导出菜单：导出成员二级菜单改为等待 `导出成员`、`导出所选` 菜单项真实出现后再点击，替代固定等待。
- [x] 加固 `tests/p0/member_management/test_09_batch_edit_member_remark.py`：失败清理中按已记录成员 ID 兜底覆盖还原原备注，并清除勾选。
- [x] 创建 `tests/p0/member_management/test_09_batch_edit_member_remark.py`。
- [x] P0-批量编辑成员备注：进入成员列表，清空筛选。
- [x] P0-批量编辑成员备注：找到并记录备注为 `自动化数据专用【勿删！！！】` 的成员 ID。
- [x] P0-批量编辑成员备注：勾选记录的成员，通过批量工具栏 `更多操作 -> 编辑备注` 选择 `覆盖`，设置备注为 `自动化-批量设置成员备注` 并校验。
- [x] P0-批量编辑成员备注：再次勾选记录的成员，选择 `追加`，输入 `自动化-追加备注`，校验备注同时包含覆盖值和追加值。
- [x] P0-批量编辑成员备注：再次勾选记录的成员，选择 `覆盖` 还原为原备注并校验。
- [x] 创建 `tests/p0/member_management/test_10_export_member.py`。
- [x] P0-导出成员：进入成员列表，清空筛选。
- [x] P0-导出成员：逐个按成员名称精确筛选，读取 `自动化成员1` 和 `外部成员1` 的成员 ID，清空筛选后按 ID 勾选。
- [x] P0-导出成员：通过批量工具栏 `更多操作 -> 导出成员 -> 导出所选` 触发下载，优先捕获 Chromium 下载事件，兜底处理 Windows 保存弹窗。
- [x] P0-导出成员：捕获 Chromium 下载事件时校验导出文件名符合配置正则。
- [x] P0-导出成员：等待导出 xlsx 生成，校验文件存在且非空。
- [x] P0-导出成员：比较导出文件与预置正确文件，校验表头一致、导出范围仅包含所选成员、目标成员行存在且非动态字段内容一致。
- [x] P0-导出成员：清理已勾选成员和临时导出文件。
- [x] 创建 `tests/p0/member_management/test_11_no_edit_permission_member.py`。
- [x] P0-无编辑权限成员环境操作校验：退出当前自动化账号，使用 MCDL007/M12345678 登录。
- [x] P0-无编辑权限成员环境操作校验：校验登录成功，进入环境列表。
- [x] P0-无编辑权限成员环境操作校验：点击序号、Playwright 鼠标横扫整行触发 hover 态，校验快捷编辑入口（环境名称/备注/环境分组/IP代理/标签）均不存在。
- [x] P0-无编辑权限成员环境操作校验：点击更多操作，校验下拉菜单无"编辑"入口。
- [x] P0-无编辑权限成员环境操作校验：选中环境，校验批量工具栏无"编辑备注"。
- [x] P0-无编辑权限成员环境操作校验：hover 批量"更多操作"，校验无"设置环境分组"、"编辑标签"、"编辑环境"。
- [x] P0-无编辑权限成员环境操作校验：退出 MCDL007 并切回自动化账号，校验登录成功。
- [x] P0-无编辑权限成员环境操作校验：finally 兜底还原自动化账号并清理筛选和选中。
- [x] P0-无编辑权限成员环境操作校验：侦察 IP代理列编辑按钮（无 .edit-btn-on-hover class），改用 .icon-edit → button 父级追溯策略统一覆盖五列快捷编辑入口。
- [x] P0-无编辑权限成员环境操作校验：两次全量验证通过（54/54）。
- [x] 整理 P0-无编辑权限成员环境操作校验：将环境列表序号点击、整行 hover、快捷编辑入口扫描、行内下拉/批量工具栏可见性判断沉淀到 `EnvironmentPage`，用例文件只保留业务流程和断言。
- [x] 清理 IP代理列临时诊断脚本 `diagnose_ip2.py`，避免临时探测文件进入版本管理。
- [x] 新增 Streamlit UI 附加入口：`streamlit_runner.py`、`ui/app.py`、`ui/pages/01_执行用例.py`、`ui/pages/02_运行历史.py`、`UI使用文档.md`。
- [x] 整理 Streamlit UI：仪表盘用例数改为动态发现统计；执行用例页模块级选择改为“选中本模块/取消本模块”显式按钮，避免渲染时覆盖单条用例勾选状态；`streamlit` 纳入 `requirements.txt`。

## 十三、后续模块用例

- [x] 实现书签覆盖用例。
- [x] 实现书签追加用例。
- [x] 实现成员导出用例。
- [x] 实现环境管理导出环境用例。
- [x] 实现 APP 抓包进程拦截验证用例。
- [ ] 实现本地扩展包安装用例。
- [ ] 实现代理管理用例。
- [ ] 实现扩展管理用例。
- [x] 实现环境分组用例第一版：创建环境分组、包含环境的分组、授权成员的分组、环境分组名称筛选、修改环境分组名称、修改环境分组备注。
- [x] 实现成员管理用例第一版：创建外部成员。
- [x] 实现成员管理用例第二版：编辑外部成员名称。
- [x] 实现成员管理用例第三版：创建内部成员。
- [x] 实现成员管理用例第四版：编辑内部成员名称。
- [x] 实现成员管理用例第五版：成员分组筛选。
- [x] 实现成员管理用例第六版：成员名称筛选。
- [x] 实现成员管理用例第七版：成员备注筛选。
- [x] 实现成员管理用例第八版：登录账号、邮箱筛选。
- [x] 实现成员管理用例第九版：批量编辑成员备注。
- [x] 实现成员管理用例第十版：导出成员。
- [x] 实现成员管理用例第十一版：无编辑权限成员环境操作校验。
- [x] 实现全局设置-禁止查看网站密码用例第一版。
- [x] 实现全局设置-禁止打开浏览器开发者工具用例。
- [x] 实现全局设置-禁止管理/移除扩展用例。
- [x] 实现全局设置-禁止成员访问谷歌扩展商店和扩展设置页面用例。
- [x] 实现全局设置-禁止访问指定网址-快捷勾选谷歌应用商店、百度，并校验 b 站可正常访问用例。
- [x] 实现全局设置-允许访问指定网址-b 站用例。
- [x] 实现全局设置-禁用抓包软件用例。
- [x] 实现全局设置-书签设置覆盖用例。
- [x] 实现全局设置-书签设置追加用例。
- [x] 实现全局设置-环境字段显示限制用例。
- [x] 实现全局设置-环境列表分页设置用例。
- [x] 实现全局设置-环境列表排序设置用例。

## 十四、运行验证

- [x] 验证 `python run.py --config config/config.yaml --precheck`。
- [x] 验证 `python run.py --config config/config.yaml --level P0 --attach-existing-app`。
- [x] 验证按模块执行。
- [x] 验证 unittest 发现 `tests/p0` 当前 54 条 P0 用例：环境管理 25 条、全局设置 12 条、环境分组管理 6 条、成员管理 11 条。
- [x] 验证按业务模块分类发现用例，`环境管理` 当前匹配 25 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`全局设置` 当前匹配 12 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`环境分组管理` 当前匹配 6 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`成员管理` 当前匹配 11 条 P0 用例。
- [x] 修复 2026-05-15 全局设置模块回归异常：原运行 `total=12 passed=8 failed=0 errors=4 skipped=0 flaky=0`，异常集中在 `test_01_disable_view_password.py`、`test_02_disable_browser_devtools.py`、`test_03_disable_extension_management.py`、`test_04_disable_member_access_google_extension_pages.py`。
- [x] 定位 `test_01_disable_view_password.py` 异常：全局设置复选框点击脚本内缺少 `checkboxStateSelector` 和 `checkboxInputSelector` 变量定义，修复后单跑通过。
- [x] 定位 `test_02_disable_browser_devtools.py` 异常：同属全局设置复选框脚本异常，修复后单跑通过。
- [x] 定位 `test_03_disable_extension_management.py` 异常：复选框脚本修复后进入内核校验；Chrome Web Store 当前会先出现“切换到 Chrome 即可安装扩展程序和主题背景”的前置阻止提示，已兼容该阻止证据并单跑通过。
- [x] 定位 `test_04_disable_member_access_google_extension_pages.py` 异常：同属全局设置复选框脚本异常，修复后单跑通过。
- [x] 验证全局设置模块目录运行通过：`python run.py --config config/config.yaml --module global_settings --attach-existing-app`，结果 `total=12 passed=12 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全局设置模块第一条 P0 用例通过。
- [x] 验证全局设置模块第二条 P0 用例通过。
- [x] 验证全局设置模块第三条 P0 用例通过。
- [x] 验证全局设置模块第四条 P0 用例通过。
- [x] 验证全局设置模块第五条 P0 用例通过。
- [x] 验证全局设置模块第五条 P0 用例追加 b 站正常访问校验后通过：`python run.py --config config/config.yaml --module test_05_block_specific_websites_google_and_baidu.py --attach-existing-app`。
- [x] 验证全局设置模块第六条 P0 用例通过。
- [x] 验证全局设置模块第七条 P0 用例通过：`python run.py --config config/config.yaml --module test_07_disable_packet_capture_software.py --attach-existing-app`。
- [x] 验证全局设置模块第八条 P0 用例通过：`test_08_bookmark_setting_overwrite.py`。
- [x] 验证全局设置模块第九条 P0 用例通过：`test_09_bookmark_setting_append.py`。
- [x] 验证全局设置模块第十条 P0 用例通过：`test_10_environment_field_display_limit.py`。
- [x] 验证全局设置模块第十一条 P0 用例通过：`test_11_environment_list_pagination_setting.py`。
- [x] 验证全局设置模块第十二条 P0 用例通过：`test_12_environment_list_sort_limit.py`。
- [x] 验证环境分组管理模块第一条 P0 用例通过：`test_01_create_environment_group.py`。
- [x] 验证环境分组管理模块第二条 P0 用例通过：`python run.py --config config/config.yaml --module test_02_group_containing_environment.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第二条 P0 用例新增 `包含环境` 筛选校验后通过：`python run.py --config config/config.yaml --module test_02_group_containing_environment.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第三条 P0 用例通过：`python run.py --config config/config.yaml --module test_03_group_authorized_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第四条 P0 用例通过：`python run.py --config config/config.yaml --module test_04_filter_group_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第五条 P0 用例通过：`python run.py --config config/config.yaml --module test_05_edit_group_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第六条 P0 用例通过：`python run.py --config config/config.yaml --module test_06_edit_group_remark.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块目录运行通过：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组统一元素定位整理后模块目录运行通过：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第一条 P0 用例通过：`python run.py --config config/config.yaml --module test_01_create_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第二条 P0 用例通过：`python run.py --config config/config.yaml --module test_02_edit_external_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第三条 P0 用例通过：`python run.py --config config/config.yaml --module test_03_create_internal_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第四条 P0 用例通过：`python run.py --config config/config.yaml --module test_04_edit_internal_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第五条 P0 用例通过：`python run.py --config config/config.yaml --module test_05_filter_member_group.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第六条 P0 用例通过：`python run.py --config config/config.yaml --module test_06_filter_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第七条 P0 用例通过：`python run.py --config config/config.yaml --module test_07_filter_member_remark.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第八条 P0 用例通过：`python run.py --config config/config.yaml --module test_08_filter_member_login_account_email.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第九条 P0 用例通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，目录运行中 `test_09_batch_edit_member_remark.py` 通过。
- [x] 验证成员管理模块第十条 P0 用例通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，目录运行中 `test_10_export_member.py` 通过。
- [x] 验证修复成员列表入口后第一条成员管理用例单跑通过：`python run.py --config config/config.yaml --module test_01_create_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块目录运行通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，结果 `total=11 passed=11 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理第十一条 P0 用例通过：`python run.py --config config/config.yaml --module test_11_no_edit_permission_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全量 P0 运行通过（含第十一条）：`python run.py --config config/config.yaml --attach-existing-app`，结果 `total=54 passed=54 failed=0 errors=0 skipped=0 flaky=0`（两次）。
- [x] 验证 P0 用例文件移动到 `environment_management/` 后全量运行通过。
- [x] 验证按单条用例执行。
- [ ] 验证 APP 启动和关闭。
- [ ] 验证已有 APP 实例关闭后重启。
- [ ] 验证 APP 进程 30 秒检测。
- [x] 验证 CDP 连接。
- [x] 验证调试模式 `--attach-existing-app` 连接已打开 APP。
- [x] 验证统一超时时间配置后，P0 内核完整性用例仍可通过。
- [x] 验证 P0-批量创建 5 个环境用例通过。
- [x] 验证 P0-批量创建 134 内核环境用例通过。
- [x] 验证 P0-批量导入环境用例通过。
- [x] 验证 P0-编辑环境名称用例通过。
- [x] 验证 P0-编辑固定打开网址用例通过。
- [x] 验证 P0-环境列表筛选-自动化分组用例通过。
- [x] 验证 P0-环境列表备注筛选用例通过。
- [x] 验证 P0-置顶环境用例通过。
- [x] 验证 P0-列表快捷修改环境名称用例通过。
- [x] 验证 P0-环境序号升降序用例通过。
- [x] 验证 P0-字段排序-备注调整到首位用例通过。
- [x] 验证 P0-导出环境用例通过。
- [x] 验证 P0-创建多分组环境用例通过。
- [x] 验证 P0-批量创建多分组环境用例通过。
- [x] 验证 P0-单环境编辑多分组用例通过。
- [x] 验证 P0-批量编辑环境多分组用例通过。
- [x] 验证 P0-创建标签用例通过。
- [x] 验证 P0-创建带有标签的环境用例通过。
- [x] 验证 P0-筛选标签用例通过。
- [x] 验证用例前后恢复钩子不影响环境管理用例执行：`test_21_create_environment_with_tags.py` 通过。
- [ ] 验证 Playwright 连接失败时的兜底逻辑。
- [ ] 验证飞书测试通知。
- [ ] 验证执行总结通知。
- [ ] 验证失败告警通知。
- [ ] 验证模块级熔断通知。
- [x] 验证失败截图：通过临时失败用例确认截图生成到 `screenshots/`，失败摘要和日志均记录截图路径。
- [ ] 验证失败日志采集。
- [ ] 验证失败重试。
- [ ] 验证登录失败熔断。
- [ ] 验证 CDP 连接失败熔断。
- [x] 验证退出码 `0`。
- [ ] 验证退出码 `1`。
- [ ] 验证退出码 `2`。
- [ ] 验证退出码 `3`。

## 十五、第一版交付

- [x] 完成第一版代码。
- [x] 完成第一版配置模板。
- [x] 完成第一版 README 使用说明。
- [x] 完成第一版 P0 用例验证。
- [ ] 完成第一版交付检查。
