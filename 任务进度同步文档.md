# Dicloak 自动化框架开发进度同步

更新时间：2026-06-09

说明：

- `[x]` 表示已完成。
- `[ ]` 表示待完成。
- 本文档主要同步开发实施进度；涉及框架行为、验证结果和接手信息变化的文档同步也记录。

## 一、项目骨架

- [x] 创建项目目录结构。
- [x] 创建 `requirements.txt`。
- [x] 创建 `.gitignore`。
- [x] 创建 `README.md`。
- [x] 创建 `run.py` 命令行入口。
- [x] 创建 `launcher_ui.py` 启动界面预留入口。
- [x] 创建 `config/` 目录。
- [x] 创建 `core/` 目录。
- [x] 创建 `pages/` 目录。
- [x] 创建 `locators/` 目录。
- [x] 创建 `tests/` 目录。
- [x] 创建 `tests/p0/` 目录。
- [x] 创建 `tests/p0/environment_management/` 环境管理用例目录。
- [x] 创建 `tests/p0/proxy_management/` 代理管理用例目录。
- [x] 创建 `tests/p0/extension_management/` 扩展管理用例目录。
- [x] 创建 `tests/p0/environment_group_management/` 环境分组管理用例目录。
- [x] 创建 `tests/p0/member_management/` 成员管理用例目录。
- [x] 创建 `tests/p0/global_settings/` 全局设置用例目录。
- [x] 创建 `tests/p1/` 目录。
- [x] 创建 `test_data/` 目录。
- [x] 创建 `test_data/import/` 目录。
- [x] 创建 `test_data/export/` 目录。
- [x] 创建 `test_data/bookmarks/` 目录。
- [x] 创建 `test_data/members/` 目录。
- [x] 创建 `test_data/extensions/` 目录。
- [x] 创建 `logs/` 目录。
- [x] 创建 `reports/` 目录。
- [x] 创建 `screenshots/` 目录。

## 二、配置文件

- [x] 创建 `config/config.example.yaml`。
- [x] 创建本地 `config/config.yaml`。
- [x] 创建 `config/test_data.example.yaml`。
- [x] 创建本地 `config/test_data.yaml`。
- [x] 将测试数据配置从主配置拆分到独立测试数据文件。
- [x] 实现 `test_data_file` 配置项，支持主配置按路径加载测试数据文件。
- [x] 实现 APP 启动配置。
- [x] 实现 CDP 连接配置。
- [x] 实现账号配置。
- [x] 实现飞书配置。
- [x] 实现报告配置预留。
- [x] 实现运行参数配置。
- [x] 实现统一超时时间配置 `timeouts`，并填入当前默认等待值。
- [x] 实现日志配置。
- [x] 实现环境名称和内核环境配置。
- [x] 实现书签相关配置。
- [x] 实现成员导出相关配置。
- [x] 实现批量导入相关配置。
- [x] 实现批量导出相关配置。
- [x] 实现抓包工具相关配置。
- [x] 实现本地扩展包相关配置。
- [x] 实现 Windows 系统代理运行环境配置 `windows_system_proxy`，统一维护系统代理主机和端口，默认 `127.0.0.1:7897`，供代理管理等需要临时开启系统代理的用例复用。
- [x] 实现代理管理创建自定义代理用例数据配置 `test_data.proxy_custom`，模板保留创建代理主机/端口，账号/密码留空并支持本地配置或环境变量注入。
- [x] 配置真实 APP exe 路径。
- [x] 配置真实登录账号和密码。
- [x] 配置真实飞书 webhook。
- [x] 配置真实批量导入文件路径和文件名。
- [x] 配置真实批量导出文件路径和文件名。
- [x] 配置真实成员导出路径和文件名。
- [x] 配置真实成员导出预期文件完整路径。
- [x] 配置真实书签文件路径和文件名。
- [x] 配置真实抓包工具进程名和启动路径。
- [x] 配置真实本地扩展包名称和路径。
- [x] 将书签、批量导入、成员导出预期文件、本地扩展包、抓包工具启动文件、导出目录改为项目内 `test_data/` 相对路径。

## 三、Core 基础能力

- [x] 实现 `core/config.py` 配置读取。
- [x] 实现配置字段默认值处理。
- [x] 实现配置字段缺失校验。
- [x] 实现 `core/logger.py` 日志系统。
- [x] 实现日志文件按运行时间生成。
- [x] 实现日志保留天数清理。
- [x] 实现 `core/result.py` 执行结果统计。
- [x] 实现通过、失败、异常、跳过数量统计。
- [x] 实现通过率计算。
- [x] 实现失败用例摘要收集。
- [x] 实现失败截图统一采集：优先 CDP，失败后回退 mss 桌面截图，Windows 下再回退 UIAutomation 桌面截图。
- [x] 实现失败摘要和日志记录截图路径。
- [x] 实现 unittest result 层用例前后恢复钩子。
- [x] 实现全局 APP 稳定态恢复 `pages/app_page.py`，用于关闭阻塞弹窗、抽屉、下拉浮层并等待遮罩消失。
- [x] 实现模块级恢复分层设计，全局恢复不进入具体业务模块。
- [x] 实现环境管理模块 `recover_to_module_home()`，进入环境列表并清理环境管理模块筛选和选中状态。
- [x] 实现环境分组管理模块 `recover_to_module_home()`，进入环境分组列表并关闭阻塞浮层。
- [x] 实现 `core/assertions.py` 断言辅助。
- [x] 实现 `core/windows_proxy.py` Windows 系统代理工具，支持读取配置代理地址、启用系统代理、关闭系统代理、读取当前注册表代理设置和通知 WinINet 配置刷新。
- [x] 实现 `core/retry.py` 失败重试。
- [x] 实现重试日志记录。
- [x] 实现 flaky 标记。
- [x] 实现 `core/circuit_breaker.py` 模块级熔断。
- [x] 实现登录失败熔断。
- [x] 实现 APP 启动失败熔断。
- [x] 实现 CDP 连接失败熔断。
- [x] 实现环境管理模块熔断。
- [x] 实现内核模块熔断。
- [x] 实现批量导入模块熔断。
- [x] 实现 `core/runner.py` 用例运行编排。
- [x] 实现命令行参数解析。
- [x] 实现按级别运行用例。
- [x] 实现按模块运行用例。
- [x] 实现单模块文件或路径精确运行。
- [x] 实现按业务模块分类运行用例，支持环境管理、代理管理、扩展管理、环境分组管理、成员管理、全局设置。
- [x] 实现按单条用例运行。
- [x] 实现用例执行优先级排序：批量运行时 `test_01_kernel_integrity.py` 固定优先执行。
- [x] 实现退出码规则。

## 四、环境预检

- [x] 实现 `core/precheck.py`。
- [x] 检查 Python 版本。
- [x] 检查 APP `exe_path` 是否存在。
- [x] 检查 APP `work_dir` 是否存在。
- [x] 检查 APP 启动参数。
- [x] 检查批量导入文件完整路径是否存在。
- [x] 检查书签文件存放目录是否存在。
- [x] 检查成员导出预期正确文件是否存在。
- [x] 检查批量导出目录是否存在。
- [x] 检查抓包工具启动路径是否存在。
- [x] 检查本地扩展包完整路径是否存在。
- [x] 检查 CDP 端口占用情况。
- [x] 检查飞书 webhook 是否为空。
- [x] 实现预检结果日志输出。
- [x] 实现预检失败时取消自动化任务。
- [x] 实现预检失败退出码。

## 五、APP 生命周期

- [x] 实现 `core/app.py`。
- [x] 实现 Dicloak 进程检测。
- [x] 实现 Dicloak 进程检测的 `Get-Process` 兜底，避免 `tasklist` 被拒绝时误判。
- [x] 实现启动前检测已有 Dicloak APP。
- [x] 实现已有 Dicloak APP 强制结束，避免退出确认弹窗卡住流程。
- [x] 实现已有 APP 关闭超时处理。
- [x] 实现按配置启动 Dicloak APP。
- [x] 实现启动时附加 CDP 参数。
- [x] 实现调试模式 `--attach-existing-app`，支持只连接已手动启动的 APP。
- [x] 实现 APP 进程 30 秒检测。
- [x] 实现 APP 进程检测失败日志。
- [x] 实现 APP 进程检测失败飞书通知。
- [x] 实现进程检测失败后继续尝试 CDP 连接。
- [x] 实现 CDP 连接失败后取消自动化任务。
- [x] 实现测试结束后关闭 APP。

## 六、CDP 与页面控制

- [x] 实现 `core/cdp_driver.py`。
- [x] 实现通过 `http://127.0.0.1:9222/json` 获取页面列表。
- [x] 实现 Playwright `connect_over_cdp` 连接。
- [x] 实现运行器 CDP 启动检查后释放连接，避免和用例内 CDP 连接冲突。
- [x] 实现 WebSocket CDP 兜底连接。
- [x] 验证 CDP 目标页面识别方式。
- [x] 实现 CDP 默认页面选择跳过 DevTools，优先连接 Dicloak 主页面。
- [x] 实现按 URL 关键字选择页面。
- [x] 实现按页面标题选择页面。
- [x] 实现按页面关键元素选择页面。
- [x] 实现页面健康检查。
- [x] 实现元素查询。
- [x] 实现元素点击前等待元素可见且可用。
- [x] 实现按坐标点击元素。
- [x] 实现按脚本定位元素并由 Playwright 直接点击，避免表格按钮坐标点击偏移。
- [x] 实现脚本定位元素点击前等待元素出现、可见且可用。
- [x] 实现文本输入前等待元素可见且可用。
- [x] 实现文本读取前等待元素可见。
- [x] 实现元素、脚本元素、请求监听等待时间优先读取 `config.yaml`。
- [x] 实现等待元素出现。
- [x] 实现等待元素可点击。
- [x] 实现等待文本变化。
- [x] 实现页面截图。
- [x] 实现 CDP 连接失败错误输出。
- [x] 加固左侧业务菜单点击：环境管理入口改为点击菜单项容器，避免文字坐标贴近窗口顶部时点击不触发。

## 七、UIAutomation 能力

- [x] 实现 `core/ui_driver.py`。
- [x] 实现 APP 主窗口查找。
- [x] 实现原生控件查找。
- [x] 实现原生按钮点击。
- [x] 实现原生输入框输入。
- [x] 实现 Windows 文件选择器识别。
- [x] 实现 Windows 文件选择器输入文件路径。
- [x] 实现 Windows 文件选择器确认操作。
- [x] 实现原生弹窗识别。
- [x] 实现原生弹窗关闭或确认。
- [x] 实现桌面截图。
- [x] 实现控件查找失败日志。

## 八、飞书通知

- [x] 实现 `core/feishu.py`。
- [x] 实现 webhook 为空时跳过通知。
- [x] 实现文本消息 payload 组装。
- [x] 实现 @ 人内容组装。
- [x] 实现 Webhook 请求发送。
- [x] 实现 HTTP 状态码校验。
- [x] 实现飞书响应 `code == 0` 校验。
- [x] 实现请求异常处理。
- [x] 实现飞书测试通知。
- [x] 实现执行总结通知。
- [x] 实现失败告警通知。
- [x] 实现模块级熔断通知。

## 九、文件与测试数据处理

- [x] 实现导入文件完整路径拼接。
- [x] 实现批量导出文件完整路径拼接。
- [x] 实现成员导出文件完整路径拼接。
- [x] 实现完整路径存在性校验。
- [x] 实现导出文件生成等待。
- [x] 实现文件大小稳定性判断。
- [x] 实现 xlsx 文件读取。
- [x] 实现 xlsx 表头读取。
- [x] 实现 xlsx 行数据读取。
- [x] 实现书签覆盖数据生成。
- [x] 实现书签追加数据生成。
- [x] 实现用例创建数据记录。
- [x] 实现用例后置数据清理。
- [x] 实现清理失败日志记录。

## 十、页面对象

- [x] 创建 `pages/login_page.py`。
- [x] 实现登录。
- [x] 实现判断是否已登录。
- [x] 实现读取当前登录账号。
- [x] 实现账号不一致时退出重登。
- [x] 实现登录页可见输入框兜底登录。
- [x] 实现读取当前团队。
- [x] 实现自动化账号切换到目标团队。
- [x] 实现团队列表加载等待。
- [x] 实现目标团队切换后校验。
- [x] 实现登录失败识别。
- [x] 创建 `pages/environment_page.py`。
- [x] 实现进入环境列表。
- [x] 实现创建环境。
- [x] 实现查询环境。
- [x] 实现打开环境。
- [x] 实现关闭环境。
- [x] 实现删除环境。
- [x] 实现读取环境列表数据。
- [x] 创建 `pages/kernel_page.py`。
- [x] 实现进入内核管理。
- [x] 实现读取内核列表。
- [x] 实现删除指定内核。
- [x] 实现下载指定内核。
- [x] 实现读取环境内核版本号。
- [x] 创建 `pages/import_page.py`。
- [x] 实现进入批量导入。
- [x] 实现选择导入文件。
- [x] 实现提交导入。
- [x] 实现读取导入结果。
- [x] 实现识别导入失败环境。
- [x] 创建 `pages/proxy_page.py`。
- [x] 实现代理管理列表进入、创建弹窗打开、表单填写、弹窗检测结果读取、创建保存、按代理 ID 查找、行内检测、行内结果读取、删除确认和清理兜底。
- [x] 实现代理管理模块 `recover_to_module_home()`，进入代理列表并关闭阻塞浮层。

## 十一、元素定位

- [x] 创建 `locators/login_locators.yaml`。
- [x] 创建 `locators/environment_locators.yaml`。
- [x] 创建 `locators/kernel_locators.yaml`。
- [x] 创建 `locators/import_locators.yaml`。
- [x] 创建 `locators/proxy_locators.yaml`，维护代理管理菜单、创建弹窗、确认弹窗、表格行/单元格和通用按钮候选。
- [x] 验证登录页元素定位。
- [x] 验证环境列表页元素定位。
- [x] 验证环境创建元素定位。
- [x] 验证环境打开元素定位。
- [x] 验证环境关闭元素定位。
- [x] 验证环境删除元素定位。
- [x] 验证环境分组筛选下拉框元素定位。
- [x] 验证环境列表备注列读取和筛选结果定位。
- [x] 验证环境行内置顶和取消置顶菜单定位。
- [x] 验证环境名称列快捷编辑入口和弹窗定位。
- [x] 验证环境序号表头升序/降序排序按钮定位。
- [x] 验证列表字段设置入口和字段拖拽排序定位。
- [x] 验证内核管理元素定位。
- [x] 验证内核版本读取元素定位。
- [x] 验证批量导入入口元素定位。
- [x] 验证导入结果元素定位。
- [x] 验证文件上传可通过 DOM input 完成。
- [x] 实现 Windows 文件选择器兜底定位。

## 十二、P0 用例

- [x] 创建 `tests/p0/environment_management/test_01_kernel_integrity.py`。
- [x] P0-内核完整性校验：进入个人设置。
- [x] P0-内核完整性校验：进入基础设置。
- [x] P0-内核完整性校验：读取环境缓存目录，并增加页面加载等待与可见输入框兜底匹配。
- [x] P0-内核完整性校验：清空环境缓存目录下的 `browsers` 子目录。
- [x] P0-内核完整性校验：返回环境管理。
- [x] P0-内核完整性校验：读取首行环境名称并按需筛选 `142`。
- [x] P0-内核完整性校验：点击目标环境打开按钮。
- [x] P0-内核完整性校验：监听 `open_env` 请求并解析内核主进程 PID。
- [x] P0-内核完整性校验：通过内核主进程 PID 读取命令行请求参数并解析内核 CDP 调试端口。
- [x] P0-内核完整性校验：失败时尝试关闭已打开环境，避免遗留运行状态。
- [x] P0-内核完整性校验：等待指定内核 PID 启动。
- [x] P0-内核完整性校验：解析并校验指定内核 PID 的 CDP 端口。
- [x] P0-内核完整性校验：校验打开按钮变为关闭。
- [x] P0-内核完整性校验：优先通过内核 CDP 读取真实版本，兜底从命令行解析并校验 142 前缀。
- [x] P0-内核完整性校验：点击目标环境关闭按钮。
- [x] P0-内核完整性校验：等待指定内核 PID 关闭。
- [x] P0-内核完整性校验：校验关闭按钮变回打开。
- [x] P0-内核完整性校验：真实执行验证。
- [x] P0-内核完整性校验：刷新 APP 后校验 `browsers` 目录存在 142 版本内核文件夹。
- [x] P0-内核完整性校验：再次打开 142 环境，并通过内核 PID 校验启动路径位于 142 版本缓存目录。
- [x] P0-内核完整性校验：清理下载记录中除第一行外的内核记录，并校验删除结果。
- [x] P0-内核完整性校验：下载最新 134 内核，并校验 `browsers` 目录存在 134 版本内核文件夹。
- [x] P0-内核完整性校验：筛选并打开 134 内核环境，校验内核版本号。
- [x] P0-内核完整性校验：点击环境列表搜索按钮右侧第二个按钮清除筛选状态。
- [x] P0-内核完整性校验：补充步骤真实执行验证。
- [x] 实现环境列表用例结束时统一清除筛选状态，避免影响后续流程。
- [x] 创建 `tests/p0/environment_management/test_02_create_default_environment.py`。
- [x] P0-创建默认配置环境：点击创建环境并填写环境名称。
- [x] P0-创建默认配置环境：提交默认配置环境。
- [x] P0-创建默认配置环境：校验环境列表存在新建环境。
- [x] P0-创建默认配置环境：打开环境并校验按钮变为关闭。
- [x] P0-创建默认配置环境：关闭环境并校验按钮变回打开。
- [x] P0-创建默认配置环境：点击打开按钮右侧更多 icon。
- [x] P0-创建默认配置环境：通过更多菜单删除环境并确认二次弹窗。
- [x] P0-创建默认配置环境：校验环境已删除。
- [x] P0-创建默认配置环境：真实执行验证。
- [x] P0-创建默认配置环境：调整为开头搜索清理残留、删除后二次搜索确认、用例结束兜底清除筛选。
- [x] 创建 `tests/p0/environment_management/test_03_batch_create_environments.py`。
- [x] P0-批量创建 5 个环境：进入环境管理页。
- [x] P0-批量创建 5 个环境：点击批量创建并填写创建数量。
- [x] P0-批量创建 5 个环境：填写环境名称前缀并提交。
- [x] P0-批量创建 5 个环境：按前缀筛选并校验 5 个环境创建成功。
- [x] P0-批量创建 5 个环境：逐行点击最左侧选择框选中 5 个环境。
- [x] P0-批量创建 5 个环境：点击批量工具栏“打开环境”。
- [x] P0-批量创建 5 个环境：校验 5 个环境均打开成功并逐个校验按钮状态。
- [x] P0-批量创建 5 个环境：点击批量工具栏“关闭环境”并确认二次弹窗。
- [x] P0-批量创建 5 个环境：校验 5 个环境均关闭成功并逐个校验按钮状态。
- [x] P0-批量创建 5 个环境：通过顶部批量工具栏“更多操作”里的“删除环境”删除 5 个环境，并确认删除成功。
- [x] P0-批量创建 5 个环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_04_create_134_kernel_environment.py`。
- [x] P0-创建 134 内核环境：点击创建环境并填写环境名称。
- [x] P0-创建 134 内核环境：展开“指纹设置”。
- [x] P0-创建 134 内核环境：点击“更多指纹”。
- [x] P0-创建 134 内核环境：点击“浏览器内核”下拉框并选择 `ChromeBrowser 134`。
- [x] P0-创建 134 内核环境：提交创建并校验环境创建成功。
- [x] P0-创建 134 内核环境：打开环境并校验按钮变为关闭。
- [x] P0-创建 134 内核环境：通过内核 CDP 校验版本号为 134 前缀。
- [x] P0-创建 134 内核环境：关闭环境并校验按钮变回打开。
- [x] P0-创建 134 内核环境：删除环境并确认删除成功。
- [x] P0-创建 134 内核环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_05_batch_create_134_kernel_environments.py`。
- [x] P0-批量创建 134 内核环境：新增批量创建 134 内核环境配置，创建数量为 3。
- [x] P0-批量创建 134 内核环境：点击批量创建，填写环境名称前缀和创建数量。
- [x] P0-批量创建 134 内核环境：展开“指纹设置”，点击“更多指纹”，选择 `ChromeBrowser 134`。
- [x] P0-批量创建 134 内核环境：校验 3 个环境创建成功。
- [x] P0-批量创建 134 内核环境：批量打开环境并收集 3 个 `/open_env` 请求里的内核 PID。
- [x] P0-批量创建 134 内核环境：加固批量打开串行 `/open_env` 捕获，支持页面 `fetch`/`XMLHttpRequest` 调用点监听。
- [x] P0-批量创建 134 内核环境：逐个校验环境按钮状态变为关闭。
- [x] P0-批量创建 134 内核环境：逐个通过内核 PID 解析 CDP 端口并校验版本号为 134 前缀。
- [x] P0-批量创建 134 内核环境：批量关闭环境并逐个校验按钮状态变回打开。
- [x] P0-批量创建 134 内核环境：通过顶部批量工具栏“更多操作”里的“删除环境”删除 3 个环境，并确认删除成功。
- [x] P0-批量创建 134 内核环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_06_batch_import_environments.py`。
- [x] P0-批量导入环境：点击批量创建右侧下拉按钮并选择“批量导入”。
- [x] P0-批量导入环境：通过隐藏 `input[type=file]` 上传配置文件里的 xlsx。
- [x] P0-批量导入环境：提交导入并等待“导入结果”弹窗。
- [x] P0-批量导入环境：校验导入结果为 3 条成功、0 条失败。
- [x] P0-批量导入环境：关闭导入结果弹窗后回到环境管理。
- [x] P0-批量导入环境：在环境列表校验 xlsx 中 3 个环境创建成功。
- [x] P0-批量导入环境：删除导入产生的 3 个环境并确认删除成功。
- [x] P0-批量导入环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_07_edit_environment_name.py`。
- [x] P0-编辑环境名称：读取环境列表第一个环境的环境序号和原始名称。
- [x] P0-编辑环境名称：通过该环境行最右侧更多按钮进入“编辑”。
- [x] P0-编辑环境名称：修改名称为 `自动化-编辑环境名称` 并提交。
- [x] P0-编辑环境名称：通过环境序号精准校验名称修改成功。
- [x] P0-编辑环境名称：再次通过环境序号进入编辑并还原原始名称。
- [x] P0-编辑环境名称：通过环境序号精准校验名称还原成功。
- [x] P0-编辑环境名称：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_08_edit_fixed_open_url.py`。
- [x] P0-编辑固定打开网址：筛选预置环境 `自动化编辑打开网址`。
- [x] P0-编辑固定打开网址：进入编辑环境并在“高级设置 - 固定打开网页”写入 `https://bilibili.com`。
- [x] P0-编辑固定打开网址：打开环境并通过内核 CDP 校验 `bilibili.com` 标签页自动打开。
- [x] P0-编辑固定打开网址：通过内核 CDP 关闭 `bilibili.com` 标签页后关闭环境。
- [x] P0-编辑固定打开网址：再次编辑环境并清空“固定打开网页”。
- [x] P0-编辑固定打开网址：再次打开环境并校验不会自动打开 `bilibili.com` 标签页。
- [x] P0-编辑固定打开网址：关闭环境并清除环境列表筛选。
- [x] P0-编辑固定打开网址：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_09_filter_environment_group.py`。
- [x] P0-环境列表筛选-自动化分组：点击环境列表“环境分组”筛选框。
- [x] P0-环境列表筛选-自动化分组：在下拉选项中选择 `自动化分组`。
- [x] P0-环境列表筛选-自动化分组：点击搜索按钮刷新列表。
- [x] P0-环境列表筛选-自动化分组：校验当前列表每一行“环境分组”列均为 `自动化分组`。
- [x] P0-环境列表筛选-自动化分组：用例结束时清除筛选。
- [x] P0-环境列表筛选-自动化分组：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_10_filter_environment_remark.py`。
- [x] P0-环境列表备注筛选：在“序号/名称/备注”筛选框输入 `备注UI自动化`。
- [x] P0-环境列表备注筛选：点击搜索按钮刷新列表。
- [x] P0-环境列表备注筛选：校验当前列表每一行“备注”列均包含 `备注UI自动化`。
- [x] P0-环境列表备注筛选：用例结束时清除筛选。
- [x] P0-环境列表备注筛选：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_11_top_environment.py`。
- [x] P0-置顶环境：获取环境列表第三个环境的环境序号。
- [x] P0-置顶环境：点击该环境行内最右侧更多选项入口。
- [x] P0-置顶环境：在下拉菜单点击“置顶”。
- [x] P0-置顶环境：等待 `env/batch/top` 接口响应完成。
- [x] P0-置顶环境：校验环境列表第一个环境序号等于刚刚记录的环境序号。
- [x] P0-置顶环境：再次点击该环境行内更多选项入口。
- [x] P0-置顶环境：在下拉菜单点击“取消置顶”。
- [x] P0-置顶环境：等待 `env/batch/top` 接口响应完成。
- [x] P0-置顶环境：校验环境列表第一个环境序号不再等于刚刚记录的环境序号。
- [x] P0-置顶环境：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_12_quick_edit_environment_name.py`。
- [x] P0-列表快捷修改环境名称：获取第一行环境名称和环境序号。
- [x] P0-列表快捷修改环境名称：点击该环境名称。
- [x] P0-列表快捷修改环境名称：点击该环境名称右侧快捷编辑入口。
- [x] P0-列表快捷修改环境名称：在编辑弹窗内修改名称为 `自动化-列表快捷修改环境名称` 并点击确定。
- [x] P0-列表快捷修改环境名称：按环境序号校验名称修改成功。
- [x] P0-列表快捷修改环境名称：再次通过名称右侧快捷编辑入口还原原始名称。
- [x] P0-列表快捷修改环境名称：按环境序号校验名称还原成功。
- [x] P0-列表快捷修改环境名称：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_13_sort_environment_serial.py`。
- [x] P0-环境序号升降序：读取当前激活排序方向，并点击当前方向取消排序状态。
- [x] P0-环境序号升降序：点击“环境序号”升序按钮。
- [x] P0-环境序号升降序：校验当前列表环境序号符合升序规则。
- [x] P0-环境序号升降序：点击“环境序号”降序按钮。
- [x] P0-环境序号升降序：校验当前列表环境序号符合降序规则。
- [x] P0-环境序号升降序：用例结束后保持“环境序号”降序状态。
- [x] P0-环境序号升降序：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_14_move_remark_column.py`。
- [x] P0-字段排序-备注调整到首位：点击列表表头最右侧字段设置按钮。
- [x] P0-字段排序-备注调整到首位：在“列表字段设置”窗口中将“备注”拖动到首位。
- [x] P0-字段排序-备注调整到首位：点击确定并校验列表第一列变为“备注”。
- [x] P0-字段排序-备注调整到首位：再次打开字段设置窗口。
- [x] P0-字段排序-备注调整到首位：将“备注”拖动回“环境名称”字段下面。
- [x] P0-字段排序-备注调整到首位：点击确定并校验“备注”列恢复到“环境名称”后面。
- [x] P0-字段排序-备注调整到首位：真实执行验证。
- [x] 创建 `tests/p0/environment_management/test_15_export_environment.py`。
- [x] P0-导出环境：在“序号/名称/备注”筛选框输入 `导出环境` 并点击搜索按钮。
- [x] P0-导出环境：点击列表表头最左侧全选框，选中当前筛选结果。
- [x] P0-导出环境：通过顶部批量工具栏“更多操作”进入“导出环境 -> 导出所选”。
- [x] P0-导出环境：在“导出环境”弹窗点击确定。
- [x] P0-导出环境：优先捕获 Chromium 下载事件，兜底处理 Windows 原生保存弹窗，按配置路径保存导出文件。
- [x] P0-导出环境：等待导出文件生成并校验文件存在且非空。
- [x] P0-导出环境：筛选后收集当前列表环境序号、名称、备注、分组，并与导出的 xlsx 数据行进行一致性校验。
- [x] 创建 `tests/p0/environment_management/test_16_create_multi_group_environment.py`。
- [x] P0-创建多分组环境：点击创建环境并填写环境名称。
- [x] P0-创建多分组环境：读取创建抽屉里已选环境分组，保留并纳入预期校验。
- [x] P0-创建多分组环境：补选 `分组三` 和 `分组二`。
- [x] P0-创建多分组环境：提交创建并校验环境列表里的环境分组符合预期。
- [x] P0-创建多分组环境：删除环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_17_batch_create_multi_group_environments.py`。
- [x] P0-批量创建多分组环境：点击批量创建并填写创建数量 `3`。
- [x] P0-批量创建多分组环境：填写环境名称前缀 `自动化-批量创建多分组环境`。
- [x] P0-批量创建多分组环境：读取批量创建抽屉里已选环境分组，保留并纳入预期校验。
- [x] P0-批量创建多分组环境：补选 `分组三` 和 `分组二`。
- [x] P0-批量创建多分组环境：提交批量创建并校验 3 个环境创建成功。
- [x] P0-批量创建多分组环境：逐个点击环境分组列 `查看` 并校验真实气泡窗里的完整分组内容。
- [x] P0-批量创建多分组环境：删除 3 个环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_18_edit_single_environment_multi_group.py`。
- [x] P0-单环境编辑多分组：获取列表第一个环境的环境序号。
- [x] P0-单环境编辑多分组：通过环境序号点击更多操作并进入编辑。
- [x] P0-单环境编辑多分组：读取并保留编辑抽屉中已有环境分组。
- [x] P0-单环境编辑多分组：追加选择 `分组二`，点击确定并处理二次确认。
- [x] P0-单环境编辑多分组：按环境序号匹配列表行并校验分组修改成功。
- [x] P0-单环境编辑多分组：再次进入编辑并取消选择 `分组二`。
- [x] P0-单环境编辑多分组：按环境序号匹配列表行并校验分组还原成功。
- [x] 创建 `tests/p0/environment_management/test_19_batch_edit_environment_multi_group.py`。
- [x] P0-批量编辑环境多分组：获取列表前三个环境的环境序号和当前分组。
- [x] P0-批量编辑环境多分组：按环境序号依次勾选前三个环境。
- [x] P0-批量编辑环境多分组：通过顶部批量工具栏“更多操作”进入“设置环境分组”。
- [x] P0-批量编辑环境多分组：修改方式选择 `追加`，选择 `分组二` 并提交。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组追加成功。
- [x] P0-批量编辑环境多分组：再次勾选前三个环境，修改方式选择 `覆盖`。
- [x] P0-批量编辑环境多分组：选择 `分组三` 和 `分组二` 并提交。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组覆盖成功。
- [x] P0-批量编辑环境多分组：最后再次勾选前三个环境，通过 `覆盖` 选择 `未分组`。
- [x] P0-批量编辑环境多分组：按环境序号逐个校验分组重置为 `未分组`。
- [x] 创建 `tests/p0/environment_management/test_20_create_tag.py`。
- [x] P0-创建标签：点击清除筛选按钮右侧按钮，打开“标签管理”弹窗。
- [x] P0-创建标签：点击“创建标签”，填写标签名称 `自动化-创建标签` 并点击创建。
- [x] P0-创建标签：在标签列表中校验标签创建成功。
- [x] P0-创建标签：点击目标标签行“操作”列第二个按钮删除标签。
- [x] P0-创建标签：在标签列表中校验标签删除成功。
- [x] P0-创建标签：关闭“标签管理”弹窗。
- [x] 创建 `tests/p0/environment_management/test_21_create_environment_with_tags.py`。
- [x] P0-创建带有标签的环境：点击创建环境并填写环境名称 `自动化-创建带有标签的环境`。
- [x] P0-创建带有标签的环境：点击 `设置标签`。
- [x] P0-创建带有标签的环境：在 `请选择/输入创建标签` 输入框中输入并选择 `标签1`。
- [x] P0-创建带有标签的环境：修改输入框内容并选择 `标签2`。
- [x] P0-创建带有标签的环境：提交创建并在列表中校验环境存在。
- [x] P0-创建带有标签的环境：校验环境列表行中包含 `标签1` 和 `标签2`。
- [x] P0-创建带有标签的环境：删除环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_22_batch_create_environments_with_tags.py`。
- [x] P0-批量创建带有标签的环境：点击批量创建，创建数量输入 `3`，名称输入 `自动化-批量创建带有标签的环境`。
- [x] P0-批量创建带有标签的环境：点击 `设置标签`。
- [x] P0-批量创建带有标签的环境：在 `请选择/输入创建标签` 输入框中输入并选择 `标签1`。
- [x] P0-批量创建带有标签的环境：修改输入框内容并选择 `标签2`。
- [x] P0-批量创建带有标签的环境：提交创建并校验环境名称、数量和标签内容。
- [x] P0-批量创建带有标签的环境：删除 3 个环境并校验删除成功。
- [x] 创建 `tests/p0/environment_management/test_23_batch_edit_environment_tags.py`。
- [x] P0-批量编辑环境标签：记录当前列表前三个环境的环境序号和原有标签内容。
- [x] P0-批量编辑环境标签：选择前三个环境，点击 `更多操作` -> `编辑标签`。
- [x] P0-批量编辑环境标签：选择 `追加标签`，输入并选择 `标签7`，提交后校验标签追加成功且原有标签仍保留。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签`。
- [x] P0-批量编辑环境标签：选择 `覆盖标签`，输入并选择 `标签5`、`标签1`，提交后按环境序号校验标签已覆盖。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签` 并选择 `清空标签`。
- [x] P0-批量编辑环境标签：处理清空标签二次确认，并按环境序号校验标签已清空。
- [x] P0-批量编辑环境标签：再次选择前三个环境，进入 `编辑标签` 并选择 `覆盖标签`。
- [x] P0-批量编辑环境标签：输入并选择 `标签1`，提交后按环境序号校验标签均为 `标签1`。
- [x] 创建 `tests/p0/environment_management/test_24_edit_environment_tags.py`。
- [x] P0-编辑环境标签：记录列表第一个环境的环境序号和原始标签。
- [x] P0-编辑环境标签：进入编辑抽屉，设置标签为原始标签加 `标签9` 并提交。
- [x] P0-编辑环境标签：按环境序号校验标签编辑成功。
- [x] P0-编辑环境标签：再次进入编辑抽屉，按原始标签集合还原并提交。
- [x] P0-编辑环境标签：按环境序号校验标签还原成功。
- [x] 创建 `tests/p0/environment_management/test_25_filter_environment_tag.py`。
- [x] P0-筛选标签：点击环境列表搜索按钮右侧、清除筛选按钮左侧的更多筛选入口。
- [x] P0-筛选标签：在更多筛选抽屉的 `标签` 输入框中输入并选择 `标签2`。
- [x] P0-筛选标签：点击 `立即筛选`。
- [x] P0-筛选标签：按列表环境序号读取 `标签` 列，校验筛选结果均包含 `标签2`。
- [x] P0-筛选标签：点击清除筛选按钮恢复列表。
- [x] 创建 `pages/global_settings_page.py`。
- [x] 创建 `core/kernel_cdp.py`，用于通过内核 CDP 新建页面、访问内核地址并读取页面错误文本。
- [x] 创建 `tests/p0/global_settings/test_01_disable_view_password.py`。
- [x] P0-校验禁止查看网站密码：进入 `全局设置` 页面。
- [x] P0-校验禁止查看网站密码：定位 `禁止查看网站密码` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-校验禁止查看网站密码：进入 `环境管理`，在 `序号/名称/备注` 输入框搜索 `142`。
- [x] P0-校验禁止查看网站密码：打开筛选后列表第一个环境并解析内核 CDP 端口。
- [x] P0-校验禁止查看网站密码：在内核中访问 `chrome://password-manager/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-校验禁止查看网站密码：控制内核访问 `https://www.bilibili.com`。
- [x] P0-校验禁止查看网站密码：点击 B 站登录入口，打开登录弹窗。
- [x] P0-校验禁止查看网站密码：在登录弹窗密码输入框输入 `12345678`。
- [x] P0-校验禁止查看网站密码：点击密码框的小眼睛按钮。
- [x] P0-校验禁止查看网站密码：断言点击小眼睛后密码仍未以明文显示。
- [x] P0-校验禁止查看网站密码：关闭 B 站测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，并取消筛选。
- [x] 创建 `tests/p0/global_settings/test_02_disable_browser_devtools.py`。
- [x] P0-禁止打开浏览器开发者工具：进入 `全局设置` 页面。
- [x] P0-禁止打开浏览器开发者工具：定位 `禁止打开浏览器开发者工具界面` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止打开浏览器开发者工具：进入 `环境管理`，在 `序号/名称/备注` 输入框搜索 `142`。
- [x] P0-禁止打开浏览器开发者工具：打开筛选后列表第一个环境并解析内核 CDP 端口。
- [x] P0-禁止打开浏览器开发者工具：控制内核访问 `https://www.bilibili.com`。
- [x] P0-禁止打开浏览器开发者工具：尝试通过 `F12`、`Ctrl+Shift+I`、右键检查快捷路径打开开发者工具。
- [x] P0-禁止打开浏览器开发者工具：校验内核 CDP target 列表中没有打开 `devtools://` 或开发者工具 target。
- [x] P0-禁止打开浏览器开发者工具：关闭 B 站测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，并取消筛选。
- [x] P0-禁止管理/移除扩展：进入 `全局设置` 页面，定位 `禁止管理/移除扩展，以及从本地安装扩展至浏览器` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止管理/移除扩展：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止管理/移除扩展：控制内核访问 `chrome://extensions/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止管理/移除扩展：控制内核访问 Chrome Web Store 指定扩展页，点击 `添加至 Chrome` 并校验安装没有成功进入已安装状态。
- [x] P0-禁止管理/移除扩展：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置取消该选项并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_04_disable_member_access_google_extension_pages.py`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：进入 `全局设置` 页面，定位 `禁止成员访问谷歌扩展商店和扩展设置页面` 复选框，未选中时勾选并点击 `确定`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：控制内核访问 `chrome://extensions/`，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：紧接着访问 Chrome Web Store 指定扩展页，校验页面/导航证据包含 `ERR_BLOCKED_BY_CLIENT`。
- [x] P0-禁止成员访问谷歌扩展商店和扩展设置页面：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置取消该选项并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_05_block_specific_websites_google_and_baidu.py`。
- [x] P0-禁止访问指定网址：进入 `全局设置` 页面，打开 `访问网站限制` 功能开关。
- [x] P0-禁止访问指定网址：将 `限制方式` 设置为 `禁止访问指定网址`，并勾选快捷选择中的 `谷歌应用商店`。
- [x] P0-禁止访问指定网址：清空 `网址列表` 后输入 `https://baidu.com` 并点击 `确定` 保存。
- [x] P0-禁止访问指定网址：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-禁止访问指定网址：控制内核依次访问 `https://baidu.com`、`https://chromewebstore.google.com`、`https://bilibili.com`，校验百度和谷歌扩展商店包含 `ERR_BLOCKED_BY_CLIENT`，b 站正常加载。
- [x] P0-禁止访问指定网址：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置关闭 `访问网站限制` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_06_allow_specific_website_bilibili.py`。
- [x] P0-允许访问指定网址：进入 `全局设置` 页面，打开 `访问网站限制` 功能开关。
- [x] P0-允许访问指定网址：将 `限制方式` 设置为 `允许访问指定网址`，清空 `网址列表` 后输入 `https://bilibili.com` 并点击 `确定` 保存。
- [x] P0-允许访问指定网址：进入 `环境管理`，搜索 `142`，打开筛选后列表第二个环境并解析内核 CDP 端口。
- [x] P0-允许访问指定网址：控制内核依次访问 `https://bilibili.com`、`https://chromewebstore.google.com`、`https://baidu.com`，校验 b 站正常加载，谷歌扩展商店和百度均被 `ERR_BLOCKED_BY_CLIENT` 拦截。
- [x] P0-允许访问指定网址：关闭测试标签页，正常关闭环境，校验环境按钮恢复为 `打开`，清除筛选，回到全局设置关闭 `访问网站限制` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_07_disable_packet_capture_software.py`。
- [x] P0-禁用抓包软件：进入 `全局设置` 页面，打开 `禁用抓包软件` 功能开关。
- [x] P0-禁用抓包软件：读取 `test_data.packet_capture.process_name`，清空 `软件名称` 输入框并写入抓包软件进程名，点击 `确定` 保存。
- [x] P0-禁用抓包软件：读取 `test_data.packet_capture.startup_path` 启动抓包软件，并确认配置的抓包进程存在。
- [x] P0-禁用抓包软件：进入 `环境管理`，点击列表第一个环境的 `打开`，校验出现 `禁止打开环境` 弹窗，并校验内核进程未启动、环境按钮仍为 `打开`。
- [x] P0-禁用抓包软件：关闭 `禁止打开环境` 弹窗，关闭抓包软件后再次打开同一环境，校验不再弹窗、内核进程存在、环境按钮变为 `关闭`。
- [x] P0-禁用抓包软件：关闭环境并校验按钮恢复为 `打开`，回到全局设置关闭 `禁用抓包软件` 并返回环境管理。
- [x] 创建 `tests/p0/global_settings/test_08_bookmark_setting_overwrite.py`。
- [x] P0-书签设置-覆盖：打开 142 环境并在内核中创建临时书签，确认原始书签存在。
- [x] P0-书签设置-覆盖：进入 `全局设置`，配置书签设置为覆盖模式并上传覆盖书签文件。
- [x] P0-书签设置-覆盖：重新打开环境，通过内核 CDP 校验上传书签存在，临时书签已被覆盖删除。
- [x] P0-书签设置-覆盖：关闭环境、清理筛选并关闭书签设置开关。
- [x] 创建 `tests/p0/global_settings/test_09_bookmark_setting_append.py`。
- [x] P0-书签设置-追加：打开 142 环境，读取或创建原始书签作为追加前基线。
- [x] P0-书签设置-追加：进入 `全局设置`，配置书签设置为追加模式并上传追加书签文件。
- [x] P0-书签设置-追加：重新打开环境，通过内核 CDP 校验原始书签和追加书签同时存在。
- [x] P0-书签设置-追加：配置覆盖清空规则，重新打开环境并校验书签已清空。
- [x] P0-书签设置-追加：关闭环境、清理筛选并关闭书签设置开关。
- [x] 创建 `tests/p0/global_settings/test_10_environment_field_display_limit.py`。
- [x] P0-环境字段显示限制：记录环境列表原始表头并确认字段设置入口可见。
- [x] P0-环境字段显示限制：进入 `全局设置`，配置环境字段显示限制为 `环境序号`、`环境名称`。
- [x] P0-环境字段显示限制：返回环境列表，校验字段设置入口隐藏且列表表头只保留指定字段。
- [x] P0-环境字段显示限制：关闭限制后校验字段设置入口恢复可用，表头恢复到原始状态。
- [x] 创建 `tests/p0/global_settings/test_11_environment_list_pagination_setting.py`。
- [x] P0-环境列表分页设置：进入 `全局设置`，配置环境列表分页固定为 `20 条/页`。
- [x] P0-环境列表分页设置：刷新并进入环境列表，校验分页选择器隐藏且当前页行数符合固定分页。
- [x] P0-环境列表分页设置：关闭分页限制后校验分页选择器恢复可见，并恢复为 `10条/页`。
- [x] 创建 `tests/p0/global_settings/test_12_environment_list_sort_limit.py`。
- [x] P0-环境列表排序设置：进入 `全局设置`，配置环境列表按 `环境序号` 升序。
- [x] P0-环境列表排序设置：刷新并进入环境列表，校验表头排序按钮隐藏且环境序号升序。
- [x] P0-环境列表排序设置：切换为 `环境序号` 降序并校验列表降序。
- [x] P0-环境列表排序设置：关闭排序限制后校验排序按钮恢复可见，并恢复环境序号降序。
- [x] 创建 `locators/environment_group_locators.yaml`，集中维护环境分组模块的菜单、按钮、弹窗、表格行和输入框定位。
- [x] 整理 `locators/environment_group_locators.yaml`，补充筛选模式切换、搜索/清除、下拉菜单、表格单元格、行内编辑、行内操作、授权成员悬浮窗等统一元素，并补齐每个元素的人工查看注释。
- [x] 整理 `pages/environment_group_page.py`，将环境分组通用 CSS 选择器改为读取 locator 文件，页面对象保留按业务文本、分组 ID、列内容判断的动态逻辑。
- [x] 创建 `pages/environment_group_page.py`，封装进入环境分组、创建分组、删除分组、二次确认、校验存在/不存在等操作。
- [x] 接入环境分组模块级恢复，失败恢复时进入环境分组列表并关闭阻塞浮层。
- [x] 创建 `tests/p0/environment_group_management/test_01_create_environment_group.py`。
- [x] P0-创建环境分组：进入环境分组页面。
- [x] P0-创建环境分组：如果存在历史分组 `自动化-创建环境分组`，先删除并处理二次确认。
- [x] P0-创建环境分组：点击 `创建环境分组`，填写分组名称并提交。
- [x] P0-创建环境分组：在列表中校验分组创建成功。
- [x] P0-创建环境分组：删除该分组并校验删除成功。
- [x] 创建 `tests/p0/environment_group_management/test_02_group_containing_environment.py`。
- [x] P0-包含环境的分组：进入环境分组页面，按需清理历史分组 `自动化-包含环境的分组`。
- [x] P0-包含环境的分组：创建分组并校验创建成功。
- [x] P0-包含环境的分组：前往环境列表，点击创建环境，记录默认环境名称。
- [x] P0-包含环境的分组：清空创建环境抽屉中默认环境分组，只选择 `自动化-包含环境的分组` 后提交。
- [x] P0-包含环境的分组：校验环境创建成功，并校验该环境的环境分组正确。
- [x] P0-包含环境的分组：回到环境分组页面，在 `包含环境` 筛选框输入记录的环境名称，并从下拉框中选择该环境。
- [x] P0-包含环境的分组：校验环境分组列表筛选结果均包含目标环境，且返回目标分组。
- [x] P0-包含环境的分组：点击筛选行最右侧的 `清除筛选` 按钮，校验筛选状态清除后目标分组仍可见。
- [x] P0-包含环境的分组：回到环境分组页面，删除该分组时勾选 `删除该分组下的环境`。
- [x] P0-包含环境的分组：校验环境分组删除成功。
- [x] P0-包含环境的分组：前往环境列表校验刚创建的环境已被删除，随后立即清除筛选并回到环境分组页面。
- [x] 创建 `locators/member_locators.yaml`，集中维护成员列表、编辑弹窗、环境分组多选和行内编辑按钮定位。
- [x] 创建 `pages/member_page.py`，封装进入成员列表、打开编辑内部成员弹窗、读取/追加成员授权环境分组、校验成员列表授权分组回显等操作。
- [x] 创建 `tests/p0/environment_group_management/test_03_group_authorized_member.py`。
- [x] P0-授权成员的分组：进入环境分组页面，按需清理历史分组 `自动化-授权成员的分组`。
- [x] P0-授权成员的分组：创建环境分组并在列表中校验创建成功。
- [x] P0-授权成员的分组：前往成员列表，打开 `自动化成员1` 的编辑内部成员弹窗。
- [x] P0-授权成员的分组：记录成员原本勾选的环境分组，并追加勾选本次创建的环境分组后提交。
- [x] P0-授权成员的分组：回到环境分组页面，点击新建分组授权成员列的 `查看`，校验悬浮窗包含 `自动化成员1`。
- [x] P0-授权成员的分组：通过 `授权成员` 筛选框选择 `自动化成员1`，校验筛选结果包含原始分组和新建分组。
- [x] P0-授权成员的分组：清除筛选内容，删除新建环境分组并确认二次弹窗。
- [x] P0-授权成员的分组：校验分组删除成功后，前往成员列表确认 `自动化成员1` 的授权环境分组恢复为原本分组，再回到环境分组页面。
- [x] 创建 `tests/p0/environment_group_management/test_04_filter_group_name.py`。
- [x] P0-环境分组名称筛选：进入环境分组页面。
- [x] P0-环境分组名称筛选：找到 `分组名称` 筛选框右侧切换模式按钮，切换到 `备注`。
- [x] P0-环境分组名称筛选：输入 `勿动！！！` 并点击搜索按钮。
- [x] P0-环境分组名称筛选：校验筛选后列表存在数据，且列表中所有备注都匹配 `勿动！！！`。
- [x] P0-环境分组名称筛选：再次切换回 `分组名称`，点击 `清除筛选` 按钮，校验筛选内容清除。
- [x] 创建 `tests/p0/environment_group_management/test_05_edit_group_name.py`。
- [x] P0-修改环境分组名称：进入环境分组页面，找到列表中第一个有编辑入口的分组。
- [x] P0-修改环境分组名称：记录该分组名称和名称下方的 ID。
- [x] P0-修改环境分组名称：点击该分组行的编辑按钮，在 `编辑环境分组` 弹窗中将名称修改为 `自动化-修改环境分组名称` 并点击确定。
- [x] P0-修改环境分组名称：根据记录的 ID 找到同一分组，校验名称修改成功。
- [x] P0-修改环境分组名称：再次编辑该分组，将名称还原为第一步记录的名称并点击确定。
- [x] P0-修改环境分组名称：根据记录的 ID 找到同一分组，校验名称还原成功。
- [x] 创建 `tests/p0/environment_group_management/test_06_edit_group_remark.py`。
- [x] P0-修改环境分组备注：进入环境分组页面，找到列表中第一个有编辑入口的分组。
- [x] P0-修改环境分组备注：记录该分组备注和名称下方的 ID。
- [x] P0-修改环境分组备注：点击该分组行的编辑按钮，在 `编辑环境分组` 弹窗中将备注修改为 `自动化-修改环境分组备注` 并点击确定。
- [x] P0-修改环境分组备注：根据记录的 ID 找到同一分组，校验备注修改成功。
- [x] P0-修改环境分组备注：再次编辑该分组，将备注还原为第一步记录的备注并点击确定。
- [x] P0-修改环境分组备注：根据记录的 ID 找到同一分组，校验备注还原成功。
- [x] 扩展 `locators/member_locators.yaml`，补充创建成员类型浮层、成员列表表头/单元格、行内删除、radio、switch、已选文本等成员管理统一元素。
- [x] 扩展 `pages/member_page.py`，封装创建外部成员、删除成员、读取成员列表字段、打开编辑弹窗补充校验等操作。
- [x] 创建 `tests/p0/member_management/test_01_create_external_member.py`。
- [x] P0-创建外部成员：进入成员列表页面。
- [x] P0-创建外部成员：点击 `创建成员`，在类型浮层中选择 `外部成员` 卡片的 `创建`。
- [x] P0-创建外部成员：在 `创建外部成员` 弹窗中填写名称 `自动化-创建外部成员`、成员分组 `运营组`、邮箱 `wklocxt1k@tempmail.cn`、环境分组 `未分组`、成员身份 `员工`。
- [x] P0-创建外部成员：检查 `到期停用` 开关，开启时关闭。
- [x] P0-创建外部成员：点击确定后在成员列表中校验成员名称、授权环境分组、成员身份、所属成员分组、成员状态。
- [x] P0-创建外部成员：打开编辑弹窗补充校验成员邮箱、成员分组和环境分组回显。
- [x] P0-创建外部成员：删除该成员并校验删除成功。
- [x] 创建 `tests/p0/member_management/test_02_edit_external_member_name.py`。
- [x] P0-编辑外部成员名称：进入成员列表页面。
- [x] P0-编辑外部成员名称：找到 `外部成员1` 并点击编辑。
- [x] P0-编辑外部成员名称：将成员名称修改为 `自动化-编辑外部成员名称` 并点击确定。
- [x] P0-编辑外部成员名称：在列表中校验名称修改成功。
- [x] P0-编辑外部成员名称：再次编辑该成员，将名称还原为 `外部成员1` 并点击确定。
- [x] P0-编辑外部成员名称：在列表中校验名称还原成功。
- [x] 创建 `tests/p0/member_management/test_03_create_internal_member.py`。
- [x] P0-创建内部成员：进入成员列表页面。
- [x] P0-创建内部成员：点击 `创建成员`，在类型浮层中选择 `内部成员` 卡片的 `创建`。
- [x] P0-创建内部成员：在 `创建内部成员` 弹窗中填写名称 `自动化-创建内部成员`、登录账号 `mcdl666`、登录密码 `M12345678`、成员分组 `运营组`、环境分组 `未分组`、成员身份 `员工`。
- [x] P0-创建内部成员：根据当前必填项选择上级经理 `外部成员1`，检查 `到期停用` 开关，开启时关闭。
- [x] P0-创建内部成员：点击确定后关闭 `创建成功` 凭证弹窗，在成员列表中校验成员名称、授权环境分组、成员身份、所属成员分组、成员状态。
- [x] P0-创建内部成员：打开编辑弹窗补充校验登录账号、成员分组和环境分组回显。
- [x] P0-创建内部成员：删除该成员并校验删除成功。
- [x] 创建 `tests/p0/member_management/test_04_edit_internal_member_name.py`。
- [x] P0-编辑内部成员名称：进入成员列表页面。
- [x] P0-编辑内部成员名称：找到 `内部成员003` 并点击编辑。
- [x] P0-编辑内部成员名称：将成员名称修改为 `自动化-编辑内部成员名称`，选择当前必填的上级经理 `外部成员1` 并点击确定。
- [x] P0-编辑内部成员名称：在列表中校验名称修改成功。
- [x] P0-编辑内部成员名称：再次编辑该成员，将名称还原为 `内部成员003` 并点击确定。
- [x] P0-编辑内部成员名称：在列表中校验名称还原成功。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表筛选区、成员分组筛选框、清空筛选按钮图标和筛选已选文本等统一元素。
- [x] 扩展 `pages/member_page.py`，封装成员分组筛选、清空筛选、读取当前列表“所属成员分组”列和等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_05_filter_member_group.py`。
- [x] P0-成员分组筛选：进入成员列表页面，先创建临时外部成员 `自动化-成员分组筛选` 到 `运营组`，保证 `运营组` 筛选结果非空。
- [x] P0-成员分组筛选：点击 `成员分组` 筛选框，选择 `运营组`，校验列表“所属成员分组”列均为 `运营组`。
- [x] P0-成员分组筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-成员分组筛选：再次点击 `成员分组` 筛选框，选择 `管理组`，校验列表“所属成员分组”列均为 `管理组`。
- [x] P0-成员分组筛选：再次点击 `清空筛选` 按钮，并删除临时 `运营组` 成员。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表筛选区输入框和搜索按钮图标统一元素。
- [x] 扩展 `pages/member_page.py`，封装成员名称/ID 筛选、搜索、读取当前列表成员名称和 ID、等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_06_filter_member_name.py`。
- [x] P0-成员名称筛选：进入成员列表页面，找到 `成员名称/ID` 筛选输入框，输入 `自动化成员`。
- [x] P0-成员名称筛选：点击输入框右侧第一个搜索按钮，校验列表中成员名称均包含 `自动化成员`。
- [x] P0-成员名称筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-成员名称筛选：再次输入 `1972494001272483841` 并点击搜索按钮，校验列表中成员 ID 均匹配该 ID。
- [x] P0-成员名称筛选：再次点击 `清空筛选` 按钮。
- [x] 通过临时脚本验证成员列表 `更多筛选` 抽屉结构，确认 `备注` 输入框和 `立即筛选` 按钮定位。
- [x] 扩展 `locators/member_locators.yaml`，补充成员列表更多筛选抽屉统一元素。
- [x] 扩展 `pages/member_page.py`，封装更多筛选入口、备注输入框、立即筛选按钮、读取当前列表备注列和等待备注筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_07_filter_member_remark.py`。
- [x] P0-成员备注筛选：进入成员列表页面，点击搜索按钮右侧 `更多筛选` 按钮。
- [x] P0-成员备注筛选：在展开抽屉中找到 `备注` 输入框，输入 `必要数据`。
- [x] P0-成员备注筛选：点击 `立即筛选`，校验列表中成员备注均包含 `必要数据`。
- [x] P0-成员备注筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] 扩展 `pages/member_page.py`，封装登录账号/邮箱更多筛选、读取当前列表成员名称、逐行打开编辑弹窗读取账号或邮箱并等待筛选结果等操作。
- [x] 创建 `tests/p0/member_management/test_08_filter_member_login_account_email.py`。
- [x] P0-登录账号、邮箱筛选：进入成员列表页面，点击搜索按钮右侧 `更多筛选` 按钮。
- [x] P0-登录账号、邮箱筛选：在展开抽屉中找到 `登录账号/邮箱` 输入框，输入 `mcdl003` 并点击 `立即筛选`。
- [x] P0-登录账号、邮箱筛选：筛选后逐行打开编辑弹窗读取 `登录账号`，校验包含 `mcdl003`。
- [x] P0-登录账号、邮箱筛选：点击筛选栏最右侧 `清空筛选` 按钮。
- [x] P0-登录账号、邮箱筛选：再次通过 `登录账号/邮箱` 输入 `oytrhsjwe@tempmail.cn` 并点击 `立即筛选`。
- [x] P0-登录账号、邮箱筛选：筛选后逐行打开编辑弹窗读取 `成员邮箱`，校验包含 `oytrhsjwe@tempmail.cn`。
- [x] P0-登录账号、邮箱筛选：用例结束时清空筛选。
- [x] 修复 `MemberPage.open_list()`：进入成员列表前先按需展开 `团队管理`，避免当前停留在环境管理且团队菜单折叠时找不到 `成员列表`。
- [x] 加固 `MemberPage` 成员行定位：编辑、删除、存在判断、详情读取、授权环境分组读取和勾选操作改为解析成员名称/ID 后精确匹配，避免 `外部成员1` 误命中 `外部成员10` 这类同名前缀数据。
- [x] 加固 `MemberPage` 批量导出菜单：导出成员二级菜单改为等待 `导出成员`、`导出所选` 菜单项真实出现后再点击，替代固定等待。
- [x] 加固 `tests/p0/member_management/test_09_batch_edit_member_remark.py`：失败清理中按已记录成员 ID 兜底覆盖还原原备注，并清除勾选。
- [x] 创建 `tests/p0/member_management/test_09_batch_edit_member_remark.py`。
- [x] P0-批量编辑成员备注：进入成员列表，清空筛选。
- [x] P0-批量编辑成员备注：找到并记录备注为 `自动化数据专用【勿删！！！】` 的成员 ID。
- [x] P0-批量编辑成员备注：勾选记录的成员，通过批量工具栏 `更多操作 -> 编辑备注` 选择 `覆盖`，设置备注为 `自动化-批量设置成员备注` 并校验。
- [x] P0-批量编辑成员备注：再次勾选记录的成员，选择 `追加`，输入 `自动化-追加备注`，校验备注同时包含覆盖值和追加值。
- [x] P0-批量编辑成员备注：再次勾选记录的成员，选择 `覆盖` 还原为原备注并校验。
- [x] 创建 `tests/p0/member_management/test_10_export_member.py`。
- [x] P0-导出成员：进入成员列表，清空筛选。
- [x] P0-导出成员：逐个按成员名称精确筛选，读取 `自动化成员1` 和 `外部成员1` 的成员 ID，清空筛选后按 ID 勾选。
- [x] P0-导出成员：通过批量工具栏 `更多操作 -> 导出成员 -> 导出所选` 触发下载，优先捕获 Chromium 下载事件，兜底处理 Windows 保存弹窗。
- [x] P0-导出成员：捕获 Chromium 下载事件时校验导出文件名符合配置正则。
- [x] P0-导出成员：等待导出 xlsx 生成，校验文件存在且非空。
- [x] P0-导出成员：比较导出文件与预置正确文件，校验表头一致、导出范围仅包含所选成员、目标成员行存在且非动态字段内容一致。
- [x] P0-导出成员：清理已勾选成员和临时导出文件。
- [x] 创建 `tests/p0/member_management/test_11_no_edit_permission_member.py`。
- [x] P0-无编辑权限成员环境操作校验：退出当前自动化账号，使用 MCDL007/M12345678 登录。
- [x] P0-无编辑权限成员环境操作校验：校验登录成功，进入环境列表。
- [x] P0-无编辑权限成员环境操作校验：点击序号、Playwright 鼠标横扫整行触发 hover 态，校验快捷编辑入口（环境名称/备注/环境分组/IP代理/标签）均不存在。
- [x] P0-无编辑权限成员环境操作校验：点击更多操作，校验下拉菜单无"编辑"入口。
- [x] P0-无编辑权限成员环境操作校验：选中环境，校验批量工具栏无"编辑备注"。
- [x] P0-无编辑权限成员环境操作校验：hover 批量"更多操作"，校验无"设置环境分组"、"编辑标签"、"编辑环境"。
- [x] P0-无编辑权限成员环境操作校验：退出 MCDL007 并切回自动化账号，校验登录成功。
- [x] P0-无编辑权限成员环境操作校验：finally 兜底还原自动化账号并清理筛选和选中。
- [x] P0-无编辑权限成员环境操作校验：侦察 IP代理列编辑按钮（无 .edit-btn-on-hover class），改用 .icon-edit → button 父级追溯策略统一覆盖五列快捷编辑入口。
- [x] P0-无编辑权限成员环境操作校验：两次全量验证通过（54/54）。
- [x] 创建 `tests/p0/member_management/test_12_api_disable_external_member.py`。
- [x] P0-API编辑外部成员-停用成员：通过 GET 请求调用 `member/open/edit` 接口，将目标外部成员状态改为 `DISABLED`，校验 HTTP 状态码为 200 且 JSON `msg=success`。
- [x] P0-API编辑外部成员-停用成员：临时探测脚本确认停用后 APP 内切换环境分组/环境管理会弹出 Element Plus message box，弹窗包含“退出登录”和“切换团队”按钮。
- [x] P0-API编辑外部成员-停用成员：点击“退出登录”后校验回到登录页，再调用接口将成员状态恢复为 `ENABLED`，重新登录自动化账号、确认自动化团队并回到成员列表。
- [x] P0-API编辑外部成员-停用成员：优化弹窗触发等待，环境分组切换后若已出现强制退出弹窗则立即点击“退出登录”，避免继续尝试切环境管理时被弹窗遮挡并等待超时。
- [x] 创建 `tests/p0/member_management/test_13_api_disuse_external_member.py`。
- [x] P0-API编辑外部成员-到期停用成员：通过 GET 请求调用 `member/open/edit` 接口，传入 `disuse_enable=true`、`disuse_time=2026-1-31 12:00:00`、`time_zone=Etc/GMT-8`，校验 HTTP 状态码为 200 且 JSON `msg=success`。
- [x] P0-API编辑外部成员-到期停用成员：临时探测确认页面内刷新图标会刷新列表数据，但不稳定触发到期停用后的强制退出弹窗；页面级刷新可触发包含“退出登录”的强制退出弹窗，因此用例先点击 APP 刷新按钮，再以页面级刷新兜底触发会话检查。
- [x] P0-API编辑外部成员-到期停用成员：点击“退出登录”后校验回到登录页；按步骤调用 `status=ENABLED` 启用成员，再额外调用 `disuse_enable=false` 清理共享成员的到期停用开关，重新登录自动化账号、确认自动化团队并回到成员列表。
- [x] 优化 `LoginPage.switch_team()`：切换团队改为 hover 打开 `.org-change-popover` 后按目标团队行点击，避免全局文本点击命中错误节点；验证从 `【内部】测试` 可恢复切回自动化团队。
- [x] 创建 `tests/p0/member_management/test_14_api_disable_internal_member.py`。
- [x] P0-API编辑内部成员-停用成员：退出自动化账号后登录内部成员 `MCDL007`，通过 GET 请求调用 `member/open/edit` 接口，将目标内部成员 ID `1947196562844110850` 状态改为 `DISABLED`，校验 HTTP 状态码为 200 且 JSON `msg=success`。
- [x] P0-API编辑内部成员-停用成员：在 APP 内依次尝试切换环境分组和环境管理，校验停用后的内部成员自动退登并回到登录页；直接点击“立即登录”后校验提示“该账号已被停用。您可以联系原团队的管理员处理，或自行注册新账号”，并校验没有成功登录。
- [x] P0-API编辑内部成员-停用成员：再次调用接口将 `status=ENABLED`，校验接口成功；重新登录内部成员账号确认可登录，再退出并登录自动化账号、确认自动化团队后回到成员列表。
- [x] 优化 `LoginPage.logout_to_login_page()`：显式退出账号时重试打开账号菜单、点击退出、处理确认弹窗并等待登录页，避免 logout 后仍停留在业务页导致用例波动。
- [x] 优化 P0-API编辑内部成员-停用成员：内部成员登录步骤改为幂等，当前已是 `MCDL007` 时直接复用登录态，避免停用前重复退出/登录内部成员账号。
- [x] 创建 `tests/p0/member_management/internal_member_api_flow.py`，统一维护内部成员 API 类用例的账号、成员 ID、停用提示、内部成员登录、停用后登录失败断言和自动化账号恢复逻辑。
- [x] 创建 `tests/p0/member_management/test_15_api_disuse_internal_member.py`。
- [x] P0-API编辑内部成员-到期停用成员：退出自动化账号后登录内部成员 `MCDL007`，通过 GET 请求调用 `member/open/edit` 接口，传入 `disuse_enable=true`、`disuse_time=2026-1-31 12:00:00`、`time_zone=Etc/GMT-8`，校验 HTTP 状态码为 200 且 JSON `msg=success`。
- [x] P0-API编辑内部成员-到期停用成员：点击 APP 刷新按钮后校验回到登录页；若页面内刷新未触发会话失效，则页面级刷新兜底触发登录页；直接点击“立即登录”后校验停用账号提示，并校验没有成功登录。
- [x] P0-API编辑内部成员-到期停用成员：按步骤调用 `status=ENABLED` 启用成员，再额外调用 `disuse_enable=false` 清理共享内部成员的到期停用开关；重新登录内部成员账号确认可登录，再退出并登录自动化账号、确认自动化团队后回到成员列表。
- [x] 优化成员 open API 调用稳定性：`MemberEditApiClient` 在 HTTP 状态码非 200 时额外重试 3 次，默认间隔 1 秒；可通过 `test_data.api_member_edit.status_retry.times` 和 `status_retry.interval_seconds` 覆盖，四条成员 API 用例统一生效。
- [x] 增强四条成员 open API 用例的异常兜底恢复：新增 `tests/p0/member_management/api_case_recovery.py`，当用例中途异常时先 best-effort 调用接口恢复自动化账号 `status=ENABLED`、`disuse_enable=false`，再尝试登录自动化账号、切回自动化团队并进入成员列表；恢复异常写入 warning 日志，不覆盖原始失败原因。
- [x] 整理 P0-无编辑权限成员环境操作校验：将环境列表序号点击、整行 hover、快捷编辑入口扫描、行内下拉/批量工具栏可见性判断沉淀到 `EnvironmentPage`，用例文件只保留业务流程和断言。
- [x] 清理 IP代理列临时诊断脚本 `diagnose_ip2.py`，避免临时探测文件进入版本管理。
- [x] 新增 Streamlit UI 附加入口：`streamlit_runner.py`、`ui/app.py`、`ui/pages/01_执行用例.py`、`ui/pages/02_运行历史.py`、`UI使用文档.md`。
- [x] 整理 Streamlit UI：仪表盘用例数改为动态发现统计；执行用例页模块级选择改为“选中本模块/取消本模块”显式按钮，避免渲染时覆盖单条用例勾选状态；`streamlit` 纳入 `requirements.txt`。
- [x] 优化 Streamlit UI 执行链路：`AutomationRunner._run_suite()` 支持注入输出流，UI 执行改为复用 CLI 执行入口，保持恢复、截图、重试、flaky 统计和飞书通知行为一致。
- [x] 优化 Streamlit UI 用例发现：改用独立 discovery logger，避免页面刷新或用例统计时调用 `setup_logger()` 清空正在运行线程的日志 handler。
- [x] 优化 Streamlit UI 稳定性：首页和执行页增加配置/用例发现失败兜底提示；执行页增加长时间无日志提示；运行历史页增强 unittest fallback 摘要解析，并在筛选切换时重置分页。
- [x] 优化 Streamlit UI 并发控制：`streamlit_runner.py` 增加进程级执行锁，同一进程内仅允许一个 UI 执行任务运行，避免多个会话同时抢占 APP/CDP 和全局 logger。
- [x] 修正 Streamlit 首页 `st.page_link()` 路径为相对 `ui/app.py` 的 `pages/...`，并同步 `UI使用文档.md`。
- [x] 验证 Streamlit UI 整理结果：`python -m compileall -q core\runner.py streamlit_runner.py ui` 通过；`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases)); assert len(cases) == 54"` 通过，用例发现数量 54；`git diff --check` 通过，仅有 CRLF 工作区提示。
- [x] 优化自动化运行日志：`setup_logger()` 默认复用当前运行 handler，`run.py` 和 UI 执行入口显式 `reset=True` 新建本次运行日志，避免用例 `setUpClass()` 重复调用 `setup_logger()` 时把同一次执行拆成多个短日志。
- [x] 补充用例级日志：`AutomationTestResult` 统一记录 `CASE START`、`CASE PASS`、`CASE FAIL`、`CASE ERROR`、`CASE SKIP` 和耗时，失败/错误日志包含异常堆栈，历史页展开后可看到更完整的自动化执行过程。
- [x] 优化运行历史日志展示：按钮根据日志文件大小显示“查看完整日志”或“查看日志末尾 5000 字符”，日志超过预览长度时提示总字符数和当前预览范围。
- [x] 验证自动化日志优化：`python -m compileall -q core\logger.py core\result.py core\runner.py run.py streamlit_runner.py ui` 通过；临时 `AutomationTextRunner` 验证通过，同一次运行只生成 1 个 `run_*.log`，且包含 `CASE START #1 ...` 和 `CASE PASS ... elapsed=...`；当时用例发现为 54；`git diff --check` 通过，仅有 CRLF 工作区提示。
- [x] 整理 UI 接手文档结构：修正版本号为 1.5，拆分 UI 相关文件和核心兼容扩展点，删除无效图片占位，明确 CLI/UI 并发边界，并补充当前 UI 定位和后续计划；README 同步 UI 能力和使用限制。
- [x] 优化 Streamlit UI 易用性：执行页增加用例搜索，支持按模块、测试类、测试方法或完整 test_id 缩小展示范围；执行无 summary 时按预检失败、APP/CDP 失败、无匹配用例、执行器异常等场景展示更明确状态。
- [x] 优化运行历史筛选：历史页增加关键词、来源和日期范围筛选，筛选条件变化时重置分页；历史记录排序改为按日志文件修改时间倒序，避免旧/新日志文件名格式差异影响展示顺序。
- [x] 验证 Streamlit UI 启动与页面访问：重启 `streamlit run ui/app.py --server.port 8501 --server.address 127.0.0.1`，确认首页 HTTP 200，执行页和运行历史页可打开，新实例 PID 为 31676。
- [x] 关闭 Streamlit UI 后台服务：停止 PID 31676，并确认 8501 端口无监听；`UI使用文档.md` 补充 Windows 后台进程停止方式和 UI 冒烟验证范围。
- [x] 优化 Streamlit UI 实时日志区域：执行页 `st.code()` 增加固定高度 `_LOG_DISPLAY_HEIGHT = 420`，避免运行时日志区域随内容长度持续撑高页面；`UI使用文档.md` 升级到 1.8 并同步高度/保留行数调整方式。
- [x] 兼容环境列表表头短文案调整：APP 环境列表表头由 `环境序号`、`环境名称`、`环境分组` 调整为 `序号`、`名称`、`分组` 后，`EnvironmentPage` 增加环境列表表头别名归一化，统一兼容排序按钮、表头顺序、列表字段设置拖拽、按表头读取单元格和标签列读取逻辑。
- [x] 兼容全局设置环境字段短文案调整：`GlobalSettingsPage` 增加环境字段别名归一化，兼容环境字段显示限制弹窗 checkbox、当前设置回显、环境列表排序字段选择、下拉项选择和 `升序`/`降序` 回显判断。
- [x] 回归验证环境列表表头短文案兼容修复：`test_13_sort_environment_serial.py`、`test_14_move_remark_column.py`、`test_10_environment_field_display_limit.py`、`test_12_environment_list_sort_limit.py` 均在 `--attach-existing-app` 模式下串行单跑通过，均为 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 新增代理管理 P0 用例“创建自定义代理”：用例开始时开启 `config.yaml` 顶层 `windows_system_proxy.host/port` 指定的 Windows 系统代理，默认 `127.0.0.1:7897`，结束后关闭系统代理；进入代理管理，创建自定义 HTTP 代理，填写主机、端口、账号、密码，并显式确保“代理类型”为 `HTTP`；点击弹窗“检测代理”，等待“连接测试成功”或“连接失败”；若连接失败则记录延迟断言但继续清理；创建成功后按新代理 ID、主机、端口和代理类型校验列表、点击行内第一个操作按钮检测、等待“检测中”消失并从原单元格读取实际结果，最后点击行内最后一个操作按钮删除，并按新代理 ID、主机、端口和代理类型校验删除成功。
- [x] 代理管理“创建自定义代理”用例支持本地 `config/test_data.yaml` 或 `DICLOAK_PROXY_CUSTOM_ACCOUNT`、`DICLOAK_PROXY_CUSTOM_PASSWORD` 注入账号和密码；仓库模板不写真实账号密码。
- [x] 代理管理“创建自定义代理”用例已将 ping/F5 预检改为开启配置中的 Windows 系统代理；弹窗检测和列表行内检测均成功时用例通过，任一检测返回“连接失败”时按业务要求最终延迟断言失败；创建成功时会继续执行行内检测并删除新代理，避免影响后续运行。

## 十三、后续模块用例

- [x] 实现书签覆盖用例。
- [x] 实现书签追加用例。
- [x] 实现成员导出用例。
- [x] 实现环境管理导出环境用例。
- [x] 实现 APP 抓包进程拦截验证用例。
- [ ] 实现本地扩展包安装用例。
- [x] 实现代理管理用例第一版：创建自定义代理。
- [ ] 实现扩展管理用例。
- [x] 实现环境分组用例第一版：创建环境分组、包含环境的分组、授权成员的分组、环境分组名称筛选、修改环境分组名称、修改环境分组备注。
- [x] 实现成员管理用例第一版：创建外部成员。
- [x] 实现成员管理用例第二版：编辑外部成员名称。
- [x] 实现成员管理用例第三版：创建内部成员。
- [x] 实现成员管理用例第四版：编辑内部成员名称。
- [x] 实现成员管理用例第五版：成员分组筛选。
- [x] 实现成员管理用例第六版：成员名称筛选。
- [x] 实现成员管理用例第七版：成员备注筛选。
- [x] 实现成员管理用例第八版：登录账号、邮箱筛选。
- [x] 实现成员管理用例第九版：批量编辑成员备注。
- [x] 实现成员管理用例第十版：导出成员。
- [x] 实现成员管理用例第十一版：无编辑权限成员环境操作校验。
- [x] 实现成员管理用例第十二版：API编辑外部成员-停用成员。
- [x] 实现成员管理用例第十三版：API编辑外部成员-到期停用成员。
- [x] 实现成员管理用例第十四版：API编辑内部成员-停用成员。
- [x] 实现成员管理用例第十五版：API编辑内部成员-到期停用成员。
- [x] 实现全局设置-禁止查看网站密码用例第一版。
- [x] 实现全局设置-禁止打开浏览器开发者工具用例。
- [x] 实现全局设置-禁止管理/移除扩展用例。
- [x] 实现全局设置-禁止成员访问谷歌扩展商店和扩展设置页面用例。
- [x] 实现全局设置-禁止访问指定网址-快捷勾选谷歌应用商店、百度，并校验 b 站可正常访问用例。
- [x] 实现全局设置-允许访问指定网址-b 站用例。
- [x] 实现全局设置-禁用抓包软件用例。
- [x] 实现全局设置-书签设置覆盖用例。
- [x] 实现全局设置-书签设置追加用例。
- [x] 实现全局设置-环境字段显示限制用例。
- [x] 实现全局设置-环境列表分页设置用例。
- [x] 实现全局设置-环境列表排序设置用例。

## 十四、运行验证

- [x] 验证 `python run.py --config config/config.yaml --precheck`。
- [x] 验证 `python run.py --config config/config.yaml --level P0 --attach-existing-app`。
- [x] 验证按模块执行。
- [x] 验证 unittest 发现 `tests/p0` 当前 59 条 P0 用例：环境管理 25 条、全局设置 12 条、环境分组管理 6 条、成员管理 15 条、代理管理 1 条。
- [x] 验证按业务模块分类发现用例，`环境管理` 当前匹配 25 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`全局设置` 当前匹配 12 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`环境分组管理` 当前匹配 6 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`成员管理` 当前匹配 15 条 P0 用例。
- [x] 验证按业务模块分类发现用例，`代理管理` 当前匹配 1 条 P0 用例。
- [x] 验证环境列表表头短文案兼容修复后 P0-环境序号升降序用例通过：`python run.py --config config/config.yaml --module test_13_sort_environment_serial.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境列表表头短文案兼容修复后 P0-字段排序-备注调整到首位用例通过：`python run.py --config config/config.yaml --module test_14_move_remark_column.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境列表表头短文案兼容修复后全局设置-环境字段显示限制用例通过：`python run.py --config config/config.yaml --module test_10_environment_field_display_limit.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境列表表头短文案兼容修复后全局设置-环境列表排序设置用例通过：`python run.py --config config/config.yaml --module test_12_environment_list_sort_limit.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 修复 2026-05-15 全局设置模块回归异常：原运行 `total=12 passed=8 failed=0 errors=4 skipped=0 flaky=0`，异常集中在 `test_01_disable_view_password.py`、`test_02_disable_browser_devtools.py`、`test_03_disable_extension_management.py`、`test_04_disable_member_access_google_extension_pages.py`。
- [x] 定位 `test_01_disable_view_password.py` 异常：全局设置复选框点击脚本内缺少 `checkboxStateSelector` 和 `checkboxInputSelector` 变量定义，修复后单跑通过。
- [x] 定位 `test_02_disable_browser_devtools.py` 异常：同属全局设置复选框脚本异常，修复后单跑通过。
- [x] 定位 `test_03_disable_extension_management.py` 异常：复选框脚本修复后进入内核校验；Chrome Web Store 当前会先出现“切换到 Chrome 即可安装扩展程序和主题背景”的前置阻止提示，已兼容该阻止证据并单跑通过。
- [x] 定位 `test_04_disable_member_access_google_extension_pages.py` 异常：同属全局设置复选框脚本异常，修复后单跑通过。
- [x] 验证全局设置模块目录运行通过：`python run.py --config config/config.yaml --module global_settings --attach-existing-app`，结果 `total=12 passed=12 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全局设置模块第一条 P0 用例通过。
- [x] 验证全局设置模块第二条 P0 用例通过。
- [x] 验证全局设置模块第三条 P0 用例通过。
- [x] 验证全局设置模块第四条 P0 用例通过。
- [x] 验证全局设置模块第五条 P0 用例通过。
- [x] 验证全局设置模块第五条 P0 用例追加 b 站正常访问校验后通过：`python run.py --config config/config.yaml --module test_05_block_specific_websites_google_and_baidu.py --attach-existing-app`。
- [x] 验证全局设置模块第六条 P0 用例通过。
- [x] 验证全局设置模块第七条 P0 用例通过：`python run.py --config config/config.yaml --module test_07_disable_packet_capture_software.py --attach-existing-app`。
- [x] 验证全局设置模块第八条 P0 用例通过：`test_08_bookmark_setting_overwrite.py`。
- [x] 验证全局设置模块第九条 P0 用例通过：`test_09_bookmark_setting_append.py`。
- [x] 验证全局设置模块第十条 P0 用例通过：`test_10_environment_field_display_limit.py`。
- [x] 验证全局设置模块第十一条 P0 用例通过：`test_11_environment_list_pagination_setting.py`。
- [x] 验证全局设置模块第十二条 P0 用例通过：`test_12_environment_list_sort_limit.py`。
- [x] 验证环境分组管理模块第一条 P0 用例通过：`test_01_create_environment_group.py`。
- [x] 验证环境分组管理模块第二条 P0 用例通过：`python run.py --config config/config.yaml --module test_02_group_containing_environment.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第二条 P0 用例新增 `包含环境` 筛选校验后通过：`python run.py --config config/config.yaml --module test_02_group_containing_environment.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第三条 P0 用例通过：`python run.py --config config/config.yaml --module test_03_group_authorized_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第四条 P0 用例通过：`python run.py --config config/config.yaml --module test_04_filter_group_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第五条 P0 用例通过：`python run.py --config config/config.yaml --module test_05_edit_group_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块第六条 P0 用例通过：`python run.py --config config/config.yaml --module test_06_edit_group_remark.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组管理模块目录运行通过：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证环境分组统一元素定位整理后模块目录运行通过：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第一条 P0 用例通过：`python run.py --config config/config.yaml --module test_01_create_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第二条 P0 用例通过：`python run.py --config config/config.yaml --module test_02_edit_external_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第三条 P0 用例通过：`python run.py --config config/config.yaml --module test_03_create_internal_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第四条 P0 用例通过：`python run.py --config config/config.yaml --module test_04_edit_internal_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第五条 P0 用例通过：`python run.py --config config/config.yaml --module test_05_filter_member_group.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第六条 P0 用例通过：`python run.py --config config/config.yaml --module test_06_filter_member_name.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第七条 P0 用例通过：`python run.py --config config/config.yaml --module test_07_filter_member_remark.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第八条 P0 用例通过：`python run.py --config config/config.yaml --module test_08_filter_member_login_account_email.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块第九条 P0 用例通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，目录运行中 `test_09_batch_edit_member_remark.py` 通过。
- [x] 验证成员管理模块第十条 P0 用例通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，目录运行中 `test_10_export_member.py` 通过。
- [x] 验证修复成员列表入口后第一条成员管理用例单跑通过：`python run.py --config config/config.yaml --module test_01_create_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理模块目录运行通过：`python run.py --config config/config.yaml --module member_management --attach-existing-app`，结果 `total=11 passed=11 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理第十一条 P0 用例通过：`python run.py --config config/config.yaml --module test_11_no_edit_permission_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证成员管理第十二条 P0 用例通过：`python run.py --config config/config.yaml --module test_12_api_disable_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；优化弹窗等待后单跑耗时约 21.45s。
- [x] 验证新增成员管理第十二条后用例发现数量：`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases))"` 输出 `55`。
- [x] 验证成员管理第十三条 P0 用例通过：`python run.py --config config/config.yaml --module test_13_api_disuse_external_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，单跑耗时约 23.38s。
- [x] 验证新增成员管理第十三条后用例发现数量：`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases))"` 输出 `56`。
- [x] 验证成员管理第十四条 P0 用例通过：`python run.py --config config/config.yaml --module test_14_api_disable_internal_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，单跑耗时约 43.92s；首次验证出现退出登录未到登录页和内部成员登录态误判波动，补强 `LoginPage.logout_to_login_page()` 与通用登录态判断后重跑稳定通过。
- [x] 验证新增成员管理第十四条后用例发现数量：`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases))"` 输出 `57`。
- [x] 验证成员管理第十五条 P0 用例通过：`python run.py --config config/config.yaml --module test_15_api_disuse_internal_member.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，单跑耗时约 58.45s；补充成员 open API 非 200 状态码重试 3 次后回归通过。
- [x] 验证新增成员管理第十五条后用例发现数量：`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases))"` 输出 `58`。
- [x] 验证四条成员 open API 用例补充异常兜底恢复后均单跑通过：`test_12_api_disable_external_member.py` 结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，耗时约 30.49s；`test_13_api_disuse_external_member.py` 结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，耗时约 30.08s；`test_14_api_disable_internal_member.py` 结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，耗时约 56.27s；`test_15_api_disuse_internal_member.py` 结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，耗时约 61.77s。
- [x] 验证四条成员 open API 兜底恢复改动：`python -m py_compile tests\p0\member_management\api_case_recovery.py tests\p0\member_management\member_open_api.py tests\p0\member_management\test_12_api_disable_external_member.py tests\p0\member_management\test_13_api_disuse_external_member.py tests\p0\member_management\test_14_api_disable_internal_member.py tests\p0\member_management\test_15_api_disuse_internal_member.py` 通过；`python -m compileall -q pages tests` 通过；用例发现仍为 `58`；敏感 token/临时脚本扫描无命中。
- [x] 同步全部项目文档：`README.md`、`dicloak自动化框架搭建计划.md`、`UI使用文档.md`、`飞书接入文档.md`、`test_data/README.md` 与本文档均已对齐当前 58 条 P0、成员管理 15 条、成员 open API 重试/异常兜底恢复、UI 1.9、飞书 flaky 字段和测试数据敏感信息规则。
- [x] 配置化四条成员 open API 用例数据：`config/test_data.example.yaml` 和本地 `config/test_data.yaml` 补齐 `test_data.api_member_edit`，统一维护接口地址、外部成员 ID、内部成员信息、到期停用参数和接口状态码重试参数；配置采用 `internal_member`、`disuse`、`status_retry` 小块分组，避免 YAML 过度膨胀；真实 token 继续通过 `DICLOAK_API_MEMBER_EDIT_TOKEN` 注入，不写入仓库。
- [x] 验证代理管理“创建自定义代理”静态检查：`python -m compileall -q pages tests core` 通过；`git diff --check` 通过，仅有 `README.md`、`config/config.example.yaml`、`config/test_data.example.yaml`、`core/config.py`、`test_data/README.md`、`任务进度同步文档.md` 工作区 LF/CRLF 转换提示。
- [x] 验证代理管理“创建自定义代理”单跑：`python run.py --config config/config.yaml --module test_01_create_custom_proxy.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；日志确认 `Windows system proxy enable proxy_server=127.0.0.1:7897`，创建行 `Proxy custom created id=2064195512527990786 type=HTTP host=accel.ipflygates.com port=5001`，随后执行行内检测并只删除该新代理，用例结束后 `Windows system proxy disable current_enable=1 current_server=127.0.0.1:7897`，运行后注册表确认 `ProxyEnable=0`。
- [x] 新增跨平台兼容改造计划书和跨平台运行与开发文档，确定采用“CDP 主链路跨平台 + 平台能力适配层 + 高风险能力分阶段降级/skip”的渐进式方案。
- [x] 启动跨平台 M1 最小骨架：新增 `core/platform/` 平台识别与能力声明，新增 `core/system_proxy.py` 统一系统代理入口；Windows 继续复用 `core/windows_proxy.py`，macOS/Linux 暂时明确 unsupported；代理管理创建自定义代理用例改为平台不支持系统代理时 `skipTest`，避免非 Windows 导入阶段失败。
- [x] 调整依赖声明：新增 `psutil>=5.9.0,<6.0`，并将 `uiautomation` 改为 `platform_system == "Windows"` 条件依赖，为后续 `core/process.py` 跨平台迁移做准备。
- [x] 验证跨平台 M1 最小骨架 Windows attach-existing-app smoke：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 启动跨平台 M2 进程层迁移：`core/process.py` 保持原有函数名和调用方不变，内部优先使用 `psutil` 读取进程名、PID、命令行、可执行路径和监听端口；Windows 原有 `tasklist`、PowerShell、`wmic`、`Get-NetTCPConnection` 路径保留为 fallback。已用当前 DICloak 进程做轻量探针，确认可读取运行状态、主进程 PID、exe 路径、命令行和监听端口。
- [x] 验证跨平台 M2 进程层迁移低风险回归：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证跨平台 M2 进程层迁移定向内核用例：`python run.py --config config/config.yaml --module test_01_kernel_integrity.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；覆盖 PID 存活、命令行解析、可执行路径读取、监听端口发现和内核 CDP 识别。
- [x] 验证新增代理管理用例后发现数量：`python -c "from streamlit_runner import discover_cases; cases=discover_cases(); print(len(cases))"` 输出 `59`。
- [x] 验证全量 P0 运行通过（含第十一条）：`python run.py --config config/config.yaml --attach-existing-app`，结果 `total=54 passed=54 failed=0 errors=0 skipped=0 flaky=0`（两次）。
- [x] 验证 P0 用例文件移动到 `environment_management/` 后全量运行通过。
- [x] 验证按单条用例执行。
- [x] 验证 APP 启动和关闭：跨平台 M3 托管启动修复后，`environment_group_management` 托管运行通过；M6 环境分组、成员管理、环境管理、全局设置均使用托管启动运行，结束后未发现残留 `DICloak.exe` 主进程。
- [x] 验证已有 APP 实例关闭后重启：M3/M6 多次不带 `--attach-existing-app` 运行均完成 APP 启动、前端 ready 等待、CDP startup check 和结束关闭。
- [x] 验证 APP 进程 30 秒检测：M3/M6 托管运行日志均显示 `APP process detected: DICloak.exe`，当前 Windows 配置仍能在启动等待窗口内识别 APP 主进程。
- [x] 验证 CDP 连接。
- [x] 验证调试模式 `--attach-existing-app` 连接已打开 APP。
- [x] 验证统一超时时间配置后，P0 内核完整性用例仍可通过。
- [x] 验证 P0-批量创建 5 个环境用例通过。
- [x] 验证 P0-批量创建 134 内核环境用例通过。
- [x] 验证 P0-批量导入环境用例通过。
- [x] 验证 P0-编辑环境名称用例通过。
- [x] 验证 P0-编辑固定打开网址用例通过。
- [x] 验证 P0-环境列表筛选-自动化分组用例通过。
- [x] 验证 P0-环境列表备注筛选用例通过。
- [x] 验证 P0-置顶环境用例通过。
- [x] 验证 P0-列表快捷修改环境名称用例通过。
- [x] 验证 P0-环境序号升降序用例通过。
- [x] 验证 P0-字段排序-备注调整到首位用例通过。
- [x] 验证 P0-导出环境用例通过。
- [x] 验证 P0-创建多分组环境用例通过。
- [x] 验证 P0-批量创建多分组环境用例通过。
- [x] 验证 P0-单环境编辑多分组用例通过。
- [x] 验证 P0-批量编辑环境多分组用例通过。
- [x] 验证 P0-创建标签用例通过。
- [x] 验证 P0-创建带有标签的环境用例通过。
- [x] 验证 P0-筛选标签用例通过。
- [x] 验证用例前后恢复钩子不影响环境管理用例执行：`test_21_create_environment_with_tags.py` 通过。
- [ ] 验证 Playwright 连接失败时的兜底逻辑。
- [ ] 验证飞书测试通知。
- [ ] 验证执行总结通知。
- [ ] 验证失败告警通知。
- [ ] 验证模块级熔断通知。
- [x] 验证失败截图：通过临时失败用例确认截图生成到 `screenshots/`，失败摘要和日志均记录截图路径。
- [ ] 验证失败日志采集。
- [ ] 验证失败重试。
- [ ] 验证登录失败熔断。
- [ ] 验证 CDP 连接失败熔断。
- [x] 验证退出码 `0`。
- [ ] 验证退出码 `1`。
- [ ] 验证退出码 `2`。
- [ ] 验证退出码 `3`。

## 十五、第一版交付

- [x] 完成第一版代码。
- [x] 完成第一版配置模板。
- [x] 完成第一版 README 使用说明。
- [x] 完成第一版 P0 用例验证。
- [ ] 完成第一版交付检查。

## 2026-06-15 跨平台 M3 APP 生命周期低风险迁移

- [x] 新增 `core/app_config.py` 统一解析 APP 托管启动配置，支持 `platforms.<name>.app`/`platforms.<name>` 平台覆盖层，兼容 `executable_path` 和 `exe_path`，缺省时回退旧 `app` 配置。
- [x] 将 `core/app.py` 的启动路径、工作目录、APP 进程名读取接入统一解析器；Windows 关闭 APP 仍保留原 `taskkill /F /T /IM` 路径，macOS/Linux 预置 `psutil` 进程树终止路径。
- [x] 将 `core/precheck.py` 的 APP 路径预检接入统一解析器，保证平台覆盖层和旧配置走同一套校验。
- [x] 将 `browser_process_name` 接入平台覆盖层，并同步到环境管理内核进程发现、内核缓存可执行文件等待和全局设置抓包阻断用例。
- [x] 同步 `core/platform/capabilities.py` 能力矩阵：macOS/Linux 的进程命令行读取和监听端口读取已标记为支持，APP 托管启动仍保留待真实平台验收。
- [x] 将 `core/ui_driver.py` 主窗口进程名提示接入统一 APP 配置解析，减少平台覆盖层和 Windows 桌面能力入口之间的配置分叉。
- [x] 验证 M3 基础编译：`python -m compileall -q core pages tests` 通过。
- [x] 验证 M3 预检：`python run.py --config config/config.yaml --precheck` 通过，当前 Windows 仍解析到原 `DICloak.exe`、`GinsBrowser.exe`、APP 工作目录和 CDP 启动参数。
- [x] 验证 M3 低风险模块回归：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=2`；两条首轮 `overlay did not close` 重试后通过，记录为既有 UI 波动。
- [x] 首次补跑 M3 托管启动验收：`python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=0 failed=0 errors=6 skipped=0 flaky=0`；定位为 APP 进程已启动且 CDP 可连接，但 Electron 前端仍停留在 `正在加载中...`，用例进入登录流程过早。
- [x] 修复托管启动就绪判断：在 `core/cdp_driver.py` 新增 `wait_for_app_ready()`，并在 `core/runner.py` startup check 中等待前端 ready 后再释放连接；ready 条件为加载文案消失并出现登录控件、主界面菜单或可操作控件。
- [x] 修复后补跑 M3 托管启动验收：`python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=1`；验证 APP 启动、前端就绪等待、CDP startup check、低风险模块执行和结束关闭 APP 均可用。

## 2026-06-15 跨平台 M4 桌面文件对话框能力降级

- [x] 新增 `core/platform/desktop.py`，统一维护桌面文件对话框能力判断和不支持平台提示。
- [x] 加固 `core/ui_driver.py`：初始化时先检查 `desktop_file_dialog` 能力，非支持平台直接给出明确错误，避免依赖 `uiautomation` 导入失败。
- [x] 调整导出环境用例：CDP download 优先，Windows 保存框兜底保留；非 Windows 如需原生保存框则明确 `skipTest`。
- [x] 调整导出成员用例：与导出环境保持一致的 CDP 优先和桌面能力降级策略。
- [x] 代码审阅结论：Windows 行为未改变，平台判断未散落到测试文件之外，macOS/Linux 缺少原生文件对话框能力时不会硬失败。
- [x] 验证 M4 导出环境 Windows 托管运行：`python run.py --config config/config.yaml --module test_15_export_environment.py`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证 M4 导出成员 Windows 托管运行：`python run.py --config config/config.yaml --module test_10_export_member.py`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。

## 2026-06-15 跨平台 M5 系统代理抽象复核

- [x] 复核代理管理用例导入：用例已通过 `core.system_proxy` 访问系统代理能力，非支持平台类级别 `skipUnless(system_proxy_supported())`。
- [x] 修复系统代理清理问题：新增 `restore_windows_system_proxy_settings()` 和 `restore_system_proxy_settings()`，用例 finally 中恢复开始前的 `ProxyEnable`、`ProxyServer`、`ProxyOverride` 快照，不再固定关闭系统代理。
- [x] 验证系统代理快照原样恢复探针：读取当前系统代理快照后立即恢复，恢复前后快照一致。
- [x] 验证 M5 编译：`python -m compileall -q core pages tests` 通过。
- [x] 运行 M5 Windows 代理用例：`python run.py --config config/config.yaml --module proxy_management`，结果 `total=1 passed=0 failed=1 errors=0 skipped=0 flaky=0`。
- [x] M5 失败定位：APP 托管启动、前端 ready、系统代理启用、代理创建、代理删除、系统代理恢复均执行到位；失败仅为业务代理检测返回 `连接失败`，当前判断依赖外部代理服务/账号连通性，不能作为本轮兼容改造通过证据。
- [x] M5 运行后状态确认：系统代理已恢复到用例开始前状态 `ProxyEnable=1`、`ProxyServer=127.0.0.1:7897`，APP/CDP 已关闭。

## 2026-06-15 跨平台 M6 Windows 模块回归

- [x] 验证 M6 环境分组模块 Windows 托管回归：`python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证 M6 成员管理模块 Windows 托管回归：`python run.py --config config/config.yaml --module member_management`，结果 `total=15 passed=15 failed=0 errors=0 skipped=0 flaky=1`。
- [x] 成员管理波动记录：`test_09_batch_edit_member_remark.py` 首轮批量备注追加校验波动，重试后通过；当前判断为 UI/数据刷新波动，未发现跨平台兼容层代码问题。
- [x] 验证 M6 环境管理模块 Windows 托管回归：`python run.py --config config/config.yaml --module environment_management`，结果 `total=25 passed=25 failed=0 errors=0 skipped=0 flaky=4`。
- [x] 环境管理波动记录：首轮波动集中在 `overlay did not close`、环境序号排序刷新等待、导出环境搜索结果等待，重试后均通过；当前未发现跨平台进程层、APP 启动层或桌面文件对话框降级改造导致的回归。
- [x] 环境管理模块收尾检查：`python -m compileall -q core pages tests` 通过，`git diff --check` 无空白错误，仅有 Windows LF/CRLF 提示；托管运行结束后未发现残留 `DICloak.exe` 主进程。
- [x] 验证 M6 全局设置模块首次 Windows 托管回归：`python run.py --config config/config.yaml --module global_settings`，结果 `total=12 passed=8 failed=2 errors=2 skipped=0 flaky=0`。
- [x] 定位全局设置首次回归失败：`test_05_block_specific_websites_google_and_baidu.py` 与 `test_06_allow_specific_website_bilibili.py` 均因允许 URL `https://bilibili.com` 返回 `ERR_CONNECTION_CLOSED` 失败；`test_11_environment_list_pagination_setting.py` 与 `test_12_environment_list_sort_limit.py` 为前置失败后的页面/全局设置状态扰动。
- [x] 修复访问限制允许 URL 外网依赖：新增 `core/local_http.py` 的 `LocalHttpProbe`，05/06 用例改用本机 `127.0.0.1` HTTP 探针作为允许访问目标，阻断目标仍保留百度和谷歌应用商店。
- [x] 验证全局设置访问限制修复：`test_05_block_specific_websites_google_and_baidu.py` 单跑通过，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全局设置允许访问修复：`test_06_allow_specific_website_bilibili.py` 单跑通过，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全局设置后续状态用例恢复：`test_11_environment_list_pagination_setting.py` 单跑通过，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证全局设置排序设置恢复：`test_12_environment_list_sort_limit.py` 单跑通过，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证 M6 全局设置模块修复后 Windows 托管回归：`python run.py --config config/config.yaml --module global_settings`，结果 `total=12 passed=12 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 全局设置模块收尾检查：`python -m compileall -q core pages tests` 通过，`git diff --check` 无空白错误，仅有 Windows LF/CRLF 提示；托管运行结束后未发现残留 `DICloak.exe` 主进程。
- [x] 同步跨平台配置模板：`config/config.example.yaml` 补充 `platform`、`platforms.windows/macos/linux` 和 `system_proxy` 示例；`core.config.DEFAULT_CONFIG` 同步默认结构并继续兼容旧 `app.*`、`windows_system_proxy`。
- [x] 验证配置模板补充后 Windows 预检：`python run.py --config config/config.yaml --precheck` 通过。
- [x] 验证示例配置平台覆盖解析：使用 `resolve_app_config()` 分别解析 `windows`、`macos`、`linux`，均能得到对应 APP 进程名、内核进程名和可执行路径字段。

## 2026-06-15 Linux 远程调通

- [x] 连接远端 Linux：`ssh dic@192.168.20.160`，确认系统为 Ubuntu 24.04、Python 3.12.3、DICloak 安装于 `/opt/DICloak/dicloak`。
- [x] 同步当前工作区到远端：`/home/dic/dicloak_automation_linux`，并在远端创建 `.venv`、安装 `requirements.txt`；Linux 下 `uiautomation` 因平台条件依赖被正确跳过。
- [x] 验证远端基础编译：`python -m compileall -q core pages tests` 通过。
- [x] 手动确认 Linux 图形会话环境：GNOME/Wayland，存在 `/run/user/1000/bus`、`wayland-0`、Xwayland `X0/X1` 和 `.mutter-Xwaylandauth.*`。
- [x] 修复 Linux SSH 托管启动：`core.app.AppManager` 启动 APP 时自动补齐 Linux 图形会话环境变量，解决 Electron 在 SSH 会话中缺少 `DISPLAY/XAUTHORITY` 导致无法启动的问题。
- [x] 修复 Linux 进程层 fallback：`core.process` 在非 Windows 平台不再误调用 Windows `tasklist/wmic/powershell` fallback，避免 Linux 上出现 `unknown encoding: mbcs`。
- [x] 修正 Linux 内核浏览器进程名：配置示例和远端运行配置使用 `browser_process_name: ginsbrowser`。
- [x] 验证 Linux 远端预检：`python run.py --config config/config.yaml --precheck` 通过。
- [x] 验证 Linux attach-existing-app 模式环境分组模块：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证 Linux 托管启动模式环境分组模块：`python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 验证 Linux 托管运行结束后 CDP 9222 关闭，说明 APP 关闭流程已执行。
- [x] 记录 Linux 当前边界：已调通依赖安装、precheck、CDP、APP 托管启动/关闭和环境分组低风险模块；系统代理、原生文件选择器、环境管理/成员管理/全局设置仍待分层验证。
- [x] 同步 Linux 启动说明和不支持清单到 `README.md`、`跨平台运行与开发文档.md`：补充远端目录、venv 激活、precheck、托管启动、attach-existing-app 命令，以及 Linux 系统代理、原生文件选择器、未验证模块边界。
## 2026-06-16 Mac 远程跑通记录

- [x] 连接 Mac 远程终端：`ssh tianji@192.168.40.5`，确认系统为 macOS 14.5、arm64。
- [x] 确认 DICloak APP：`/Applications/DICloak.app`，版本 `2.9.4`，可执行文件 `/Applications/DICloak.app/Contents/MacOS/DICloak`，进程名 `DICloak`，内核浏览器进程名 `GinsBrowser`。
- [x] 同步当前工作区到 Mac：`/Users/tianji/dicloak_automation_mac_20260616_115230`，并创建软链接 `/Users/tianji/dicloak_automation_mac`。
- [x] 处理 Mac Python 版本：系统 Python 3.9.6 不满足 `>=3.10` 预检要求，改用 `/Users/tianji/.pyenv/shims/python3.11` 创建 `.venv`，实际版本 `Python 3.11.6`。
- [x] 安装依赖并确认 `yaml`、`playwright`、`psutil`、`openpyxl` 可导入。
- [x] 生成 Mac 专用远端配置 `config/config.macos.yaml`，配置 `platform.name=macos`、APP 路径、工作目录、进程名、`--remote-debugging-port=9222` 和 `--remote-allow-origins=*`；远程验证关闭飞书通知。
- [x] Mac 编译检查通过：`python -m compileall -q core pages tests`，结果 `COMPILE_EXIT=0`。
- [x] Mac 预检通过：`python run.py --config config/config.macos.yaml --precheck`，结果 `PRECHECK_EXIT=0`。
- [x] Mac 托管启动模式跑通环境分组模块：`python run.py --config config/config.macos.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`，重试次数 `0`。
- [x] 单独新建 `Mac远程跑通记录.md`，记录 Mac 接入、环境、配置、验证命令、结果和后续风险。

## 2026-06-16 Mac P0 全量运行记录

- [x] 为提升 Mac 全量成功率，已将 `test_07_disable_packet_capture_software` 标记为 Windows 专属用例；Mac 上跳过该依赖 `geek.exe` 和 `taskkill` 的用例。
- [x] 修复 Mac 内核缓存目录解析：设置页返回 `.DICloakCache`，但 Mac 内核目录实际位于 `~/Library/Application Support/DICloak/browsers`，新增 `resolve_kernel_browsers_dir()` 兼容解析。
- [x] 补充 `resolve_app_config()` 的 macOS/Linux 默认进程名兜底，避免 Mac 用例在缺少平台覆盖时继续查找 `GinsBrowser.exe`。
- [x] 加固环境列表分页下拉选择：Mac 上 Crisp 浮层可能拦截真实点击，已增加坐标点击和 DOM 事件 fallback。
- [x] 失败项复跑通过：`python run.py --config config/config.macos.yaml --module test_01_kernel_integrity.py`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 失败项复跑通过：`python run.py --config config/config.macos.yaml --module test_11_environment_list_pagination_setting.py`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] Mac P0 全量通过：`python run.py --config config/config.macos.yaml --level P0`，结果 `total=59 passed=57 failed=0 errors=0 skipped=2 flaky=1`，退出码 `0`。
- [x] Mac 全量重试统计：`RETRY_FAILURE_COUNT=1`，`RETRYING_COUNT=1`，`PASSED_AFTER_RETRY_COUNT=1`；重试后通过用例为 `test_01_kernel_integrity.py`。
- [x] Mac 全量跳过项：抓包软件阻断用例因 Windows 工具依赖跳过；代理管理用例因 macOS 系统代理能力当前 unsupported 跳过。
- [x] Mac 全量收尾：运行结束后 `POST_CLEANUP_CDP_9222_OCCUPIED=False`，已清理 DICloak 主进程和 Helper 残留。

## 2026-06-16 Mac 代理管理不跳过调整记录

- [x] 调整代理管理用例策略：macOS 当前仍不支持系统代理启停能力，但 `test_create_custom_proxy_detect_and_delete` 不再按 `system_proxy_supported()` 类级跳过。
- [x] 新策略：Windows 支持系统代理时继续启用并恢复系统代理快照；macOS/Linux 不支持系统代理时记录日志并继续执行代理创建、检测、删除业务流程。
- [x] Mac 单跑代理管理通过：`python run.py --config config/config.macos.yaml --module proxy_management`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] Mac 代理管理单跑关键结果：系统代理 unsupported 被记录为信息日志；自定义代理创建成功；行内检测结果 `连接成功`；删除成功。
- [x] 调整后 Mac P0 全量通过：`python run.py --config config/config.macos.yaml --level P0`，结果 `total=59 passed=58 failed=0 errors=0 skipped=1 flaky=1`，退出码 `0`。
- [x] 调整后 Mac 全量唯一跳过项：`test_disable_packet_capture_software`，原因是依赖 Windows `.exe` 工具和 `taskkill`。
- [x] 调整后 Mac 全量收尾：`POST_CLEANUP_CDP_9222_OCCUPIED=False`，未发现 DICloak/CDP 残留。

## 2026-06-16 Streamlit UI 内网远程执行第一版

- [x] 明确方案：UI 只做远程调度，不直接跨机器控制 APP/CDP；远端继续执行已有 `python run.py`。
- [x] 新增 `core/remote_runner.py`，支持读取远程节点、构造远端 CLI 命令、SSH 执行、实时日志回传和节点级执行锁。
- [x] 新增 `config/remote_hosts.example.yaml`，覆盖 Linux/macOS 示例节点；真实 `config/remote_hosts.yaml` 加入 `.gitignore`。
- [x] `requirements.txt` 增加 `paramiko`，用于 SSH 远程执行。
- [x] `streamlit_runner.py` 新增 `discover_remote_hosts()` 和 `run_remote_cli()`，保留原本机 `run_selected_tests()` 执行路径。
- [x] `ui/pages/01_执行用例.py` 新增“执行位置=本机/远程节点”；远程节点模式支持远程预检、P0 全量、业务模块、目录模块、单用例名。
- [x] UI 结果解析兼容远端 CLI 摘要 `Final test summary: total=... passed=... failed=... errors=... skipped=... flaky=...`。
- [x] 远程日志回传前增加基础脱敏，隐藏 `apiSecret`、`BOOT_TOKEN`、`USER_PASSWD`、`password`、`token`。

审阅发现并修复：

- [x] 首次 Mac 远程预检失败：远程节点配置直接使用 pyenv Python，缺少 `PyYAML`。修复为先激活 `.venv/bin/activate`，再执行 `python run.py`。
- [x] Mac 模块远程运行时，APP stdout 可能包含 `apiSecret`。修复为远程日志进入 UI queue 前统一脱敏。

验证：

```powershell
python -m compileall -q core streamlit_runner.py ui
python -m compileall -q core pages tests streamlit_runner.py ui
python -c "from streamlit_runner import discover_cases; print(len(discover_cases()))"
```

结果：

```text
compileall: passed
discover_cases: 59
```

UI 冒烟：

```text
streamlit run ui/app.py --server.port 8501 --server.address 127.0.0.1 --server.headless true
首页 HTTP_200=True
服务已停止，PORT_STILL_LISTENING=False
```

真实远程验证：

```text
Mac remote precheck:
cd /Users/tianji/dicloak_automation_mac && . .venv/bin/activate && python run.py --config config/config.macos.yaml --precheck
退出码 0

Mac remote environment_group_management:
cd /Users/tianji/dicloak_automation_mac && . .venv/bin/activate && python run.py --config config/config.macos.yaml --module environment_group_management
total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0
退出码 0
```

当时边界：

- 当时远程模式暂不按 UI 左侧勾选 test id 执行；2026-06-18 已调整为“执行用例”时按下方已勾选 test id 执行。
- 第一版不做代码同步、依赖安装、远端配置生成、远端截图/报告拉取。
- 远端项目、虚拟环境、APP 图形会话和配置需提前准备好。

## 2026-06-16 Streamlit UI 远程节点健康检查

- [x] 新增 `core.remote_runner.run_remote_health_check()` 和 `build_remote_health_check_command()`，健康检查只读执行，不启动 APP、不跑用例。
- [x] 新增 `streamlit_runner.check_remote_host()`，复用 UI 进程级执行锁。
- [x] 执行页远程节点模式新增“检查远程节点”按钮。
- [x] 健康检查输出 `[PASS]` / `[FAIL]` 明细，并以 `远程健康检查完成 → 失败=N` 汇总。

检查项：

- `project_dir` 是否存在。
- `run.py` 是否存在。
- 远端运行配置是否存在。
- `venv_activate` 是否存在并可激活。
- Python 版本是否可读取。
- `yaml`、`playwright`、`psutil`、`openpyxl` 是否可导入。
- 远端配置是否能通过 `core.config.load_config()` 加载。
- 配置解析出的 DICloak APP 路径是否存在。

Mac 真实节点验证：

```text
远程节点：macos-arm64
[PASS] project_dir: /Users/tianji/dicloak_automation_mac
[PASS] run.py exists
[PASS] config exists: config/config.macos.yaml
[PASS] venv activate exists: .venv/bin/activate
[PASS] python version: Python 3.11.6
[PASS] python dependencies: all core modules importable
[PASS] config load: config loaded; platform=macos
[PASS] APP path exists: /Applications/DICloak.app/Contents/MacOS/DICloak
远程健康检查完成 → 失败=0
远程健康检查结束 → 节点=macos-arm64 退出码=0
```

远端缺项目验证：

```text
[FAIL] project_dir missing: /Users/tianji/dicloak_missing_project_for_health_check
远程健康检查完成 → 失败=1
EXIT_CODE=1
```

本地收尾验证：

```text
python -m compileall -q core pages tests streamlit_runner.py ui
通过

git diff --check
通过，仅有 LF/CRLF 换行提示

discover_cases
CASES=59

discover_remote_hosts
HOSTS=macos-arm64,linux-ubuntu

git check-ignore -v config/remote_hosts.yaml
.gitignore:13:config/remote_hosts.yaml

UI 冒烟
HTTP_200=True
PORT_STILL_LISTENING=False
```

## 2026-06-16 Streamlit UI 远程产物拉取

- [x] 新增远程执行后产物拉取能力：通过 SFTP 拉取远端本次运行开始后修改过的 `logs/`、`screenshots/`、`reports/`。
- [x] 新增本机归档目录规范：`remote_artifacts/<node-name>/<yyyyMMdd_HHmmss>/`。
- [x] `.gitignore` 增加 `remote_artifacts/*`，避免远程日志、截图、报告误提交。
- [x] `core.remote_runner` 新增 `RemoteArtifactResult` 和 `collect_remote_artifacts()`。
- [x] `streamlit_runner.run_remote_cli()` 增加 `collect_artifacts` 参数；远程命令执行结束后自动归档。
- [x] 执行页远程节点模式新增“远程执行后拉取产物”开关，默认开启。
- [x] 远程 UI 日志脱敏补强：新增隐藏 JSON 中的 `email`、`name`、`realName`、`idCard`、`mobile` 字段。

Mac 真实节点验证：

```text
远程命令：
cd /Users/tianji/dicloak_automation_mac && . .venv/bin/activate && python run.py --config config/config.macos.yaml --module environment_group_management

结果：
total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0
退出码=0
```

产物拉取结果：

```text
远程产物拉取完成 → 文件数=1 local_dir=remote_artifacts\macos-arm64\20260616_185532
远程产物归档 → 文件数=1 本地目录=remote_artifacts\macos-arm64\20260616_185532
```

归档文件：

```text
remote_artifacts/macos-arm64/20260616_185532/logs/run_20260616_185243_313845.log
```

忽略规则验证：

```text
git check-ignore -v remote_artifacts\macos-arm64\20260616_185532\logs\run_20260616_185243_313845.log
.gitignore:17:remote_artifacts/*
```

本地收尾验证：

```text
python -m compileall -q core pages tests streamlit_runner.py ui
通过

git diff --check
通过，仅有 LF/CRLF 换行提示

discover_cases
CASES=59

discover_remote_hosts
HOSTS=macos-arm64,linux-ubuntu

UI 冒烟
HTTP_200=True
PORT_STILL_LISTENING=False

Mac 远端残留检查
DICloak/GinsBrowser: none
CDP 9222: none
```

Linux 真实节点补充验证：

```text
远程健康检查：
[PASS] project_dir: /home/dic/dicloak_automation_linux
[PASS] run.py exists
[PASS] config exists: config/config.yaml
[PASS] venv activate exists: .venv/bin/activate
[PASS] python version: Python 3.12.3
[PASS] python dependencies: all core modules importable
[PASS] config load: config loaded; platform=auto
[PASS] APP path exists: /opt/DICloak/dicloak
远程健康检查完成 → 失败=0

远程命令：
cd /home/dic/dicloak_automation_linux && . .venv/bin/activate && python run.py --config config/config.yaml --module environment_group_management

结果：
total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0
退出码=0

远程产物拉取完成 → 文件数=1 local_dir=remote_artifacts\linux-ubuntu\20260616_191350
远程产物归档 → 文件数=1 本地目录=remote_artifacts\linux-ubuntu\20260616_191350

归档文件：
remote_artifacts/linux-ubuntu/20260616_191350/logs/run_20260616_191026_066285.log

忽略规则验证：
git check-ignore -v remote_artifacts\linux-ubuntu\20260616_191350\logs\run_20260616_191026_066285.log
.gitignore:17:remote_artifacts/*

Linux 远端残留检查
dicloak/ginsbrowser: none
CDP 9222: none
```

本地收尾验证补充：

```text
python -m compileall -q core pages tests streamlit_runner.py ui
通过

git diff --check
通过，仅有 LF/CRLF 换行提示

python run.py --config config/config.yaml --precheck
通过

Windows 模块用例本轮未强制托管执行：当前本机已有 DICloak 进程，但未开放 CDP 9222；直接托管运行会关闭现有 APP，会话风险高于本轮 Linux 远程验证收益。上一次 M10 本地 UI/远程链路验证结果保持不变。
```

## 2026-06-17 Windows 全量回归审阅记录

- [x] 审阅本轮跨平台与远程执行相关改动，重点覆盖 `core/remote_runner.py`、`streamlit_runner.py`、`ui/pages/01_执行用例.py`、`core/app.py`、`core/process.py`、`core/system_proxy.py`、`pages/environment_page.py` 和代理/导出相关用例。
- [x] 静态检查通过：`python -m compileall -q core pages tests streamlit_runner.py ui`。
- [x] 空白检查通过：`git diff --check` 无空白错误，仅有 Windows 工作区 LF/CRLF 换行提示。
- [x] 首轮 Windows P0 全量托管运行：`python run.py --config config/config.yaml --level P0`，结果 `total=59 passed=53 failed=0 errors=6 skipped=0 flaky=6`。
- [x] 按用户要求，代理检测失败不作为本轮阻断项；代理用例失败原因为“创建弹窗检测代理结果为连接失败”，系统代理快照已恢复。
- [x] 修复环境管理稳定性问题：
  - `_wait_for_overlay_closed()` 增强 Element Plus 弹窗/抽屉可见性判断，过滤关闭后仍保留尺寸但不在视口内的节点，并排除“快速入门”引导浮层。
  - `wait_environment_serials_sorted()` 增加置顶行处理，环境序号升降序断言只校验置顶行之后的普通行，避免置顶环境固定在首行导致排序误判。
- [x] 修复后单跑验证通过：
  - `python run.py --config config/config.yaml --module test_02_create_default_environment.py` → `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_13_sort_environment_serial.py` → `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_12_environment_list_sort_limit.py` → `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 第二轮 Windows P0 全量托管运行：`python run.py --config config/config.yaml --level P0`，结果 `total=59 passed=57 failed=1 errors=1 skipped=0 flaky=0`。
- [x] 第二轮全量剩余问题：
  - `test_01_create_custom_proxy.py` 代理检测失败，按用户要求忽略。
  - `test_02_create_default_environment.py` 在全量顺序中仍出现一次 `overlay did not close`，但单跑已通过；结合截图状态为环境列表加载态、本机网络波动和用户确认，本轮不继续重跑全量。

## 2026-06-17 M10 提交前整理

- [x] 审阅待提交范围：当前变更由跨平台适配层、远程执行器、UI 远程节点、远程产物拉取、Windows 稳定性修复和相关文档组成。
- [x] 检查未跟踪文件：新增项均为预期代码或文档，包括 `core/app_config.py`、`core/platform/`、`core/remote_runner.py`、`core/system_proxy.py`、`core/local_http.py`、`config/remote_hosts.example.yaml`、Mac/Linux 记录文档和跨平台计划/运行文档。
- [x] 检查敏感信息：未发现远程 SSH 密码写入仓库；`config/remote_hosts.yaml`、`remote_artifacts/`、`logs/`、`screenshots/`、`reports/` 仍由 `.gitignore` 排除。
- [x] 规范可读性：
  - `streamlit_runner.py` 抽出 `_remote_host_by_name()`，避免远程执行和健康检查重复遍历 host 配置。
  - `ui/pages/01_执行用例.py` 拆分过长 import。
  - `pages/environment_page.py` 只在 `_wait_for_overlay_closed()` 保留复杂可见性判断；无关 checkbox/poper 可见性判断恢复为简单尺寸判断，减少误读。
- [x] README 当前用例说明同步：全局设置访问限制用例的允许网址已使用本地 HTTP 探针，不再依赖 b 站作为允许访问验证目标。

## 2026-06-17 PR 合并前补充审阅

- [x] 已确认 `codex/cross-platform-foundation` 分支与远端同步，工作区无未提交内容。
- [x] PR 前轻量验证通过：`python -m compileall -q core pages tests streamlit_runner.py ui`。
- [x] PR 前空白检查通过：`git diff --check origin/main...HEAD`。
- [x] 敏感信息扫描未发现远程 Linux/Mac SSH 密码进入仓库；`config/remote_hosts.yaml`、`remote_artifacts/`、`logs/`、`screenshots/`、`reports/` 继续由 `.gitignore` 排除。
- [x] 审阅远程日志脱敏实现时发现 JSON 形态的 `"password": "..."`、`"token": "..."` 未被专门覆盖；已补充 `core/remote_runner.py` 脱敏规则，保证文档描述与实际行为一致。

## 2026-06-17 合并后主分支冒烟与环境分组删除稳定性修复

- [x] PR #1 已合并到 `main` 后，本地 `main` 快进到 `d3662f4 Merge pull request #1 from MCDL14/codex/cross-platform-foundation`。
- [x] 合并后静态编译通过：`python -m compileall -q core pages tests streamlit_runner.py ui`。
- [x] 合并后预检通过：`python run.py --config config/config.yaml --precheck`，Python、APP 路径、启动参数、测试数据、CDP 端口、飞书 webhook 均为 PASS。
- [x] 首轮环境分组模块冒烟：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app`，发现 `test_01`、`test_02`、`test_03` 均在删除环境分组的二次确认弹窗处失败，异常为 `secondary dialog confirm button was not found: ('确定', '确认')`。
- [x] 定位原因：环境分组列表行内操作为无文本图标按钮，删除按钮为 `.icon-delete`；分组名单元格文本包含 `ID`，旧行匹配对单元格文本等值判断不够稳；二次确认按钮等待仅 1000ms，对 Element Plus message box 动画和刷新状态容错不足。
- [x] 修复 `pages/environment_group_page.py`：
  - 环境分组行匹配支持“分组名 + ID”单元格，按清理空白后的 `startsWith/includes` 兼容。
  - 删除操作优先定位 `.icon-delete` 所在按钮，避免无文本图标按钮按文本匹配失败。
  - 二次确认弹窗按钮等待提升到 `page_seconds`，并按清理空白后的文本等值或包含关系匹配。
- [x] 修复后单用例验证通过：
  - `python run.py --config config/config.yaml --module test_01_create_environment_group.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_02_group_containing_environment.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_03_group_authorized_member.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 修复后环境分组模块回归通过：`python run.py --config config/config.yaml --module environment_group_management --attach-existing-app` -> `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] 已确认失败期间残留的 `自动化-创建环境分组`、`自动化-包含环境的分组`、`自动化-授权成员的分组` 已清理，不再出现在当前环境分组列表。

## 2026-06-17 远程执行 UI 体验增强与能力矩阵

- [x] 清理已合并本地分支：`codex/cross-platform-foundation`、`codex/environment-group-delete-stability` 均已确认进入 `origin/main` 后删除本地分支。
- [x] 远端分支删除尝试遇到 GitHub 连接超时/重置，已保留为收尾重试项，不影响当前新分支开发。
- [x] 收尾阶段重试远端分支删除成功：`origin/codex/cross-platform-foundation`、`origin/codex/environment-group-delete-stability` 均已删除，并执行 `git fetch --prune origin` 清理远端引用。
- [x] 新建分支 `codex/remote-execution-polish`，从最新 `main` 开始做远程执行 UI 增强。
- [x] `streamlit_runner.discover_remote_hosts()` 增加非敏感节点字段：`port`、`project_dir`、`python`、`venv_activate`、`command_prefix`、`auth`；认证只显示 `SSH key`、`SSH agent/key` 或 `password_env:<环境变量名>`。
- [x] 新增 `streamlit_runner.preview_remote_command()`，供 UI 展示远程命令预览，不连接 SSH、不读取真实密码。
- [x] 新增 `streamlit_runner.remote_capability_matrix()`，统一给 UI 展示 Windows/Linux/macOS 远程节点能力边界。
- [x] 执行页远程节点模式增强：
  - 远程节点列表为空时给出 `config/remote_hosts.example.yaml` 到 `config/remote_hosts.yaml` 的配置提示。
  - 侧边栏展示远程节点状态：节点、平台、SSH、项目目录、配置、Python、虚拟环境和认证来源。
  - 展示远程命令预览，便于启动前确认 `project_dir`、`venv_activate`、`config` 和 `--attach-existing-app`。
  - 主页面展示远程节点能力矩阵。
  - 运行结束后展示“远程执行摘要”，包含 `[PASS]` / `[FAIL]` 数量、退出码、耗时、健康检查失败项、产物归档目录和 `[FAIL]` 明细。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已补充远程节点状态、命令预览、远程执行摘要和能力矩阵说明。
- [x] 当前静态验证通过：`python -m compileall -q streamlit_runner.py ui/pages/01_执行用例.py`。
- [x] 后端接口冒烟通过：`discover_remote_hosts()` 返回 `macos-arm64,linux-ubuntu`，`remote_capability_matrix()` 返回 3 个平台，`preview_remote_command()` 可生成远程预检命令。
- [x] UI 页面冒烟通过：临时启动 `streamlit run ui/app.py --server.port 8502 --server.headless true`，浏览器打开执行页后切换“远程节点”，确认“远程节点状态”“远程命令预览”“远程节点能力矩阵”均已渲染，浏览器控制台无 error/warning，验证后关闭服务并确认 `PORT_8502_RELEASED=True`。

## 2026-06-17 Windows/Linux 主链路回归

- [x] Windows 本地主链路预检通过：`python run.py --config config/config.yaml --precheck`，Python、APP 路径、启动参数、测试数据、CDP 端口、飞书 webhook 均为 PASS。
- [x] Windows 环境分组托管回归通过：`python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`。
- [x] Windows 成员管理托管回归通过：`python run.py --config config/config.yaml --module member_management`，结果 `total=15 passed=15 failed=0 errors=0 skipped=0 flaky=0`。
- [x] Windows 全局设置托管回归通过：`python run.py --config config/config.yaml --module global_settings`，结果 `total=12 passed=12 failed=0 errors=0 skipped=0 flaky=0`。
- [x] Linux 远程认证环境未持久化到仓库；首次健康检查因本地进程未注入认证材料返回 `No authentication methods available`，随后使用一次性进程环境变量重跑健康检查通过，结果 `[PASS] project_dir/run.py/config/venv/python/dependencies/config load/APP path`，`失败=0`。
- [x] Linux 环境分组远程托管回归通过：`linux-ubuntu` 执行 `python run.py --config config/config.yaml --module environment_group_management`，结果 `total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`，产物目录 `remote_artifacts/linux-ubuntu/20260617_150131`。
- [x] Linux 成员管理远程托管回归通过：`linux-ubuntu` 执行 `python run.py --config config/config.yaml --module member_management`，结果 `total=15 passed=15 failed=0 errors=0 skipped=0 flaky=0`，产物目录 `remote_artifacts/linux-ubuntu/20260617_150849`。
- [x] 发现 Linux 远端 `/home/dic/dicloak_automation_linux` 是软链接到旧快照 `/home/dic/dicloak_automation_linux_20260615_193149`，且远端未安装 `git`；旧快照缺少本地当前 `wait_environment_serials_sorted()` 的置顶行兼容，导致全局设置排序用例误判。
- [x] 低风险同步当前代码到 Linux 新快照 `/home/dic/dicloak_automation_linux_20260617_153704`：保留旧快照，复用旧快照 `.venv` 和 `config/config.yaml`，切换 `/home/dic/dicloak_automation_linux` 软链接；远端 `python -m py_compile pages/environment_page.py` 通过。
- [x] 修复 `pages/environment_page.py`：`dismiss_blocking_overlays()` 增加 Crisp 在线客服浮层隐藏兜底，并在设置分页条数前调用，解决 Linux 下右下角客服浮层拦截分页尺寸点击的问题。
- [x] 同步后 Linux 健康检查再次通过：`远程健康检查完成 → 失败=0`。
- [x] 同步后 Linux 全局设置模块回归：`python run.py --config config/config.yaml --module global_settings`，模块级结果 `total=12 passed=8 failed=2 errors=1 skipped=1 flaky=0`。其中：
  - `test_07_disable_packet_capture_software.py` 按 Windows 专用抓包工具能力跳过，符合预期。
  - `test_11_environment_list_pagination_setting.py` 和 `test_12_environment_list_sort_limit.py` 已通过，确认当前代码修复浮层拦截和置顶行排序问题。
  - `test_08_bookmark_setting_overwrite.py`、`test_09_bookmark_setting_append.py` 首轮失败原因是 `git archive` 未包含 `.gitignore` 排除的书签 HTML 夹具；补传 `test_data/bookmarks/*.html` 后单跑均通过：`test_08`、`test_09` 均为 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `test_03_disable_extension_management.py` 仍失败：访问 Chrome Web Store 后内核 CDP 等待页面响应超时，异常 `kernel CDP command response timeout`，截图显示环境列表加载遮罩停留；当前判定为 Linux 未注入 APP/内核代理时的外部 Web Store 页面加载限制。
- [x] Windows 变更后聚焦验证：首次 `test_11_environment_list_pagination_setting.py` 未进入业务步骤即在登录/切换团队阶段失败，重试托管启动后通过，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，确认 Crisp 浮层处理未破坏 Windows 分页设置用例。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已更新 Linux 已验证范围和 Web Store 安装检查限制。

## 2026-06-17 远端代码快照同步方案

- [x] 针对“远端长期不更新导致误跑旧代码快照”问题，新增 `core/remote_sync.py`：本地生成当前工作区快照、写入 `.remote_manifest.json`、通过 SFTP 上传远端，并发布到 `<project_dir>_releases/<timestamp>_<hash>`。
- [x] 同步规则按低风险设计：排除本机真实运行配置和产物目录，包含用例必需但可能被 `.gitignore` 排除的夹具文件；远端已有 `config/*.yaml` 和 `.venv` 会从旧项目目录保留到新快照。
- [x] 兼容 Linux/macOS：远端切换项目目录使用 POSIX 通用 `mv`、`ln -sfn`、Python `pathlib`，避免依赖 Linux 专用 `mv -T` 或 `readlink -f`；macOS 节点可使用同一发布流程。
- [x] `config/remote_hosts.example.yaml` 新增 `sync_enabled`、`sync_release_root`、`sync_keep_releases`，可按节点控制是否启用同步、发布根目录和保留快照数量；新增 `config/remote_sync.example.yaml`，真实覆盖文件 `config/remote_sync.yaml` 已加入 `.gitignore`。
- [x] UI 远程节点模式新增“检查远端代码”“同步当前代码”和“远程执行前同步当前代码”：默认不自动同步，先让用户确认差异，需要时再同步或勾选执行前同步。
- [x] `streamlit_runner.discover_remote_hosts()` 已暴露非敏感同步状态，`remote_capability_matrix()` 已同步 Linux/macOS 能力边界；远程运行入口 `run_remote_cli()` 支持 `sync_before_run`。
- [x] 本地冒烟：当前工作区 manifest 可生成，真实本机配置未进入快照，书签 HTML 夹具已进入快照；Linux/macOS 默认发布目录分别解析为 `/home/dic/dicloak_automation_linux_releases` 和 `/Users/tianji/dicloak_automation_mac_releases`。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已补充远端快照同步的入口、规则、限制和 macOS 兼容说明。
- [x] 静态验证通过：`python -m compileall -q core pages tests streamlit_runner.py ui`、`git diff --check`。
- [x] 后端冒烟通过：manifest 文件数 `156`，本机真实配置 `config/config.yaml`、`config/remote_hosts.yaml`、`config/remote_sync.yaml` 均未进入快照，`config/remote_sync.example.yaml` 和书签 HTML 夹具已进入快照；`sync_enabled: "false"` 字符串可正确解析为关闭。
- [x] macOS 兼容脚本冒烟通过：远端发布脚本不包含 `mv -T` 或 `readlink -f`，并已调整为在新 release 目录编译检查通过后才切换 `project_dir` 软链接。
- [x] UI 冒烟通过：临时启动 `streamlit run ui/app.py --server.port 8502 --server.headless true`，切换“远程节点”后确认“检查远端代码”“同步当前代码”“远程执行前同步当前代码”、发布目录和能力矩阵均可渲染，浏览器控制台无 error/warning，验证后关闭服务并确认 `PORT_8502_RELEASED=True`。
- [x] 只读远端代码检查：Linux 可连接，当前活动目录 `/home/dic/dicloak_automation_linux_20260617_153704` 缺少 `.remote_manifest.json`，状态为 `unknown`，符合旧手工快照特征；macOS 当前凭据认证失败，未执行同步、未修改远端。

## 2026-06-17 远程连接信息 UI 填写与缓存

- [x] `core.remote_runner.RemoteHost` 新增运行期 `password` 字段；SSH 连接优先使用 UI 临时密码，未填写时继续回退到 `password_env`、`key_filename` 或 SSH agent/key。
- [x] `streamlit_runner.py` 新增 `config/remote_connection_cache.yaml` 读写：只缓存 SSH IP、端口、用户名和更新时间，不保存密码；该文件已加入 `.gitignore`。
- [x] 远程健康检查、远端代码检查、远端代码同步、远程执行均支持 UI 覆写 SSH IP、端口、用户名和本次会话密码。
- [x] 执行页远程节点模式新增“远程连接信息”：可填写 SSH IP/主机、端口、用户名和“SSH 密码（本次 UI 会话）”；支持手动保存连接缓存，也会在远程操作前自动保存非敏感连接信息。
- [x] 远程节点状态表区分“配置默认 SSH”和“当前 UI 连接”，并显示临时密码是否已填写、连接缓存是否启用。
- [x] 远端代码快照同步排除 `config/remote_connection_cache.yaml`，避免把本机内网连接缓存同步到 Linux/macOS 节点。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已补充 UI 填写连接信息、本机缓存和密码不落盘规则。

## 2026-06-17 执行用例页交互整理

- [x] 执行页左侧“筛选模块”改为“显示设置”：`显示模块（不影响执行）` 和 `搜索显示` 只控制列表可见性，不取消勾选，也不改变本机执行范围。
- [x] 本机执行的选中集改为直接来自全部 `st.session_state` 勾选状态，避免隐藏模块后误把隐藏用例排除在本机执行范围外。
- [x] 模块标题行外置已选数量和“选中模块 / 取消模块”按钮，不展开模块也能完成模块级全选或取消；折叠面板内继续保留逐条用例 checkbox，保持原有勾选持久化逻辑。
- [x] 远程节点模式从侧边栏长控件流调整到主页面“远程节点执行”区域，并按选择节点、SSH 连接信息、远端项目/配置状态、代码同步动作、运行范围、执行选项和命令预览分组。
- [x] 远程节点能力矩阵默认折叠，避免把主要远程操作和用例列表往下挤。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已更新执行页显示筛选语义、模块级选择入口和远程节点分区布局。
- [x] 静态验证通过：`python -m compileall -q core pages tests streamlit_runner.py ui`、`git diff --check`。
- [x] UI 冒烟通过：复用本机 `http://localhost:8502/执行用例`（PID `11532`），刷新执行页后确认本机模式显示“显示模块（不影响执行）”“搜索显示”“选中模块”“取消模块”和本机执行提示；切换“远程节点”后确认“远程节点执行”下的选择节点、SSH 连接信息、远端项目/配置状态、代码同步动作、运行范围、执行选项、命令预览均可见；浏览器控制台无 error/warning。
- [x] 代码审阅修复：`run_remote_cli()` 已补传 UI SSH 覆写参数，确保远程执行、执行前同步和产物拉取使用当前 UI 连接信息；远端发布脚本复制旧 `config/*.yaml` 时跳过 `remote_connection_cache.yaml`；`README.md` 已把远程连接信息位置从旧侧边栏描述改为执行页“远程节点执行”区域。
- [x] macOS 远端验证认证阻塞已解除：按用户提供的本次会话 SSH 密码临时注入 Streamlit 进程，不写 YAML、不写连接缓存、不进日志。
- [x] Mac UI 同步当前代码首轮发现 zsh 环境下裸 `python` 不存在，远端发布脚本在复用旧 `.venv` 时解析真实路径失败；已修复 `core/remote_sync.py`，优先使用旧 `.venv/bin/python` 解析 `.venv` 真实路径，避免依赖远端 shell 的 `python` 命令。
- [x] Mac UI “同步当前代码”完成：节点 `macos-arm64`，状态 `synced`，文件数 `156`，发布目录 `/Users/tianji/dicloak_automation_mac_releases/20260617_192528_8410e13f343c8fa8`，活动目录切换为该 release 快照，复用旧 `.venv`。
- [x] Mac UI 远程 P0 全量执行完成：`python run.py --config config/config.macos.yaml --level P0`，结果 `total=59 passed=57 failed=0 errors=1 skipped=1 flaky=0`，退出码 `1`，耗时 `1763.94s`，产物归档 `remote_artifacts/macos-arm64/20260617_200227`，拉取文件数 `3`。
- [x] Mac P0 唯一错误为 `tests.p0.proxy_management.test_01_create_custom_proxy.TestCreateCustomProxy.test_create_custom_proxy_detect_and_delete`，异常位置 `pages/proxy_page.py:131`：`create proxy dialog did not close after confirm`。该问题更像代理业务/远端 APP 状态或测试数据问题，本轮未用 UI/同步层改动掩盖。
- [x] 远程执行结果展示修复：执行页“远程执行摘要”优先使用最终测试统计，避免只统计远程健康检查 `[PASS]`/`[FAIL]` 行导致 P0 执行后显示 `0/0`；“失败/错误详情”改为提取用例失败块和 unittest 错误块，减少 Chromium stderr 噪声。
- [x] Mac 远端环境噪声记录：远端 APP 日志出现 `/Applications/DICloak.app/Contents/Resources/gost` 缺失/启动超时，以及执行结束后的 Electron `terminatePython` 空引用报错；当前不影响 SSH 同步和用例执行链路判断，后续按 APP/环境问题单独收敛。
- [x] 收尾验证：`python -m compileall -q core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有 LF/CRLF 换行提示；浏览器刷新 `http://localhost:8502/执行用例` 后确认本机模式和远程节点模式均可渲染，远程节点模式下选择节点、SSH 连接信息、远端项目/配置状态、代码同步动作、运行范围、执行选项、命令预览均可见，页面无 ImportError/Traceback，浏览器控制台无 error/warning；验证后停止临时 Streamlit 进程并确认 `PORT_8502_RELEASED=True`，避免临时认证材料继续留在进程环境里。
- [x] 代码审阅补充修复：`core/remote_sync.py` 将真实配置、连接缓存、日志、截图、报告和 `remote_artifacts` 拆为不可覆盖的保护排除项，即使本机 `config/remote_sync.yaml` 自定义规则漏写这些路径也不会进入远端快照；远端同步 stdout/stderr 和异常 stderr 复用远程日志脱敏规则，避免 `password`、`token` 等敏感字段进入 UI 日志。
- [x] 新增 `tests/p1/test_remote_sync.py` 覆盖远端同步安全边界：自定义同步规则不能包含受保护运行文件，远端同步日志和错误消息会脱敏；验证通过 `python -m unittest tests.p1.test_remote_sync`，结果 `Ran 2 tests ... OK`。
- [x] 修复后验证：`python -m compileall -q core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有 LF/CRLF 换行提示；默认 manifest 再次确认 `config/config.yaml`、`config/test_data.yaml`、`config/remote_hosts.yaml`、`config/remote_sync.yaml`、`config/remote_connection_cache.yaml` 均未进入快照。
- [x] 针对 UI 中误把“远程预检”当成全量执行的问题，执行页远程按钮改为按运行范围动态显示：预检为“执行远程预检”，P0 为“远程执行 P0 全量”，业务模块/目录模块/单用例也显示具体范围；远程预检旁增加提示“只检查环境，不运行任何用例”。

## 2026-06-18 CDP 连接超时与 raw websocket 兜底修复

- [x] 定位本机 Windows CDP 异常：`http://127.0.0.1:9222/json/version` 和 `/json/list` 均可访问，DICloak 页面 target 存在，但最小 Playwright `connect_over_cdp("http://127.0.0.1:9222")` 在 `ws connected` 后超时，说明当前 Electron CDP 会话 degraded，并非单纯端口未开放。
- [x] 定位框架误导点：`fallback_driver: websocket` 只建立 raw CDP socket，不能提供 Playwright `page`，后续页面对象必然报 `Playwright page is not connected`，不应作为页面自动化兜底成功返回。
- [x] 修复 `core/cdp_driver.py`：`connect_over_cdp()` 传入配置剩余超时时间，避免 Playwright 默认 180 秒卡住；raw websocket fallback 改为明确失败并提示重启 APP/CDP 或修复 Playwright endpoint；异常路径统一关闭已打开的 browser/playwright/websocket。
- [x] 调整默认配置和示例：`core/config.py`、`config/config.example.yaml` 中 `cdp.fallback_driver` 默认置空，避免新配置继续启用不可用的 raw websocket 页面自动化兜底。
- [x] 新增 `tests/p1/test_cdp_driver.py`，覆盖 Playwright attach timeout 传递、raw websocket fallback 不再被视为成功、默认配置关闭 raw websocket fallback。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已补充 CDP HTTP 可访问但 Playwright attach 失败的排查说明，以及 raw websocket 不作为页面对象自动化兜底的边界。
- [x] 验证通过：`python -m unittest tests.p1.test_cdp_driver tests.p1.test_remote_sync` -> `Ran 5 tests ... OK`；`python -m compileall -q core pages tests streamlit_runner.py ui` -> 通过；`git diff --check` -> 通过，仅有工作区 LF/CRLF 提示。
- [x] 本机 degraded CDP 探针验证：临时将 `connect_timeout` 覆盖为 `3` 秒后调用 `CDPDriver.connect()`，约 `3.13s` 返回 `CDPConnectionError`，错误信息为 raw websocket fallback 已禁用；确认不再等待 180 秒，也不再输出 raw websocket 假成功。

## 2026-06-18 UI 执行状态与残留锁处理

- [x] 定位用户终止上一轮本机执行后再次点击执行提示“已有 UI 执行任务正在运行”：当前没有独立 `python run.py` 进程，但旧 Streamlit 进程内后台线程仍可能持有 `_RUN_LOCK`，导致新任务被挡住。
- [x] 修复 `streamlit_runner.py`：增加 UI 任务状态登记，记录任务类型、开始时间和后台线程标识；统一本机执行、远程执行、远程代码检查/同步、远程节点检查的锁获取与释放逻辑；忙碌提示会附带当前任务信息。
- [x] 修复执行页：侧边栏新增“执行状态”，空闲时显示“空闲，可以启动执行”；后台线程真实运行时显示任务和开始时间；检测到无活动线程的残留锁时提供“解除残留执行锁”按钮，避免用户再次点击执行只看到日志提示。
- [x] 重启 Streamlit 清理旧进程内锁：旧 PID `22488` 已停止，新 PID `12364` 已启动，`http://localhost:8502/执行用例` 返回 HTTP 200。
- [x] UI 冒烟通过：浏览器打开执行页后侧边栏显示“执行状态 -> 空闲，可以启动执行”，运行按钮恢复为“运行选中（59 条）”，浏览器控制台无 error/warning。
- [x] 验证通过：`python -m compileall -q streamlit_runner.py ui/pages/01_执行用例.py`、`python -m compileall -q core pages tests streamlit_runner.py ui`、`python -m unittest tests.p1.test_cdp_driver tests.p1.test_remote_sync`、`git diff --check` 均通过；`git diff --check` 仅有工作区 LF/CRLF 提示。

## 2026-06-18 成员停用退登提示窗兼容

- [x] 按实际 APP 交互探测新退登提示：内部成员被 API 停用后自动回到登录页，URL 形如 `#/sso/login?errorCode=300003&msg=...`，页面弹出 Element Plus message box，标题“操作提示”，正文“该成员已被禁用。请联系您的团队管理员。”，按钮文案“确定”；登录页输入框仍保留被停用成员账号和密码。
- [x] 修复 `pages/login_page.py`：新增 `dismiss_login_notice_if_present()`，在 `login()` 和 `click_login_button()` 点击“立即登录”前自动确认登录页退登原因提示窗；只在登录页且弹窗文本包含“操作提示/退出登录/禁用/停用/团队管理员/到期”等原因关键词时点击“确定/确认/我知道了”。
- [x] 修复 `click_force_logout_button()`：改为在页面内直接点击强制退登弹窗按钮并重试，避免 Element Plus 弹窗动画或 DOM 替换导致 Playwright element handle `not attached`。
- [x] 增强 `tests/p0/member_management/member_open_api.py`：OpenAPI 编辑成员请求遇到 `requests.RequestException`（如连接重置）时按既有 `status_retry` 策略重试，避免临时网络抖动导致到期停用用例恢复阶段失败。
- [x] 临时探针 `_tmp_probe_member_logout_prompt.py` 已删除；探测过程中和并行误跑后均已通过 OpenAPI 恢复外部/内部成员 `status=ENABLED`、`disuse_enable=false`。
- [x] 串行验证通过：
  - `python run.py --config config/config.yaml --module test_12_api_disable_external_member.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_13_api_disuse_external_member.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_14_api_disable_internal_member.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。
  - `python run.py --config config/config.yaml --module test_15_api_disuse_internal_member.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`。

## 2026-06-18 远程节点执行 UI 精简

- [x] 审阅远程节点执行区后确认：项目目录、配置文件、Python、虚拟环境、发布目录、能力矩阵、命令预览和手动检查/同步按钮主要面向维护者/排障，不应默认占据执行者主路径。
- [x] 调整 `ui/pages/01_执行用例.py`：远程节点下拉只显示节点名和平台；主路径保留选择节点、连接摘要、SSH 密码、运行范围和执行选项。
- [x] 将 SSH IP、端口、用户名和连接缓存移动到“修改 SSH 连接”折叠区；默认使用节点配置或本机连接缓存，密码仍只作为本次 UI 会话临时值。
- [x] 将“检查远程节点”“检查远端代码”“同步当前代码”移动到“检查和同步”折叠区；保留“执行前同步当前代码”作为执行选项。
- [x] 将远程命令预览、节点配置详情和 Windows/Linux/macOS 能力矩阵移动到“高级信息”折叠区，降低日常执行认知负担。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已更新远程节点执行主路径和折叠信息位置。
- [x] 验证通过：`python -m compileall -q core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有工作区 LF/CRLF 提示；浏览器打开 `http://localhost:8502/执行用例` 并切换“远程节点”后确认默认视图只展示选择节点、连接信息、SSH 密码、运行范围和执行选项，展开“检查和同步”“高级信息”后确认检查、同步、命令预览、节点配置和能力矩阵仍可见，浏览器控制台无 error/warning。

## 2026-06-18 UI 执行状态动态刷新修复

- [x] 定位远程任务运行时侧边栏“执行状态”仍显示“空闲”的原因：侧边栏状态在点击运行前渲染一次，后台线程启动后只刷新主区域日志，没有刷新侧边栏状态块。
- [x] 修复 `ui/pages/01_执行用例.py`：把侧边栏执行状态改为占位块渲染，并在后台线程启动、日志轮询收到新日志、轮询空闲等待和任务结束后刷新；本机执行、远程执行、远程节点检查、远端代码检查和同步共用同一刷新逻辑。
- [x] 文档同步：`UI使用文档.md` 已补充执行状态会随日志轮询刷新。
- [x] 验证通过：`python -m compileall -q core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有工作区 LF/CRLF 提示；浏览器刷新 `http://localhost:8502/执行用例` 后确认侧边栏“执行状态”和“空闲，可以启动执行”正常渲染，切换“远程节点”后远程执行区正常渲染，页面无 ImportError/Traceback，浏览器控制台无 error/warning。

## 2026-06-18 远程运行类型按勾选用例执行

- [x] 采纳执行者视角反馈：原“运行范围”下拉同时承载“远程预检”和“跑哪些用例”，容易和下方模块/用例勾选混淆；远程选择“执行用例”时应直接按下方已勾选用例执行。
- [x] 调整 `ui/pages/01_执行用例.py`：将远程“运行范围”改为“运行类型”下拉，选项只保留“远程预检”和“执行用例”；选择“执行用例”后不再出现第二个范围选择框，而是显示将远程执行下方已勾选的用例数量。
- [x] 扩展 CLI 和远程命令链路：`run.py --case` 支持重复传参；`AutomationRunner` 支持多个 case 过滤；`core.remote_runner.RemoteRunRequest` 支持 `scope="cases"` 并生成多个 `--case <test_id>`；`streamlit_runner.run_remote_cli()` 将 UI 勾选的 `selected_ids` 传给远端命令。
- [x] 新增 `tests/p1/test_remote_selected_cases.py`，覆盖重复 `--case` 参数解析、runner 多 case 过滤和远程多 `--case` 命令构造。
- [x] 文档同步：`README.md`、`UI使用文档.md`、`跨平台运行与开发文档.md` 已更新为“远程预检/执行用例”两种运行类型，并说明“执行用例”按下方已勾选 test id 执行。
- [x] 验证通过：`python -m unittest tests.p1.test_remote_selected_cases tests.p1.test_cdp_driver tests.p1.test_remote_sync` -> `Ran 8 tests ... OK`；`python -m compileall -q run.py core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有工作区 LF/CRLF 提示。
- [x] UI 验证通过：浏览器刷新 `http://localhost:8502/执行用例` 并切换远程节点后，默认“远程预检”不显示勾选数量；切换“执行用例”后显示“将远程执行下方已勾选 N 条用例”，按钮显示“远程执行选中（N 条）”，不再出现“用例范围”或“远程执行 P0 全量”；页面无 ImportError/Traceback，浏览器控制台无 error/warning。
- [x] UI 继续精简：模块标题行去掉“模块用例 xx 条”说明，只保留模块名和已选数量；运行类型为“远程预检”时不展示主用例列表，改为提示“远程预检只检查远端环境，不运行用例”。
- [x] 验证通过：`python -m unittest tests.p1.test_remote_selected_cases tests.p1.test_cdp_driver tests.p1.test_remote_sync` -> `Ran 8 tests ... OK`；`python -m compileall -q run.py core pages tests streamlit_runner.py ui` 通过；`git diff --check` 通过，仅有工作区 LF/CRLF 提示；浏览器切换远程节点后确认远程预检显示“不展示用例列表”提示且不显示“查看用例/模块用例”，切换“执行用例”后用例列表恢复、显示已选数量且不再出现“模块用例 xx 条”，页面无 ImportError/Traceback，浏览器控制台无 error/warning。
- [x] 修复热更新兼容问题：执行页新增 `case_ids` 签名检查，若 Streamlit 后端未加载新 `streamlit_runner.py`，会提示重启 UI 并禁用按勾选用例远程执行，避免高级信息中直接暴露 `preview_remote_command() got an unexpected keyword argument 'case_ids'`；已重启 8502 Streamlit，新 PID `7336`，浏览器验证高级信息命令预览正常显示多个 `--case`，无预览失败、ImportError/Traceback 或控制台 error/warning。
- [x] 微调用例列表模块行对齐：模块标题和“已选 x/y”同用 markdown 渲染，并将模块行 columns 设置为 `vertical_alignment="center"`，避免“已选”看起来掉到下一行。

## 2026-06-23 代理管理批量创建用例

- [x] 新增 `tests/p0/proxy_management/test_02_batch_create_proxy.py`：覆盖代理管理页“批量创建”组合流程，包括单条代理预览字段校验、Windows 系统代理 `127.0.0.1:7897` 启停并检测出口 IP、错误多行数据提示“第3行格式有误”、有效多行数据确认弹窗成功/重复数量校验、列表创建结果校验，以及勾选本次创建的 3 条代理后通过顶部批量操作栏“删除”完成批量删除并再次校验删除成功。
- [x] 扩展 `pages/proxy_page.py`：新增批量创建页进入、textarea 填充和失焦、预览表结构化读取、格式错误显式等待、批量检测等待、结果弹窗成功/重复数量解析、确认后返回列表、批量页恢复、代理列表清空选择、按 ID 勾选代理行、顶部批量操作栏删除和删除后等待缺失能力；同时收窄代理列表页判断，避免停在批量创建页时被误判为列表页。
- [x] 用例按用户要求采用软断言：预览字段、出口 IP、格式错误提示、成功/重复数量等断言失败时先记录 warning，继续执行后续业务步骤和 finally 清理，最后统一失败。
- [x] 静态验证通过：`python -m compileall -q pages tests\p0\proxy_management`。
- [x] 空白检查通过：`git diff --check` 无空白错误，仅有当前 Windows 工作区 LF/CRLF 换行提示。
- [x] 按用户修正第 13 步有效批量数据：将 `127.0.0.1:7890{}` 改为 `127.0.0.1:7897{}`；由于当前账号已有 `SOCKS5://127.0.0.1:7897`，该行按重复处理，不再等待它新增，改为校验重复项未产生新 ID。
- [x] 新增用例实跑通过：`python run.py --config config/config.yaml --module test_02_batch_create_proxy.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；运行确认系统代理快照恢复、格式错误提示等待、结果弹窗符合 `成功 3 个、重复 2 个`、三条新代理按 ID 勾选后通过顶部批量操作栏删除，且删除后再次校验均不存在。

## 2026-06-24 代理管理创建动态代理 NodeMaven 用例

- [x] 新增 `tests/p0/proxy_management/test_03_create_nodemaven_proxy.py`：覆盖代理管理页“创建代理”选择 `NodeMaven (动态代理)`、填写主机/端口/账号/密码、选择“国家/地区=美国”、填写备注、创建弹窗检测、创建后列表字段校验、行内第一个操作按钮重新检测、删除代理并校验删除成功。
- [x] 扩展 `pages/proxy_page.py`：创建代理弹窗的代理类型选择改为优先按表单 label `代理类型` 定位，兼容 `NodeMaven (动态代理)` 这类非纯协议选项；新增国家/地区下拉选择、备注填写、弹窗文本读取、按 ID 读取行文本能力，供动态代理检测详情和出口 IP 列断言复用。
- [x] 优化代理删除确认弹窗点击：`confirm_secondary_dialog()` 默认优先匹配“确定删除”，再降级到“确认删除/确定/确认”，避免单条删除弹窗已出现后先等待不匹配按钮导致延迟点击。
- [x] 用例按用户要求采用软断言：创建弹窗检测结果、弹窗国家/地区、行内检测结果和行内出口 IP 国家/地区断言失败时先记录 warning，继续执行后续保存、关闭系统代理和清理流程，最后统一失败。
- [x] 删除代理前显式关闭系统代理：Windows 平台在弹窗检测前开启配置中的系统代理 `127.0.0.1:7897`，贯穿到列表行内检测完成；在执行删除代理之前调用 `disable_system_proxy()` 关闭系统代理，finally 中保留原始快照恢复，避免执行机原有系统代理状态被用例永久改写。
- [x] `config/test_data.example.yaml` 新增 `test_data.proxy_nodemaven` 示例结构；账号和密码建议放本地 `config/test_data.yaml` 或通过 `DICLOAK_PROXY_NODEMAVEN_ACCOUNT`、`DICLOAK_PROXY_NODEMAVEN_PASSWORD` 注入，不写入仓库示例。
- [x] 临时探针确认真实 UI 边界：代理类型下拉项实际为 `NodeMaven (动态代理)`；选择后出现 `IP`、`国家/地区`、`州/省`、`城市` 字段，国家/地区下拉包含“美国”，备注字段仍使用 placeholder `备注`。
- [x] 静态验证通过：`python -m compileall -q pages tests\p0\proxy_management`。
- [x] 新增用例实跑通过：`python run.py --config config/config.yaml --module test_03_create_nodemaven_proxy.py --attach-existing-app`，结果 `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；日志确认创建弹窗检测返回 `连接测试成功`，详情包含 `国家/地区: United States(US)`；列表行内检测返回 `连接成功`，出口 IP 列包含 `US-United States`；日志顺序确认列表行内检测完成后先关闭系统代理，再按新代理 ID 删除并校验不存在。

## 2026-06-24 重试策略与删除确认优化

- [x] 调整 `core/runner.py` 用例级重试策略：仅当单次结果为 unittest `error` 且没有 `failure` 时才按 `run.retry_times` 重试；断言失败 `failure` 代表业务结果不符合预期，直接输出最终失败，不再重试。
- [x] 新增 `tests/p1/test_runner_retry_policy.py`：覆盖断言失败不重试、执行异常重试后通过计入 `flaky` 两个策略分支。
- [x] 代理删除确认弹窗延迟原因确认：此前默认候选顺序为“确认删除/确定/确认”，实际弹窗按钮为“确定删除”时，会先等待不匹配候选超时，再点到确认按钮；现已将“确定删除”放到默认候选首位。
- [x] 验证断言失败不重试：临时断言失败用例执行 `python run.py --config config/config.yaml --module tests/p1/test_tmp_assertion_no_retry.py --attach-existing-app`，结果 `total=1 passed=0 failed=1 errors=0 skipped=0 flaky=0`，日志无 `Retrying`，确认断言失败不会再触发第二次执行；验证后已删除临时用例文件。
- [x] 验证通过：`python -m unittest tests.p1.test_runner_retry_policy` -> `Ran 2 tests ... OK`；`python run.py --config config/config.yaml --module test_03_create_nodemaven_proxy.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`，日志中 `NodeMaven delete start` 与 `NodeMaven delete finished` 同秒完成，确认删除确认弹窗不再因候选文案顺序等待。

## 2026-06-24 代理检测等待诊断优化

- [x] 根据真实 APP 探测结果优化创建代理弹窗检测等待：点击“检测代理”后先确认检测已真实启动，启动信号包括检测按钮 disabled/loading、弹窗 loading、弹窗文案变化或最终检测结果已出现；随后再等待“连接测试成功/连接失败”，避免只靠最终文案等待时无法区分“未发起检测”和“检测中未返回”。
- [x] 扩展 `pages/proxy_page.py` 创建弹窗检测状态脚本，返回 `visible`、`result`、`has_loading`、`button_disabled`、`button_loading`、`button_text`、弹窗文本和行摘要；超时时压缩输出最后状态，便于定位 APP 未触发检测、按钮状态异常或结果文案未渲染。
- [x] 保持批量创建和列表行内检测正常路径不变，仅增强超时诊断：批量检测超时输出是否见过 activity/loading/button disabled、最后出口 IP 文案和预览行；行内检测超时输出当前行文本、检测单元格索引和最后检测结果文本。
- [x] 实跑前发现历史探针遗留 `NodeMaven://gate.nodemaven.com:8080` 自动化数据导致保存后按新 ID 查找失败，已按具体 ID 清理该探针遗留记录；该问题与本轮等待优化无关，属于测试账号数据残留造成的重复/保存阻断。
- [x] 验证通过：`python -m compileall -q core pages tests\p0\proxy_management tests\p1` 通过；`python -m unittest tests.p1.test_runner_retry_policy` -> `Ran 2 tests ... OK`；`python run.py --config config/config.yaml --module test_03_create_nodemaven_proxy.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；`python run.py --config config/config.yaml --module test_02_batch_create_proxy.py --attach-existing-app` -> `total=1 passed=1 failed=0 errors=0 skipped=0 flaky=0`；`git diff --check` 无空白错误，仅有当前 Windows 工作区 LF/CRLF 换行提示。
