# 全局设置固定基准与按影响字段恢复改造方案

> 状态：核心方案已于 2026-08-25 落地并完成 P1、全局设置模块及关联环境管理用例真实验证；后续继续优化长耗时链路
>
> 最后更新：2026-08-27
>
> 当前边界：本文同时保留设计依据与实现记录；自动恢复使用接口固定基准，UI 快照仅作为显式诊断能力保留

## 1. 最新方案结论

1. 用户提供的完整组织配置 POST 请求体作为固定基准；任何直接调用恢复 POST 的请求体都必须满足接口完整结构，不能裁剪成局部配置块。
2. 完整 POST 请求体和 GET 校验范围是两个独立概念。固定基准 GET 比较只检查 `browser_config`、`data_sync_config`、`bookmark_config`、`env_data_sync`、`proxy_detect_config`、`access_limit`、`local_data_config`、`env_page_config`、`env_sort_config`、`env_title_config` 共 10 个配置块；GET 响应中的其他顶层配置和基准未声明的嵌套字段一律忽略。
3. 每条涉及全局设置的用例在业务步骤开始前调用 GET，并与固定基准做语义比较；不一致时调用 POST 恢复固定基准，再调用 GET 复查。
4. 运行前 POST 后仍与固定基准不一致时，记录脱敏差异和警告后继续执行用例，不进行 UI 恢复。
5. 只有用例实际尝试修改全局设置后才需要进入用例结束恢复；仅打开或读取全局设置的用例不触发恢复。
6. 用例流程自身具备恢复能力时，以 GET 确认本用例影响字段已恢复为准；确认成功后，`finally` 不重复恢复。
7. `finally` 只检查和恢复当前用例明确声明的受影响配置字段，不检查其他配置，也不做整页 UI 快照校验；如果需要 POST，仍必须构造完整请求体，不能只发送受影响字段。
8. UI 点击“确定”前注册目标 POST 接口监听；点击后同时等待页面 loading 结束和接口响应。取消等待“保存成功”提示，该提示不再参与保存成功判定。
9. UI 保存成功后，以本次捕获的 POST 请求体作为期望值调用 GET 复查；内容比较同样只检查上述 10 个配置块。
10. UI 保存单次等待目标接口响应的超时时间为 20 秒，总尝试次数最多 3 次。
11. 直接调用组织配置 GET/POST 时，单次超时时间为 30 秒，总尝试次数最多 3 次，并校验 HTTP 状态码。
12. 保留现有 UI 快照采集、比较和显式恢复能力，但不再自动用于用例结束恢复。
13. 原有局部 UI 操作校验必须保留：勾选复选框、开启开关、选择下拉值或修改组合配置后，仍先通过页面状态确认目标控件已经变为预期状态；接口响应和 GET 复查是在此基础上追加的持久化校验，不能替代局部 UI 校验。

## 2. 当前实现与 APP 实际状态

### 2.1 现有 UI 快照能力

当前入口位于 `pages/global_settings_page.py`：

```python
capture_global_settings_snapshot()
restore_global_settings_snapshot(snapshot)
_global_settings_snapshot_matches(expected, actual)
_assert_global_settings_snapshot_matches(expected, actual)
```

当前 schema 1 采集 10 组状态：

1. `simple_checkboxes`
2. `website_restriction`
3. `packet_capture_blocking`
4. `bookmark_setting`
5. `environment_field_display_limit`
6. `environment_list_pagination`
7. `environment_list_sort`
8. `data_sync`
9. `clear_local_cache`
10. `extension_tamper_protection`

现有 `_strong_global_settings_snapshot()` 已有部分语义比较能力，例如网站限制、抓包软件限制、字段权限、分页和排序关闭时只比较关闭状态；本次应保留并补全这一原则。

### 2.2 2026-08-25 同步到代码的 UI 固定基准

已通过项目现有 CDP 通道进入全局设置页，只读取页面状态，没有点击确定、保存或修改任何设置。

```json
{
  "schema_version": 2,
  "simple_checkboxes": {
    "禁止查看网站密码": true,
    "禁止打开浏览器开发者工具": false,
    "禁止管理/移除扩展，以及从本地安装扩展至浏览器": false,
    "禁止成员访问谷歌扩展商店和扩展设置页面": false
  },
  "website_restriction": {
    "enabled": false
  },
  "packet_capture_blocking": {
    "enabled": false
  },
  "bookmark_setting": {
    "enabled": false,
    "restore_supported": true
  },
  "environment_field_display_limit": {
    "enabled": false
  },
  "environment_list_pagination": {
    "enabled": false
  },
  "environment_list_sort": {
    "enabled": false
  },
  "data_sync": {
    "cookie": true,
    "local_storage": true,
    "indexeddb": true,
    "one_way_enabled": false,
    "whitelist_groups": []
  },
  "clear_local_cache": {
    "clear_method": "不清除"
  },
  "extension_tamper_protection": {
    "enabled": false
  },
  "proxy_check_failure_block_open": {
    "enabled": false
  },
  "country_mismatch_block_open": {
    "enabled": false
  }
}
```

说明：

- 这是 UI 校验基准，不是接口 POST 请求体。最初只读采集时“禁止查看网站密码”为关闭；固定接口基准实际应用后，`browser_config.type=32769` 对应页面状态为开启，因此版本化 UI 基准已按实测结果更新为 `true`。
- 页面说明文字、书签卡片文本、关闭功能下的空选项等不稳定内容不进入强校验基准。
- “开启扩展加密并防止篡改”已确认由 `browser_config.type` 中的位控制，不使用 `cookie_encrypt_config`。`cookie_encrypt_config.status == true` 不能推断该 UI 选项已开启；具体位值必须通过单项切换前后的 POST/GET `type` 差异确认，禁止根据当前整数猜测。

### 2.3 UI 快照能力需要更新

`capture_global_settings_snapshot()` 升级为 schema 2，并新增：

```python
"proxy_check_failure_block_open": {
    "enabled": proxy_check_failure_block_open_enabled(),
},
"country_mismatch_block_open": {
    "enabled": country_mismatch_block_open_enabled(),
},
```

同时更新：

- `_strong_global_settings_snapshot()`：对两个新增布尔开关做精确比较。
- `restore_global_settings_snapshot()`：保留显式 UI 恢复时对两个新增字段的恢复能力。
- UI 快照 P1：覆盖 schema 1 兼容、schema 2 采集、比较和显式恢复。
- 新的自动接口恢复流程不调用 UI 快照采集、断言或恢复；上述能力仅作为显式诊断和独立工具保留。

## 3. 两套基准的职责

### 3.1 API 固定恢复基准

用途：作为运行前环境校正和用例结束按影响字段恢复的统一目标。

特征：

- 固定版本、受代码审查；
- 内容就是用户提供的 POST 请求体；
- 不从运行前 GET 动态生成；
- 不因用例开始时的团队状态变化而改变；
- 不包含 token、完整请求头或登录态数据。
- 基准文件保留接口要求的完整 POST 结构；完整结构不等于所有字段都必须参与 GET 校验。
- 运行前和恢复后的 GET 语义比较只遍历已经确认的 10 个配置块；GET 响应中的其他配置不参与比较。
- 运行前发现基准差异时，直接发送完整固定基准请求体。
- 用例结束按影响字段恢复时，先把当前 GET 转换成完整可写 POST 结构，再只把本用例声明的字段替换为固定基准值；GET 缺失但 POST 必需的字段由完整固定基准补齐，最终发送的仍是完整请求体。

建议保存为：

```text
test_data/global_settings/org_config_baseline.json
```

### 3.2 UI 固定校验基准

用途：保留当前 APP 页面状态的固定参考，用于独立诊断、显式 UI 校验和 UI 快照能力自身的 P1 测试。

实际实现保存在 `core/global_settings_baseline.py` 的 `GLOBAL_SETTINGS_UI_SNAPSHOT_BASELINE` 常量中，并通过 `current_global_settings_ui_baseline()` 返回深拷贝。UI 基准只包含页面可稳定观察、且自动化能够可靠读取的字段。新的运行前校正和 `finally` 恢复流程不自动打开页面校验整份 UI 基准；`test_data/global_settings/` 当前只存放完整接口 POST 基准 JSON。

### 3.3 UI 动态快照能力

现有 `capture_global_settings_snapshot()` 和 `restore_global_settings_snapshot()` 继续保留，用于：

- 单独诊断页面状态；
- 手工工具或未来明确指定的场景；
- 对 UI 快照本身做 P1 验证。

它不再决定自动恢复目标，也不在接口恢复失败时自动执行恢复。自动流程不得把整份 UI 快照恢复作为接口恢复的 fallback。

## 4. API 固定恢复基准

以下 payload 是恢复的唯一权威目标：

```json
{
  "data_sync_config": {"type": 57},
  "browser_config": {"type": 32769, "expand_security_skip_super_admin": 0},
  "bookmark_config": {
    "status": false,
    "type": 1,
    "file_name": "",
    "cover_rule": 2,
    "env_group_ids": ["all"],
    "bookmark": ""
  },
  "env_config": {"env_mutil_open_type": true, "remote_inspector_type": true},
  "access_limit": {
    "status": false,
    "type": 2,
    "url_list": "http://127.0.0.1:57632/",
    "member_mroup_list": ["all"],
    "quick_selection_type": 1,
    "quick_selection_option": [1]
  },
  "proxy_detect_config": {
    "status": false,
    "name_list": "geek64.exe",
    "member_group_list": ["all"]
  },
  "app_version_limit_config": {
    "status": false,
    "app_version": "2.9.13",
    "role_ids": ["all"],
    "liunx_app_version": "2.8.14",
    "liunx_role_ids": ["all"]
  },
  "local_data_config": {
    "type": 0,
    "browser_type": "global",
    "data_type": 0,
    "synchronize": false,
    "frequency": 1,
    "interval": 1
  },
  "env_title_config": {
    "status": false,
    "env_title": "[{\"id\":0,\"key\":\"selection\",\"checked\":true,\"label\":\"\",\"disabled\":true,\"freeze\":true},{\"id\":1,\"key\":\"serialNum\",\"checked\":true,\"label\":\"序号\",\"disabled\":true,\"freeze\":false},{\"id\":2,\"key\":\"name\",\"checked\":true,\"label\":\"名称\",\"disabled\":false,\"freeze\":false},{\"id\":3,\"key\":\"remark\",\"checked\":false,\"label\":\"备注\",\"disabled\":false,\"freeze\":false},{\"id\":4,\"key\":\"groupName\",\"checked\":false,\"label\":\"分组\",\"disabled\":false,\"freeze\":false},{\"id\":5,\"key\":\"accountList\",\"checked\":false,\"label\":\"绑定账号\",\"disabled\":false,\"freeze\":false},{\"id\":8,\"key\":\"sort\",\"checked\":false,\"label\":\"自定义编号\",\"disabled\":false,\"freeze\":false},{\"id\":9,\"key\":\"lastOpenTime\",\"checked\":false,\"label\":\"最近打开时间\",\"disabled\":false,\"freeze\":false},{\"id\":8,\"key\":\"envTagList\",\"checked\":false,\"label\":\"标签\",\"disabled\":false,\"freeze\":false},{\"id\":10,\"key\":\"createTime\",\"checked\":false,\"label\":\"创建时间\",\"disabled\":false,\"freeze\":false},{\"id\":12,\"key\":\"proxyWay\",\"label\":\"\",\"checked\":false,\"disabled\":false,\"freeze\":false},{\"id\":13,\"key\":\"proxyHost\",\"label\":\"\",\"checked\":false,\"disabled\":false,\"freeze\":false},{\"id\":14,\"key\":\"exitIp\",\"label\":\"\",\"checked\":false,\"disabled\":false,\"freeze\":false},{\"id\":6,\"key\":\"environmentStatus\",\"checked\":false,\"label\":\"环境状态\",\"disabled\":false,\"freeze\":false},{\"id\":11,\"key\":\"operation\",\"checked\":true,\"label\":\"操作\",\"disabled\":true,\"freeze\":true}]",
    "member_group_list": ["all"]
  },
  "browser_show_rule_config": {"status": false, "env_label": 1},
  "update_frequency_config": {
    "status": true,
    "update_frequency": "WEEK",
    "auto_update": false,
    "auto_update_rule": "UPDATE_VERSION"
  },
  "env_data_sync": {
    "status": false,
    "role_ids": ["1849736955075411970"]
  },
  "env_sort_config": {"status": false, "order_by": "ENV_SERIAL_NUM", "sort": "DESC"},
  "env_page_config": {"status": false, "page_size": 20},
  "member_ip_limit_config": {"status": false, "type": "BAN", "ip": "", "vpn": false},
  "device_name_limit_config": {"status": false, "type": "BAN", "device_name": ""},
  "proxy_limit_config": {"status": false},
  "my_org_config": {"member_login_config": {"status": false, "ip_limit_num": 1}},
  "cookie_encrypt_config": {"status": true, "new_add": true},
  "local_network_config": {"status": false, "urls": ""},
  "env_open_limit_config": {"status": false, "member_group_list": ["all"], "open_limit": 5}
}
```

基准文件加载后需要做 schema 校验，要求 21 个顶层配置块全部存在，且禁止出现 `org_id`、`personal_config`、`debug_mode`、`del_package`、`role_id`、`white_list` 等 GET 只读字段。

完整 POST 基准中的 21 个顶层配置块全部保留，但当前 GET 语义校验白名单只有以下 10 个：

1. `browser_config`：浏览器设置。
2. `data_sync_config`：数据同步。
3. `bookmark_config`：书签。
4. `env_data_sync`：数据单向同步。
5. `proxy_detect_config`：禁用抓包软件。
6. `access_limit`：访问网站限制。
7. `local_data_config`：清除本地缓存。
8. `env_page_config`：环境列表分页设置。
9. `env_sort_config`：环境列表排序设置。
10. `env_title_config`：环境列表字段权限。

`env_config`、`app_version_limit_config`、`browser_show_rule_config`、`update_frequency_config`、`member_ip_limit_config`、`device_name_limit_config`、`proxy_limit_config`、`my_org_config`、`cookie_encrypt_config`、`local_network_config`、`env_open_limit_config` 继续存在于完整 POST 请求体中，但当前不参与固定基准 GET 语义比较。

当前用例影响：

- `test_18_clear_all_cache_every_open_sync_cloud.py` 和 `test_19_clear_all_cache_every_open_no_cloud_sync.py` 修改的清除本地缓存配置对应 `local_data_config`，纳入本轮 UI 保存后 GET 内容比较和按影响字段接口恢复；用例已有的“恢复为不清除”流程成功并经 GET 确认后，仍可跳过 `finally` 重复恢复。
- `test_20_extension_tamper_protection.py` 的目标配置属于 `browser_config.type` 位掩码，纳入 UI 保存后 GET 内容比较和按影响位接口恢复；用例已有的关闭恢复流程成功并经 GET 确认对应位回到固定基准后，可跳过 `finally` 重复恢复。

## 5. 接口契约与请求头

### 5.1 GET

```http
GET https://gin-server.dicloak.net/gin/v1/organization/{org_id}/org_config
Accept: application/json, text/plain, */*
X-Lang: zh_CN
X-Platform: APP
X-Token: {当前 APP 登录态 token}
X-Version: {当前 APP 版本号}
```

### 5.2 POST

```http
POST https://gin-server.dicloak.net/gin/v1/organization/{org_id}/org_config
Accept: application/json, text/plain, */*
Content-Type: application/json
X-Lang: zh_CN
X-Platform: APP
X-Token: {当前 APP 登录态 token}
X-Version: {当前 APP 版本号}
```

浏览器自动生成的 `user-agent`、`sec-ch-ua*`、`sec-fetch-*`、`priority` 不在代码中硬编码。`accept-language`、`cache-control`、`pragma` 可由页面环境自然携带；除非真实验证证明接口依赖，否则不作为业务契约字段。

成功条件：

- HTTP 状态码必须为 `200`，且响应 JSON 可解析；
- `code == 0`；
- GET 的 `data` 是对象；
- GET 的 `data.org_id` 与路径中的 `org_id` 一致；
- POST 返回 HTTP `200` 且 `code == 0` 后仍必须执行 GET 复查；新的自动恢复流程不再追加整页 UI 快照校验。

GET/POST 公共重试规则：

- 单次请求超时时间为 30 秒；
- 总尝试次数最多为 3 次，即首次请求包含在 3 次之内；
- 网络错误、连接/读取超时或 HTTP 状态码异常时进入重试；
- HTTP `200` 但 JSON 无法解析或 `code != 0` 也视为异常响应；
- 每次失败记录请求类型、尝试序号、耗时、HTTP 状态码和脱敏错误，不记录 token 或完整请求头；
- 连续 3 次失败后的最终用例结果规则见第 11 节，尚未确认的边界不得静默吞掉。

## 6. 运行时身份来源与安全

```javascript
const state = JSON.parse(localStorage.getItem("basic:state") || "{}");
const token = String(state.token || "").trim();
const orgId = String(state.userInfo?.orgId || "").trim();
const appVersion = (document.title.match(/V(\d+\.\d+\.\d+)/) || [])[1] || "";
```

- token 来源：`localStorage["basic:state"].token`。
- 组织 ID 来源：`localStorage["basic:state"].userInfo.orgId`，不能使用 `teamId`。
- APP 版本来源：页面标题中的 `Vx.y.z`。
- `orgConfigDataOrgId` 存在时应与 `userInfo.orgId` 一致。
- token 读取和 `fetch` 在同一段 APP 页面 JavaScript 中完成，token 不返回 Python。
- token、完整请求头、完整 `basic:state` 不得进入日志、异常、报告或基准文件。
- 用户提供的完整 curl 中含有真实 token，该值不能复制到代码或文档，实施前建议重新登录使其失效。

`X-Device-Id` 暂不发送。完整 curl 中虽然存在该请求头，但当前接口恢复实现不把它作为业务契约字段；后续只有在真实验证证明服务端强制依赖时才单独评估。

## 7. 严格触发边界

### 7.1 运行前检查边界

所有明确涉及全局设置的用例在业务步骤开始前调用统一前置检查：

```python
ensure_global_settings_baseline_before_case()
```

初始迁移清单为当前 28 条相关 P0：

- `tests/p0/global_settings/test_01_*.py` 至 `test_22_*.py`
- `tests/p0/environment_management/test_26_*.py` 至 `test_31_*.py`

实施时按“是否涉及全局设置”逐条复核，不仅依赖目录或文件名前缀自动推断。普通环境、成员、代理和扩展用例若不读取或修改全局设置，不调用该检查。

### 7.2 用例结束恢复边界

只有实际尝试写入全局设置的用例才进入结束恢复。建议每条用例显式维护：

```python
cleanup_required = False
restored_by_case_flow = False
```

- 仅打开、读取或断言全局设置时，`cleanup_required` 保持为 `False`。
- 在第一次可能提交配置修改前，将 `cleanup_required` 设为 `True`，避免请求已经落地但客户端漏收响应时遗漏清理。
- 用例自身执行恢复后，必须通过 GET 确认当前用例影响字段已经回到固定基准，才能将 `restored_by_case_flow` 设为 `True`。
- `finally` 仅在 `cleanup_required is True` 且 `restored_by_case_flow is False` 时调用统一恢复。
- 恢复逻辑不放进 runner 的通用 `finally`，也不放进所有用例共享的 `BaseTestCase.tearDown()`。

### 7.3 受影响字段声明

每条会修改全局设置的用例必须声明其影响的接口字段路径，例如：

```python
AFFECTED_ORG_CONFIG_PATHS = {
    "browser_config.type",
}
```

涉及组合配置时声明全部有效字段：

```python
AFFECTED_ORG_CONFIG_PATHS = {
    "access_limit.status",
    "access_limit.type",
    "access_limit.url_list",
    "access_limit.member_mroup_list",
}
```

推荐调用结构：

```python
ensure_global_settings_baseline_before_case()
cleanup_required = False
restored_by_case_flow = False
primary_error = None
try:
    cleanup_required = True
    # 修改设置、业务验证，以及可选的用例内恢复
except BaseException as exc:
    primary_error = exc
finally:
    if cleanup_required and not restored_by_case_flow:
        restore_affected_global_settings(
            affected_paths=AFFECTED_ORG_CONFIG_PATHS,
            primary_error=primary_error,
        )
```

具体实现可以用上下文管理器消除重复，但受影响字段、修改边界和恢复跳过条件必须清晰可见、可审查。

## 8. 新执行与恢复流程

```text
涉及全局设置的用例开始
  -> 读取并校验固定 API 基准文件
  -> 从当前 APP 页面读取 orgId/token/version
  -> GET 当前组织配置（30 秒，最多 3 次）
  -> 只比较 10 个 GET 校验白名单配置块，忽略其他配置
  -> 已符合固定基准：直接开始用例业务流程
  -> 不符合固定基准：POST 整份固定基准（30 秒，最多 3 次）
  -> GET 复查固定基准
  -> 复查仍有语义差异：记录 WARNING 和脱敏差异，继续业务流程
  -> 用例准备修改设置时标记 cleanup_required=True
  -> 通过 UI 勾选、开启或填写目标配置
  -> 保留原有局部 UI 校验，确认目标控件状态已改变且没有非预期控件联动
  -> UI 保存按照第 10 节监听 POST，不等待“保存成功”提示
  -> 执行业务验证
  -> 用例自身恢复时，只用 GET 验证本用例影响字段
  -> 自身恢复验证成功：restored_by_case_flow=True
  -> 进入 finally
  -> 未修改设置或已经自行恢复：跳过 finally 接口恢复
  -> 否则 GET 当前配置
  -> 只比较本用例 AFFECTED_ORG_CONFIG_PATHS
  -> 相关字段已符合固定基准：直接结束
  -> 相关字段不符合：以当前 GET 的可写值为主体，仅合并相关字段的固定基准值
  -> 从完整固定基准补齐 GET 缺失但 POST 必需的字段
  -> 过滤 GET 只读字段并 POST 完整合法请求体
  -> GET 复查本用例影响字段
  -> 不打开全局设置页做整份 UI 校验，不执行 UI 快照恢复
```

关键要求：

- 运行前先 GET，只有与固定基准不一致时才 POST，允许 no-op 跳过。
- 用例结束只恢复当前用例影响字段，但传输给 POST 的请求体必须完整；“只恢复部分字段”不能实现成“只发送部分字段”。
- 用例结束禁止把完整固定基准中的所有值原样覆盖到当前组织配置，非本用例影响字段应尽量保持当前 GET 值。
- 用例业务验证失败或抛异常时，只要曾尝试修改设置，仍必须尽最大可能执行 `finally` 恢复。
- 用例声称已经自行恢复时，必须有 GET 复查证据，不能仅凭 UI 方法未抛异常跳过 `finally`。
- 自动恢复链不调用 `capture_global_settings_snapshot()` 或 `restore_global_settings_snapshot()`。
- “不做整份 UI 快照校验”不等于删除控件级 UI 校验；所有原有勾选状态、开关状态、下拉选中值和非预期联动断言继续保留。
- 运行前复查仍有语义差异时按已确认规则警告后继续；接口完全不可用和 `finally` 恢复失败的最终结果规则仍需确认。

## 9. GET 语义校验

建议纯函数：

```python
semantic_org_config_diff(expected_baseline, actual_get_data)
```

比较方向固定为“10 个白名单配置块是期望子集，GET 是实际全集”：只遍历 `browser_config`、`data_sync_config`、`bookmark_config`、`env_data_sync`、`proxy_detect_config`、`access_limit`、`local_data_config`、`env_page_config`、`env_sort_config`、`env_title_config`，实际响应中的其他顶层块不形成差异。白名单配置块内部也只比较期望请求体声明且当前模式实际生效的字段。

### 9.1 带 `status` 的功能

- 基准 `status == false`：只校验 GET 中该功能确实为 `false`，忽略关闭状态下的内容字段。
- 基准 `status == true`：校验 `status == true`，并校验当前开启模式实际使用的内容字段。
- 如果某个字段只在特定 `type` 下生效，只在该类型下参与比较。

当前固定基准的 10 个校验块中，带 `status` 的功能均为关闭状态，因此固定基准比较只检查关闭状态。UI 保存后使用捕获请求体比较时，如果某个白名单功能被本次请求开启，则需要继续校验该开启模式的有效内容。

### 9.2 不带 `status` 的配置

- `data_sync_config`：校验 `type` 位掩码。
- `browser_config`：校验 `type` 位掩码和当前模式有效的附属字段。当前确认“禁止查看网站密码”“禁止打开浏览器开发者工具”“禁止管理/移除扩展，以及从本地安装扩展至浏览器”“禁止成员访问谷歌扩展商店和扩展设置页面”“开启扩展加密并防止篡改”等浏览器设置均由该位掩码中的不同位控制。
- `local_data_config`：`type == 0` 代表固定基准的“不清除”，此时只校验 GET 中 `type == 0`；本次 UI POST 设置为其他清除模式时，再校验该模式实际生效的 `data_type`、`synchronize`、`frequency`、`interval` 等内容。

`browser_config.type` 的用例结束恢复必须按受影响位合并，不能用固定基准整数整体覆盖：

```python
restored_type = (
    (current_type & ~affected_browser_mask)
    | (baseline_type & affected_browser_mask)
)
```

`test_20_extension_tamper_protection.py` 的 `affected_browser_mask` 使用扩展防篡改对应位。该位值尚未确认，实施前通过“只切换扩展防篡改 -> 捕获 POST 前后 `browser_config.type` -> 计算 XOR -> 立即恢复并 GET 复查”的受控流程建立映射。

### 9.3 GET/POST 不对称

已知 GET 示例可能缺失：

- `bookmark_config.file_name`
- `access_limit.quick_selection_option`
- `local_data_config.browser_type`

对应功能关闭或 `local_data_config.type == 0` 时，上述内容字段不参与比较；功能开启或使用其他清除模式时，按实际生效模式决定字段是否必须存在并相等。GET 缺失 `local_data_config.browser_type` 时不能直接与 POST 中的 `"global"` 判为不一致，需要单独定义该模式的等价规则。

GET 单次请求超时为 30 秒，总尝试次数最多 3 次。语义复查不再使用额外的 10 秒密集轮询；每次失败只输出脱敏后的字段路径和期望/实际控制状态。

## 10. UI 保存接口监听机制

### 10.1 保留控件级 UI 校验

接口监听改造不能删除现有页面对象中的局部 UI 状态确认。每次修改配置后、点击“确定”前，继续执行当前能力能够提供的校验，例如：

- 复选框勾选或取消后，等待该复选框的 checked 状态达到预期；
- 开关开启或关闭后，等待 switch 状态达到预期；
- 下拉框、输入框、单选项或组合配置修改后，重新读取当前选中值或输入值；
- 原有 `_assert_only_checkbox_changed()` 一类非预期联动检查继续执行，确认没有修改当前用例目标之外的可观察控件；
- 原有保存后重新进入页面并确认目标控件持久状态的逻辑，如该用例已经存在且不与新接口流程冲突，也继续保留。

证据层次固定为：

```text
局部 UI 状态正确
  -> 点击“确定”并确认目标 POST 成功
  -> GET 确认服务端持久化内容正确
  -> 执行业务效果验证
```

局部 UI 校验失败时，不点击“确定”，不等待接口，也不通过直接 POST 补写目标状态；该用例按原 UI 操作失败处理。

### 10.2 替换当前保存完成判定

改造前 `_wait_save_finished()` 会等待页面 loading 消失，然后继续等待“保存成功”提示。当前实现已经：

- 保留页面 loading 结束等待；
- 完全取消“保存成功”提示等待；
- “保存成功”提示是否出现不再影响用例结果，也不再作为接口监听失败时的 fallback；
- 新增对当前组织 `org_config` POST 的响应监听。

### 10.3 监听时序

必须先注册监听，再点击“确定”，避免接口响应过快造成漏收：

```text
注册目标响应监听
  -> 点击“确定”
  -> 等待页面 loading 结束
  -> 等待目标 POST 接口响应完成
  -> loading 和接口响应两个条件都完成后，才进入后续判断
```

目标请求匹配条件：

```text
method == POST
URL == https://gin-server.dicloak.net/gin/v1/organization/{当前团队ID}/org_config
```

每次尝试结束后必须注销监听，失败重试前重新注册，不能让多个监听器同时残留。

### 10.4 单次成功条件

- 页面 loading 已结束；
- 在 20 秒内捕获到目标 POST 响应；
- HTTP 状态码为 `200`；
- 响应 JSON 可解析且 `code == 0`。

不再检查页面顶部“保存成功”提示。

### 10.5 UI 保存重试

- UI 保存总尝试次数最多为 3 次，首次点击包含在 3 次之内；
- 单次等待目标 POST 响应的超时时间为 20 秒；
- 监听超时、网络问题、服务器异常、HTTP 状态码异常、响应 JSON 异常或 `code != 0` 时进入下一次尝试；
- 每次失败记录尝试序号、耗时、HTTP 状态码和脱敏错误，不记录 token、完整请求头或完整敏感 payload；
- 重试前应清理上一轮监听状态，并确认页面仍具备可提交条件。

### 10.6 保存后的 GET 复查

目标 POST 响应满足成功条件后，保存该次请求的完整 POST body 不可变副本，再调用 GET。GET 使用第 5 节的公共规则，单次超时 30 秒，总尝试次数最多 3 次。

保存后比较规则：

- 期望值来自本次 UI 实际发送并成功响应的 POST 请求体，不使用固定恢复基准作为本次保存期望值。
- 只比较 10 个白名单配置块；捕获请求体和 GET 中的其他配置块均忽略。
- 带 `status` 的白名单配置关闭时只检查 GET 是否关闭；开启时才检查当前模式实际生效的具体内容。
- `browser_config.type` 和 `data_sync_config.type` 按位掩码规则比较并输出可读差异。
- 本次 UI 只修改 10 个白名单之外的配置时，接口监听仍校验 HTTP `200` 和 `code == 0`，但不对该目标配置做 GET 内容比对；其业务验证和恢复继续由该用例现有流程承担。

### 10.7 已解决：保存期望与恢复基准分离

原冲突按以下方式解决：

1. UI 保存校验的期望来源是本次捕获的 POST 请求体。
2. 用例开始前和结束恢复的期望来源是预先准备的完整固定基准请求体。
3. UI 保存后不再因为与固定基准不同而立即执行固定恢复，避免在业务验证前撤销本次用例设置。
4. 两类 GET 比较都只检查 10 个白名单配置块。

仍待确认的只剩失败处置：POST 已返回 HTTP `200` 且 `code == 0`，但 GET 连续复查后仍与本次捕获请求体不一致时，是将 UI 保存判定为 ERROR，还是进入其他重试流程。

## 11. 异常优先级

已经确认：

- 运行前固定基准 POST 后，GET 成功但仍存在语义差异时记录 WARNING，并继续用例流程。
- UI 保存后的 GET 使用本次成功 POST 请求体作为期望；运行前和恢复 GET 使用完整固定基准作为期望。
- 所有自动 GET 内容比较仅覆盖 10 个白名单配置块。
- 用例未尝试修改全局设置时，不执行 `finally` 恢复。
- 用例自身恢复并经 GET 确认受影响字段符合固定基准后，不重复执行 `finally` 恢复。
- 业务验证失败或执行错误时，只要曾尝试修改全局设置，仍执行当前用例受影响字段恢复。
- 自动流程不再以整页 UI 快照校验结果改变用例状态。

尚待确认：

- 运行前 GET/POST 连续 3 次均请求失败时，是将用例标记为 ERROR，还是记录警告后继续。
- `finally` 中 GET/POST 连续 3 次失败，或恢复后相关字段仍不符合固定基准时，最终是否提升为 ERROR。
- UI 保存后的 GET 连续复查仍与本次成功 POST 请求体不一致时，最终是 ERROR 还是进入其他重试流程。

无论最终规则如何，业务断言和恢复异常都必须同时保留在结果上下文中，清理异常不能覆盖并丢失原始业务失败原因。

## 12. 建议代码边界

### 12.1 `core/org_config_api.py`

```python
class OrgConfigApiClient:
    def get_org_config(self) -> dict[str, object]: ...
    def post_baseline(self, payload: dict[str, object]) -> None: ...
    def compare_baseline_subset(self, actual: dict[str, object]) -> list[ConfigDiff]: ...
    def post_and_verify(self, payload: dict[str, object]) -> dict[str, object]: ...
```

职责：运行时身份、页面内 `fetch`、30 秒超时与最多 3 次尝试、HTTP/业务状态检查、基准子集语义差异和脱敏错误。

### 12.2 `core/global_settings_recovery.py`

```python
class GlobalSettingsBaselineRecovery:
    def ensure_before_case(self) -> None: ...
    def restore_affected_paths(
        self,
        affected_paths: set[str],
        *,
        primary_error=None,
    ) -> None: ...
```

职责：加载固定 API 基准、执行运行前 GET/必要时 POST/GET、以当前 GET 为主体合并当前用例受影响字段、执行用例结束 POST/GET，并合并业务异常与恢复异常。不负责自动 UI 快照校验或 UI 恢复。

### 12.3 `pages/global_settings_page.py`

- UI schema 升级到 2。
- 增加两个新安全开关的采集、强比较和显式 UI 恢复支持。
- 保留只读 `assert_global_settings_ui_baseline()` 和 `restore_global_settings_snapshot()`，仅供显式诊断或工具使用。
- 保留各设置方法现有的复选框、开关、下拉值和非预期联动 UI 断言；接口监听只替换保存完成证据，不替换控件操作证据。
- 改造 UI 保存完成判定：等待 loading 结束和目标 POST 响应，取消等待“保存成功”提示。
- UI 保存响应监听单次 20 秒、最多 3 次；监听必须先于“确定”点击注册，并在每次尝试后注销。
- UI 保存后的 GET 使用本次成功 POST 请求体作为期望，并只比较 10 个白名单配置块；不执行固定基准恢复。

## 13. 迁移策略

### 阶段 A：纯函数和 UI 快照升级

- 增加完整接口基准 JSON、版本化 UI 基准常量及 schema 校验。
- 实现 GET 语义比较纯函数。
- 升级 UI 快照 schema 2 和 P1。
- 不调用真实 POST。

### 阶段 B：受控接口恢复

- 实现 30 秒超时、最多 3 次尝试的 GET/POST 客户端。
- 实现运行前 GET 子集比较、必要时 POST 固定基准、GET 复查和差异警告。
- 实现按受影响字段合并当前 GET 数据并恢复，确认不会覆盖其他配置。
- 验证整个自动流程不会调用 UI 快照恢复。

### 阶段 C：逐条迁移修改类用例

- 迁移当前识别的 28 条用例。
- 删除这些用例对“运行前动态快照作为恢复目标”的依赖。
- 为每条修改类用例声明 `AFFECTED_ORG_CONFIG_PATHS` 或位掩码影响范围，并保留清晰可见的修改和恢复边界。
- 对具备用例内恢复能力的流程增加 GET 复查，复查成功后跳过 `finally` 重复恢复。
- 逐条验证未修改、修改后自行恢复、修改后依赖 `finally`、业务 FAIL 和业务 ERROR 路径。

### 阶段 D：UI 保存监听

- 盘点并保留各设置方法点击“确定”前后的局部 UI 状态校验。
- 在点击“确定”前注册当前组织 `org_config` POST 响应监听。
- 等待 loading 结束和接口响应，删除“保存成功”提示等待。
- 覆盖 20 秒超时、HTTP 异常、业务 `code` 异常和最多 3 次尝试。
- 使用本次成功 POST 请求体对 10 个白名单配置块执行保存后 GET 语义比较。
- 保存后 GET 最终不一致的结果处置确认后，再补充该分支。

### 阶段 E：回归

- 完整 P1。
- 全局设置 P0。
- 环境管理 `test_26` 至 `test_31`。
- 故意制造业务断言失败，确认只恢复当前用例影响字段。
- 运行一个不修改全局设置的用例，确认零次调用组织配置恢复接口。

## 14. 测试计划

### 14.1 P1

- 固定 API 基准含完整 21 个顶层配置块。
- 任何恢复 POST 都发送完整合法请求体，局部受影响字段不能作为局部 payload 单独发送。
- 基准出现只读字段时拒绝执行。
- token、orgId、version 缺失时零 POST。
- 组织 ID 不一致时零 POST。
- 运行前 GET 已符合基准时零 POST。
- 运行前 GET 只比较 10 个白名单配置块，忽略其他响应配置。
- 运行前不一致时执行 POST -> GET；复查仍不一致时记录 WARNING 并继续。
- GET/POST 单次超时 30 秒，总尝试次数为 3 次。
- HTTP 状态码不是 `200`、JSON 无效或 `code != 0` 时进入异常重试。
- GET 状态关闭时忽略关闭功能的内容差异。
- GET 状态开启时校验当前模式下生效字段。
- `browser_config.type` 比较和恢复支持按单个或多个受影响位处理，未受影响位保持当前 GET 值。
- 扩展防篡改使用 `browser_config.type` 的已确认位映射，不读取或比较 `cookie_encrypt_config`。
- `local_data_config.type == 0` 时只比较“不清除”控制值，忽略 `data_type`、`synchronize`、`frequency`、`interval` 和缺失的 `browser_type`。
- `local_data_config` 使用其他清除模式时，比较该模式实际生效的内容，并覆盖 `browser_type` 在 POST/GET 中不对称的等价规则。
- GET `org_id` 不一致时 ERROR。
- GET/POST 异常只返回脱敏字段路径和协议信息。
- UI schema 2 包含两个新增安全开关。
- 每个设置方法在点击“确定”前仍验证目标控件状态；局部 UI 校验失败时零保存点击、零接口补写。
- 原有非预期复选框联动检查继续执行，不因新增接口校验而删除。
- UI 点击“确定”前注册 POST 监听，并在 loading 与接口响应均结束后返回。
- UI 保存不调用 `_wait_save_success_message()`，提示出现或缺失均不影响结果。
- UI 保存监听单次超时 20 秒，总尝试次数为 3 次，每次结束后注销监听。
- UI 保存后使用该次成功 POST 请求体作为期望，只比较 10 个白名单配置块。
- 未尝试修改设置时不执行 `finally` 恢复。
- 用例内恢复只有 GET 确认受影响字段后才能跳过 `finally`。
- `finally` 只比较和恢复用例声明的字段或位掩码，POST payload 保持完整并保留其他当前配置。
- 自动流程不调用整份 UI snapshot restore/ensure 方法。
- 未涉及全局设置的用例不会创建恢复客户端，不发送 GET/POST。
- 日志、异常和报告不包含 token、完整 headers 或完整敏感 payload。

### 14.2 真实验证

1. 只读重新采集 UI，确认保留的诊断基准与第 2.2 节一致。
2. 运行前 GET 已符合基准时确认不发送 POST。
3. 人为制造一个基准字段不一致，确认执行 POST -> GET 后开始业务流程。
4. 人为制造 POST 后仍存在语义差异，确认记录警告且不触发 UI 恢复。
5. 修改一个设置后触发 `finally`，确认只恢复当前用例声明字段，其他配置保持当前值。
6. 让用例自身完成恢复并通过 GET，确认 `finally` 不重复 POST。
7. 点击“确定”时确认不等待“保存成功”提示，并捕获正确组织的 POST 响应。
8. 对复选框、开关和组合配置分别确认原有局部 UI 状态校验仍先于“确定”点击执行。
9. UI 保存成功后确认以捕获请求体为期望，且只比较 10 个白名单配置块。
10. 真实运行 `test_18` 和 `test_19`，确认 `local_data_config` 保存后 GET 比较、自身恢复确认和 `finally` 跳过符合设计。
11. 真实运行 `test_20`，确认扩展防篡改通过 `browser_config.type` 对应位完成保存后 GET 比较和按位恢复，且不依赖 `cookie_encrypt_config`。
12. 人为制造 UI 保存接口超时和 5xx，确认单次 20 秒且总尝试次数最多 3 次。
13. 人为制造业务断言失败，确认仍执行当前用例影响字段恢复。
14. 运行普通不涉及全局设置的用例，确认接口调用计数为 0。

## 15. 验收标准

- 涉及全局设置的用例在业务步骤前执行 GET 基准检查，内容比较只覆盖 10 个白名单配置块。
- 运行前配置不一致时执行 POST -> GET；仍不一致时记录脱敏警告并继续用例。
- 只有实际尝试修改全局设置的用例触发 `finally` 恢复。
- 用例自身恢复经 GET 确认成功后，不重复执行 `finally` 恢复。
- `finally` 只恢复当前用例声明的受影响字段或位，但发送的 POST 请求体必须完整，其他配置不做有意覆盖或 UI 检查。
- 原有勾选、开启、选值和非预期联动的局部 UI 校验全部保留，并在保存接口等待前完成。
- UI 点击“确定”后等待 loading 结束和目标 POST 响应，不再等待“保存成功”提示。
- UI 保存监听单次超时 20 秒、最多 3 次；直接 GET/POST 单次超时 30 秒、最多 3 次。
- GET/POST 必须校验 HTTP `200`、合法 JSON 和业务 `code == 0`。
- 自动流程不执行整页 UI 快照校验或 UI 快照恢复。
- UI 快照仍支持独立采集、比较和显式恢复，并已覆盖两个新增安全开关。
- 当前组织不匹配时零写入。
- token 和完整请求头不进入 Python、基准、日志、异常或报告。
- 不涉及全局设置的用例对组织配置接口零调用。
- UI 保存后的 GET 以本次成功 POST 请求体为期望，并只比较 10 个白名单配置块。

## 16. 补充确认

- 组织配置 GET/POST 暂不发送 `X-Device-Id`。
- 不把完整 curl 中的设备 ID 或 token 写入代码、配置、日志或文档。
- 固定 API 基准、运行前 GET 校正、按受影响字段恢复、取消自动 UI 恢复和接口重试参数已经确认。
- UI 保存完成条件已确认改为“loading 结束 + 目标 POST 响应成功”，取消等待“保存成功”提示。
- 第 10.7 节的保存后比较目标冲突已经解决；保存后 GET 最终不一致的失败处置仍待确认。

## 17. 2026-08-25 实现状态

- 已落地完整 21 块版本化 POST 基准、10 块 GET 语义白名单、状态开关语义比较和两类 `type` 位掩码比较。
- 已落地当前 APP token、团队 ID、版本号读取和 APP 页面内 GET/POST；未发送 `X-Device-Id`，Python 侧只接收是否存在 token，不接收 token 内容。
- 已落地 UI 保存 POST 监听、20 秒/3 次点击重试、直接接口 30 秒/3 次重试、HTTP 200 与业务 `code=0` 校验，以及成功 POST 请求体到 GET 的语义复查。
- 已迁移 22 条全局设置 P0 和环境管理 `test_26` 至 `test_31`；相关用例不再自动采集整页 UI 快照，也不在 finally 重进设置页，只检查和恢复本用例影响范围。
- 保留了 `capture_global_settings_snapshot()`、`assert_current_global_settings_ui_baseline()` 和无接口恢复会话时的显式 UI 恢复能力。固定接口基准实测使“禁止查看网站密码”处于开启状态，UI 基准已同步。
- 当前实现对“UI POST 已成功，但连续 GET 语义复查仍不一致”采用明确抛出 `AssertionError` 的保守策略，并由用例 finally 继续执行受影响范围恢复；若后续确认应忽略该差异，只需调整这一个策略点。
- 完整 P1 已通过：`Ran 188 tests ... OK`。真实分页设置用例优化前后均通过，耗时由 `61.31s` 降至 `50.05s`；真实 DevTools 用例通过，并验证了位掩码最终恢复的 GET -> 完整 POST -> GET 链路。

## 18. 2026-08-25 真实模块回归与耗时结论

- 全局设置 `test_01` 至 `test_22` 通过 UI 本机执行完整回归：`total=22 passed=22 failed=0 errors=0 skipped=0 flaky=0`，运行日志为 `logs/run_20260825_191718_153947.log`。22 条用例的运行前接口基准检查均在首次 GET 后确认匹配，未发生不必要的基准 POST。
- 环境管理 `test_26` 至 `test_31` 六条全局数据同步关联用例通过联合回归：`total=6 passed=6 failed=0 errors=0 skipped=0 flaky=0`，运行日志为 `logs/run_20260825_194757_106317.log`。
- 六条环境用例本次用例内部耗时合计 `509.67s`，旧 UI 快照恢复流程为 `573.17s`，减少 `63.50s`，约 `11.1%`。单条耗时依次为 `76.20s`、`76.22s`、`78.13s`、`95.32s`、`91.40s`、`92.40s`。
- 新环境三条平均 `93.04s`，比预置环境三条平均 `76.85s` 多 `16.19s`；差异主要来自每条约 `10.2s` 的环境创建、约 `3.5s` 的环境删除、首次登录后的 `2s` 等待和附加交互。
- 三种存储类型的耗时接近，慢点不在 Cookie、Local Storage 或 IndexedDB 接口。六条合计执行 18 次完整环境启动/关闭和内核 CDP 接入；模拟站会话接口通常为 `0~3ms`，首次登录约 `62~63ms`，组织配置 GET 约 `0.39~0.41s`。
- 当前仍可继续优化的主要成本为重复环境列表加载、用例间 APP CDP 重连、正常成功路径与通用恢复的重复模块归位、固定 `3s` 云同步等待和新环境首次登录后的固定 `2s` 等待。缓存删除当前仍会清理缓存根目录下全部合规 19 位数字目录，后续应优先评估只删除目标环境缓存目录，以降低耗时波动和数据影响范围。
- 上述耗时优化不改变本方案的恢复边界：涉及全局设置的用例仍在开始前执行接口基准检查；只有实际尝试写入时才在结束阶段检查受影响配置；原有 UI 控件勾选、开关、下拉值和意外联动断言继续保留。
